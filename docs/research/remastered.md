# Remastered Notes

Что делаем СЕЙЧАС (не зависит от бэка)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ФАЗА 1: ЧИСТКА (можно прямо сейчас)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    1. Удалить backend/               ← целиком, нам не нужен
    2. Удалить finance из кабинета    ← решили отложить
       - app/[lang]/(cabinet)/finance/
       - modules/finance/ (если есть)
       - Убрать из Sidebar навигации
       - Убрать из proxy.ts cabinetRoutes
    3. Обновить .env.example          ← убрать backend-специфичные переменные
    4. Обновить docs/                 ← уже частично сделано в ветке

Что делаем ПАРАЛЛЕЛЬНО (пока бэк не готов)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ФАЗА 2: ПОДГОТОВКА API-СЛОЯ
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    shared/api/client.ts сейчас:
    ┌─────────────────────────────────────────┐
    │ base URL:  http://localhost:8000        │
    │ пути:      /api/v1/auth/*               │
    │ формат:    { data: T, meta, error }     │
    │ auth:      cookies + refresh в клиенте  │
    │ headers:   Content-Type только          │
    └─────────────────────────────────────────┘

    Что нужно изменить:
    ┌─────────────────────────────────────────┐
    │ base URL:  NEXT_PUBLIC_API_URL (env)    │  ← уже так, ок
    │ пути:      /v1/cabinet/*  (или ?)       │  ← ЖДЁМ бэкендера
    │ формат:    ???                          │  ← ЖДЁМ бэкендера
    │ auth:      httpOnly cookies от бэка     │  ← credentials: include ✅
    │ headers:   + x-platform: web            │  ← ДОБАВИТЬ сейчас
    └─────────────────────────────────────────┘

Главная мысль: API клиент уже неплохо написан. credentials: "include" есть, env переменная для URL есть. Нужно только:

- добавить x-platform: web header
- адаптировать пути эндпоинтов когда бэк их даст
- возможно поправить формат ответа

Что ЖДЁТ бэкендера

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ФАЗА 3: ИНТЕГРАЦИЯ (после API контракта)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    Точки интеграции фронта с бэком:

    ┌──────────────┬────────────────────────┬──────────────┐
    │ Фронт модуль │ Текущий эндпоинт       │ Что менять   │
    ├──────────────┼────────────────────────┼──────────────┤
    │ auth/context │ /api/v1/auth/telegram  │ URL + ответ  │
    │              │ /api/v1/auth/refresh   │ URL + ответ  │
    │              │ /api/v1/auth/logout    │ URL          │
    │              │ /api/v1/auth/me        │ URL + User   │
    │              │ /api/v1/auth/dev-login │ удалить?     │
    ├──────────────┼────────────────────────┼──────────────┤
    │ profile/     │ GET  /api/v1/profile   │ URL + поля   │
    │              │ PATCH /api/v1/profile  │ URL + поля   │
    ├──────────────┼────────────────────────┼──────────────┤
    │ admin/       │ GET  /api/v1/admin/*   │ URL + ответ  │
    │              │ POST/DELETE roles      │ URL + ответ  │
    └──────────────┴────────────────────────┴──────────────┘

    Критичный вопрос для бэкендера:
    ─────────────────────────────────
    "Какой формат User объекта будет приходить?"

    Наш текущий User:          BotUser в diaverseapi:
    ┌─────────────────┐        ┌─────────────────────┐
    │ id: UUID        │        │ id: UUID            │
    │ tg_id: number   │        │ tg_id: bigint       │
    │ display_name    │        │ ??? (какие поля?)   │
    │ avatar_url      │        │ ???                 │
    │ email           │        │ ???                 │
    │ phone           │        │ ???                 │
    │ bio             │        │ ???                 │
    │ social_links    │        │ ???                 │
    │ roles: Role[]   │        │ ??? (нет RBAC)     │
    └─────────────────┘        └─────────────────────┘

Итого: масштаб "переделки"

    ПЕРЕДЕЛКА ПРОЕКТА — РЕАЛЬНЫЙ МАСШТАБ
    ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    🗑️  Удалить:
       └── backend/                    (~30 файлов)

    ✏️  Изменить (5 файлов):
       ├── shared/api/client.ts        (URL пути, header, формат)
       ├── modules/auth/context.tsx     (User тип, URL-ы)
       ├── modules/auth/.../LoginPage   (URL-ы)
       ├── modules/profile/ProfileForm  (URL-ы, поля)
       └── modules/admin/UsersTable     (URL-ы, формат)

    ✏️  Мелкие правки:
       ├── proxy.ts                    (убрать /finance)
       ├── Sidebar.tsx                 (убрать finance из навигации)
       └── docs/, .ai-factory/        (уже частично сделано)

    ✅  Без изменений (~90% кода):
       ├── Лендинг (9 секций)
       ├── Cabinet layout, theme
       ├── i18n система
       ├── GSAP анимации
       ├── UI компоненты
       └── Все stub-страницы кабинета

Это не "переделка всего проекта" — это чистка backend/ + точечная адаптация 5 файлов API-слоя. Фронтенд в целом остаётся.

---

Готов к действию? Тогда предлагаю:

1. /aif-plan на фазу 1 (чистка) — можно делать прямо сейчас
2. Параллельно — сформулировать для бэкендера API контракт (список эндпоинтов + формат ответа, который нам нужен)
3. Фаза 3 (интеграция) — когда бэк даст URL

Или хочешь ещё что-то обсудить?

✻ Sautéed for 56s
