# Daily Work - 2026-06-03 - safiu

## Public digest


- Исправлено поведение апгрейда отсеков фабрики: пакетная покупка уровней 1-5 теперь доступна только до первого открытия отсека, а после частичной прокачки не показывается как нерабочая опция.
- Улучшена обработка заблокированных ответов при апгрейде, чтобы пользователь видел причину отказа вместо визуального бездействия.
- Ветки разработки frontend и backend слиты в production-ветки для выката актуальных изменений.
- Исправлен CI-блокер frontend lint после production-merge: React effect lint errors устранены без расширения области правок.

- Обновлен визуальный стиль рейдовых ловушек и модалок спасения: фоновые изображения стали основой плашек, а текстовые блоки переведены в прозрачный glass-стиль с мягким красным акцентом.
- Подготовлен план тестового внедрения Tribute как новой платежки для адвент-календаря с ограниченным и безопасным первым запуском.
- Уточнен план внедрения Tribute для адвент-календаря: добавлены проверки Telegram-привязки, безопасная обработка платежных ссылок и более полный сценарий тестирования перед запуском.
- Начата backend-реализация Tribute: добавлены базовые настройки, feature-флаги и безопасный integration-клиент для будущего тестового подключения в адвент-календаре.
- Tribute зарегистрирован как отдельный cabinet payment provider только для authenticated Advent rollout, без гостевого checkout.
- Для Tribute подключена нормализация ошибок в общий cabinet payment error contract с защитой от утечки платежных ссылок и Telegram customer id.
- Для Tribute добавлена Advent quote strategy: USD/USDT цены переводятся в RUB через существующий FX-сервис, RUB поддерживается напрямую.
- Для Tribute реализован backend hosted checkout adapter для authenticated Advent пользователей с Telegram-привязкой.
- Для Tribute добавлен callback endpoint с локальной relay-token защитой и безопасным target lookup.
- Для Tribute реализована server-side reconciliation через `GET /orders/{id}` с безопасным status mapping и review-путями.
- Для Advent capability API добавлена user-aware доступность Tribute: без Telegram-привязки метод disabled и не выбирается default-ом.
- В Advent backend/frontend диагностике закрыто логирование raw hosted checkout URLs и token-bearing redirect ссылок.
- Во frontend Advent добавлена metadata-поддержка `tribute-hosted`: метод отображается как Tribute и прокидывается в checkout route.
- Добавлены runtime notes и env example для безопасного Tribute rollout/rollback без публикации секретов.
- Закрыты backend test gaps для Tribute client, checkout TTL, callback idempotency, quote и reconciliation сценариев.
- Закрыты frontend-регрессии для Tribute в Advent: label платежки, выбор provider-а, return page и безопасная диагностика покрыты тестами.
- Выполнена targeted verification для тестового Tribute rollout в Advent: backend и frontend проверки прошли без live Tribute API и без записи секретов.
- Подготовлена схема компенсации старых Telegram-рейдов при переходе на новую механику: фиксированные пакеты по локации, прогрессу, типу оплаты и NFT-флагу без случайных шкатулок.

## Internal log


- Обновлен `diaweb`: `FactoryUpgradeDialog` скрывает `package_1_5` после открытия отсека, отправляет пакетную покупку с `targetLevel: 5` и показывает `blocked`-ответы для апгрейда частей.
- Обновлены frontend-регрессии в `FactoryDialogs.test.tsx` для сценариев неоткрытого отсека, частично прокачанного отсека и отображения `blocked`-ошибки.
- Обновлен `diaverseapi`: добавлен тест `test_compartment_package_purchase_is_blocked_after_partial_upgrade`, закрепляющий backend-guard на пакет после частичной прокачки.
- Проверки: `npm run test -- __tests__/modules/factory/FactoryDialogs.test.tsx`, `npm run typecheck`, `.venv\Scripts\python.exe -m pytest app\factory\tests\test_compartment_service.py -q`, `git diff --check`.
- Выполнен targeted GBrain sync для `diaweb-code` и `diaverseapi-code`.
- Выполнен merge `diaweb/dev` в `diaweb/master` и push production-ветки; локальная рабочая копия возвращена на `dev`.
- Выполнен merge `diaverseapi/dev` в `diaverseapi/main` и push production-ветки; локальная рабочая копия возвращена на `dev`.
- В `diaweb` устранены 5 `react-hooks/set-state-in-effect` errors: deferred preview URL state updates, factory craft notice updates, and raid tooltip initial positioning. Проверки: `npm run lint`, `npm run typecheck`, targeted Vitest for copywriting broadcasts, factory compartment screen, and raid location stack. Commit `4d4137a` pushed to `dev`, then merged and pushed to `master`.

- В diaweb обновлены RaidTrapCard, RaidSlotsSheet, RaidLocationDetail и raidShell.module.css: добавлен показ имени/иконки пойманного пета через фронтовой matching по user_character_id/trap_id, trap-card переведена на full-bleed state-art, flowPanel/trap/rescue outcome/drawer/warning/status блоки унифицированы под прозрачный стиль. Проверки: npm test -- __tests__/modules/raids, npm run typecheck, eslint по измененным TSX без errors (2 no-img-element warnings).
- Создан быстрый AIF-план `Tribute Hosted Payments For Advent` в `.ai-factory/PLAN.md`; предыдущий fast-план сохранен в `.ai-factory/plans/2026-06-03-club10000-broadcasts-fast-completed.md`. В `diaverseapi` и `diaweb` от актуальных `dev` созданы ветки `feature/tribute-advent-payments`; план ограничивает MVP платежкой Tribute только для authenticated Advent users с Telegram ID и закладывает generic provider adapter через cabinet payments registry.
- По `$aif-improve` доработан `.ai-factory/PLAN.md`: добавлены задачи по Tribute error normalization/redaction и санитизации hosted checkout URL logs, уточнены user-aware capability gating по `User.tg_user_id`, callback relay token в generated `webhookUrl`, checkout TTL/reuse policy, generic payments rollout gate, backend/frontend tests и docs targets. Остановлен локальный `diaweb/frontend` Next dev-server на порту 3000. Выполнен targeted GBrain sync для `diaverse-aif`.
- Выполнен Task 1 плана Tribute: в `diaverseapi` добавлены `CABINET_PAYMENTS_TRIBUTE_ENABLED/VISIBLE`, `TRIBUTE_*` settings, пакет `app/integrations/tribute` с async `TributeClient`, Pydantic-схемами Shop API orders/webhooks, exception classes и security/redaction helpers для hosted links, callback relay token и чувствительных provider полей. Проверки: Ruff по `app/core/settings.py`, `app/core/features.py`, `app/integrations/tribute`; `compileall` по тем же backend-файлам.
- Выполнен Task 2 плана Tribute: добавлен `CabinetPaymentProviderCode.tribute_hosted`, provider registration в `app/cabinet/payments/registry.py` с `domain_codes=("advent",)`, `supports_guest_checkout=False`, `method_kind=bank_transfer`, metadata `integration=tribute/checkout_kind=hosted`, visibility/enabled flags и дополнительный gate на `CABINET_GENERIC_PAYMENTS_ENABLED`. Создан минимальный `CabinetTributeService` package для registry adapter factory; полноценная checkout/reconcile логика остается в Task 5. Проверки: Ruff/compileall по затронутым cabinet payment файлам, import-smoke registry lookup.
- Выполнен Task 3 плана Tribute: `try_normalize_cabinet_payment_error` теперь распознает `TributeApiError`, `TributeTransportError`, `TributeConfigurationError` и callback guard errors, мапит их в rejected/retryable/configuration/signature cabinet errors и редактирует `paymentLink`, `webAppUrl`, `customerId`, callback/api tokens в details. Добавлен focused `tests/test_tribute_integration.py` на redaction и normalized categories. Проверки: `pytest tests/test_tribute_integration.py -q`, Ruff/compileall по mapper, Tribute integration и тесту.
- Выполнен Task 4 плана Tribute: добавлен `TributeAdventPaymentQuoteStrategy`, экспорт в `app/cabinet/payments/quotes/__init__.py` и wiring в `AdventPaymentQuoteService`. Стратегия поддерживает USD/USDT -> RUB через `CabinetFxRateService`, direct RUB без FX lookup и явный reject unsupported currency combinations. Добавлены регрессии в `tests/test_cabinet_payment_fx.py`. Проверки: `pytest tests/test_cabinet_payment_fx.py -q`, Ruff/compileall по quote changes.
- Выполнен Task 5 плана Tribute: добавлен полноценный `CabinetTributeService` и dependency provider для hosted checkout. Адаптер создает Tribute order, берет `customerId` только из `users.tg_user_id`, явно запрещает guest checkout, пишет redacted provider payload, сохраняет raw redirect только в checkout payload/session fields и формирует `webhookUrl` с relay token. Добавлен `tests/test_cabinet_tribute_payments.py` на создание checkout и reject пользователя без Telegram ID. Проверки: `pytest tests/test_cabinet_tribute_payments.py -q`, Ruff/compileall по `app/cabinet/payments/tribute` и тесту.
- Выполнен Task 6 плана Tribute: создан `app/cabinet/payments/tribute/api.py`, подключен router `cabinet/payments/tribute`, добавлены callback token header constants, `CabinetTributeTarget`, target lookup по Tribute order id/public reference и `handle_callback` с relay-token guard. Добавлены тесты на callback router, invalid token и generic target reconciliation call. Проверки: `pytest tests/test_cabinet_tribute_payments.py -q`, Ruff/compileall по Tribute package, integration helpers, router wiring и тесту.
- Выполнен Task 7 плана Tribute: `CabinetTributeService.reconcile_payment_session` теперь получает authoritative Tribute order через `client.get_order`, валидирует сумму/валюту/public reference перед `paid`, мапит `pending/completed/failed/canceled/refund/chargeback/unknown` в локальные статусы, не откатывает уже paid session на late failed/canceled и переводит refund/chargeback/unknown в review. Provider payload хранится redacted в `last_order_status`; callback после paid вызывает существующие finalization hooks. Добавлены регрессии на paid, amount mismatch, stale failed и refund-after-paid. Проверки: `pytest tests/test_cabinet_tribute_payments.py -q`, Ruff/compileall по Tribute package, integration helpers, router wiring и тесту.
- Выполнен Task 8 плана Tribute: `/payment-capabilities` теперь передает authenticated user context в Advent service; `get_payment_capabilities` disabled-ит `tribute-hosted` для authenticated user без `tg_user_id` с reason `telegram_identity_required`, не выбирает его default-ом и сохраняет available/default поведение для Telegram-linked users. Явный requested `tribute-hosted` без Telegram ID отклоняется локальным `CabinetPaymentInvalidRequestError`, guest actor не получает available Tribute. Проверки: `pytest tests/test_cabinet_advent_payments.py tests/test_cabinet_tribute_payments.py -q`, Ruff/compileall по Advent service/api, Tribute package и тестам.
- Выполнен Task 9 плана Tribute: в Advent backend stale checkout reuse logs raw `provider_checkout_url` заменен на `checkout_url_present`; во frontend `logAdventClient*` добавлен sanitizer для `redirect_url`, `redirectUrl`, `checkout_url`, `paymentLink`, `webAppUrl`, `link` и вложенных payload keys. Добавлена Vitest-регрессия на redacted diagnostics. Проверки: backend `pytest tests/test_cabinet_advent_payments.py tests/test_cabinet_tribute_payments.py tests/test_tribute_integration.py -q`, Ruff/compileall; frontend `npm test -- __tests__/modules/advent/advent-api.test.ts`, `npm run typecheck`.
- Выполнен Task 10 плана Tribute: в `diaweb` добавлен provider type `"tribute-hosted"` и fallback label `Tribute`, helper сохраняет backend `public_label` как приоритет для Tribute, а Advent provider selector показывает Tribute отдельной кнопкой и прокидывает `provider=tribute-hosted` в payment URL. Проверки: `npm test -- __tests__/modules/payments/helpers.test.ts __tests__/modules/advent/AdventPagePayments.test.tsx __tests__/modules/advent/AdventPaymentPage.test.tsx`, `npm run typecheck`.
- Выполнен Task 11 плана Tribute: обновлен `diaverseapi/.env.example` блоком `CABINET_GENERIC_PAYMENTS_ENABLED`, `CABINET_PAYMENTS_TRIBUTE_VISIBLE/ENABLED` и `TRIBUTE_*` placeholders; `diaverseapi/docs/cabinet-commerce.md` и `docs/features/advent-calendar.md` описывают Advent-only scope, Telegram eligibility, callback URL shape, server-side reconciliation, rollout/rollback order и запрет на логирование токенов, PII, callback payloads и hosted checkout URLs. Проверка: `scripts/docs-health.ps1` без errors (1 старый unrelated warning про `docs/tasks/fabric/lvl1/lvl1.md` без H1).
- Выполнен Task 12 плана Tribute: backend tests дополнены `httpx.MockTransport` проверкой `TributeClient.create_order/get_order` и API-key auth scheme без live API, TTL fallback assertion для hosted checkout, duplicate callback idempotency и расширенным набором quote/capability/reconciliation regressions. Проверки: `pytest tests/test_tribute_integration.py tests/test_cabinet_tribute_payments.py tests/test_cabinet_payment_fx.py tests/test_cabinet_advent_payments.py -q`, Ruff/compileall по backend payment/advent/tribute файлам и тестам.
- Выполнен Task 13 плана Tribute: в `AdventPaymentStubPage` добавлен вывод provider label из `payment-capabilities`, `helpers.test.ts` закрепляет disabled `tribute-hosted` как невыбираемый метод с сохраненным `telegram_identity_required`, `AdventPaymentPage.test.tsx` проверяет backend `public_label` для Tribute на return/status page. Проверки: `npm test -- __tests__/modules/payments/helpers.test.ts __tests__/modules/advent/AdventPagePayments.test.tsx __tests__/modules/advent/AdventPaymentPage.test.tsx __tests__/modules/advent/advent-api.test.ts`, `npm run typecheck`.
- Выполнен Task 14 плана Tribute: targeted verification — backend `pytest tests/test_tribute_integration.py tests/test_cabinet_tribute_payments.py tests/test_cabinet_payment_fx.py tests/test_cabinet_advent_payments.py tests/test_cabinet_payment_sessions.py -q` дал 73 passed; backend Ruff по Tribute/payment/advent scope clean; frontend `npm run test -- __tests__/modules/advent/AdventPagePayments.test.tsx __tests__/modules/advent/AdventPaymentPage.test.tsx` дал 5 passed; `npm run typecheck` clean. Первичный прогон выявил и был исправлен guard в `test_cabinet_payment_sessions.py`: `tribute-hosted` Advent-only и не должен поддерживать factory/raids domains; также убран unused import `ZERO` из `advent/payment_facts.py`.
- Создан `docs/tasks/raids/legacy-telegram-raids-compensation.md`: зафиксирован аудит `raids-tg-bot.csv` на 781 active legacy-рейд, формула компенсации, таблицы множителей, базовые пакеты ресурсов, порядок агрегации по `user_character_id -> user_id` и открытые вопросы по USDT credit/unresolved pets. Проверка: `scripts/docs-health.ps1` без errors, старый warning по `docs/tasks/fabric/lvl1/lvl1.md`.
