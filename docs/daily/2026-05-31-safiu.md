# Daily Work - 2026-05-31 [safiu]

## Public digest






<!-- Только безопасные публичные заметки на русском. Этот раздел можно публиковать в Confluence и использовать для генерации постов. -->

- Исправлены отсутствующие иконки наград в бесплатной ежедневной награде магазина: награды снова используют актуальные изображения.

- Исправлен сценарий завершения рейда: награды теперь показываются только после клика по готовому слоту, а попадание пэта в ловушку открывает отдельное предупреждение вместо пустых итогов.

- В итогах рейда скрыты нулевые награды: игрок видит только фактически полученные ресурсы, шкатулки и особый лут.

- Упрощен блок “О локации” в рейдах: убраны дубли цены и особого лута, риск ловушки подписан по режимам, а у шкатулок добавлена подсказка про рост шанса от серии.

- В блок “Бонус серии” в рейдах добавлена понятная подсказка: игрок видит, что серия усиливает ресурсы и шкатулки и сбрасывается при смене локации.

- Исправлено открытие дополнительных рейдовых слотов: если у игрока есть Токен слота, интерфейс теперь получает доступное действие “Открыть слот за 1 токен” и понятнее объясняет, что нужен уже открытый дополнительный слот.

- Подготовлен быстрый план улучшения UX рейдовых слотов: свободные слоты должны выглядеть одинаково, а режим отправки выбирается на финальном шаге.

- Реализовано улучшение UX рейдовых слотов: игрок выбирает обычную свободную ячейку и пэта, а способ отправки выбирает уже на финальном шаге.

- Улучшен сценарий спасения пэта из ловушки: выбор спасателя теперь выглядит как обычный выбор пэта с изображениями, а после попытки игрок видит понятный результат спасения.

- Опубликованы публичные дневные отчеты за 28, 29 и 30 мая в Confluence.

- Добавлен управляемый флаг для отключения автоматической генерации постов в непроизводственных окружениях, чтобы снизить расход API-токенов.

- Подготовлена dev-среда для тестирования рейдов: тестовым аккаунтам начислены ресурсы, XDV-баланс, слоты, подписка и рейдовые пэты.

- Дополнительно подготовлены реальные аккаунты тестировщиков на dev: им выданы XDV, слоты, ресурсы, подписка и доступные рейдовые пэты.

- В runbook copywriting добавлена инструкция по флагу отключения автоматической генерации постов для непроизводственных окружений.

- Для dev-окружения включен отдельный запрет генерации постов в staff copywriting, чтобы ручные действия в dev не расходовали API-токены общего copywriting-сервиса.

## Internal log







<!-- Локальные технические заметки на русском. Этот раздел нельзя публиковать или отправлять в генерацию постов. -->

- Backend fix внесен в diaverseapi: dev commit bad2db19 и main cherry-pick 94cc5626. Обновлена нормализация image_url для shop_free_daily snapshots, добавлены регрессионные тесты. На продовой БД обновлены 22 entries текущего пула, 513 free reward claim snapshots и 1 старый pilot_skin claim; после проверки .png snapshots в free reward claims = 0. Проверки: pytest tests/test_cabinet_shop_specials.py -q, ruff check service.py и test_cabinet_shop_specials.py, GBrain sync diaverseapi-code.

- Фикс бесплатной награды запушен в origin/dev (до db568f1a) и origin/main (до f2f62345) обычным push без force.

- В dev-ветках diaweb и diaverseapi изменен claim-flow рейдов. Backend возвращает `metadata.claim_outcome`, при ловушке не начисляет reward/xp и создает trap outcome; frontend вызывает `claim_run` по клику на готовый слот, затем показывает либо `RaidResultModal`, либо новую `RaidTrapOutcomeModal`. Проверки: backend settlement pytest/ruff, frontend RaidFlows vitest, frontend typecheck.

- В diaweb доработан `RaidResultModal`: отфильтрованы reward-строки с количеством 0, обновлен регрессионный тест `RaidFlows.test.tsx`. Проверки: `npm test -- --run __tests__/modules/raids/RaidFlows.test.tsx`, `npm run typecheck`.

- В diaweb обновлены `RaidLocationBriefing`, `RaidRewardPreview`, `mechanicsDisplay` и i18n-словарь рейдов: верхний intel-блок больше не показывает XDV/USDT entry и special loot, `trapRiskLabel` выводит риск ловушки по режимам, `basic` chest переименован в Simple/Простая, формула шкатулок показывает uncapped значение и итоговый cap 100%. Проверки: `npm test -- --run __tests__/modules/raids/RaidLocationStack.test.tsx __tests__/modules/raids/RaidFlows.test.tsx`, `npm run typecheck`.

- В diaweb добавлен tooltip для `RaidLocationBriefing` рядом с заголовком “Бонус серии”: новые ключи i18n `seriesHintLabel`/`seriesHint`, hover/focus/click-состояние, CSS bubble и обновленные тестовые фикстуры. Проверки: `npm test -- --run __tests__/modules/raids/RaidLocationStack.test.tsx __tests__/modules/raids/RaidFlows.test.tsx __tests__/modules/raids/RaidShell.test.tsx`, `npm run typecheck`.

- В diaverseapi `RaidStateService` теперь наполняет `profile.available_actions` и top-level `available_actions` действием `unlock_slot_token` на основе баланса `slot_token`, лимита 5 токен-слотов и стоимости 1 токен за слот. В diaweb обновлены тексты: “Открыть слот за 1 токен” и lock-reasons про уже открытый доп. слот. Проверки: backend pytest `test_state_service.py`, `test_slot_token_service.py`, `test_raids_flow.py`, ruff для измененных raid-файлов; frontend vitest для cabinet raids page и raid flow/stack/shell, `npm run typecheck`.

- Обновлен `.ai-factory/PLAN.md`: быстрый план `Raid Slot Selection UX` фиксирует flow `локация -> слот -> пэт -> режим -> подтверждение`, нейтральные свободные слоты, отдельную карточку открытия токен-слота, USDT без лимита и риск необходимости backend-контракта для сохранения выбранной визуальной ячейки. Проверки не запускались, так как это только планирование.

- В diaweb переработан raid slot flow: добавлен `slotAvailability`, убран fake `usdt_batch` из обычной сетки, добавлена карточка открытия токен-слота с подтверждением, финальный шаг теперь показывает доступность режимов по емкости, USDT отображается как no-limit entry point. В diaverseapi добавлен optional `display_slot_index`, сохранение выбранной XDV-ячейки в `RaidRun.state.display_slots` и раскладка state slots с учетом сохраненного индекса. Проверки: frontend `npm run typecheck`, vitest для raid stack/flows/shell/api; backend pytest `test_raids_flow.py`, `test_state_service.py`, `test_domain_rules.py`, `test_command_start.py`, ruff для измененных raid-файлов.

- В diaverseapi `rescue_trap` теперь возвращает `metadata.rescue_outcome` со статусом попытки, rescue attempt, исходной ловушкой и ловушкой спасателя при провале. В diaweb добавлены карточки выбора спасателя с backend `skin_image_url`, фильтры по редкости и `RaidRescueOutcomeModal` для успеха/провала. Проверки: frontend `npm run typecheck`, `npm test -- __tests__/modules/raids`, targeted ESLint для raid-файлов; backend `.venv\Scripts\ruff.exe check app/raids/services/trap_service.py app/raids/tests/test_raids_flow.py`, `.venv\Scripts\python.exe -m pytest app/raids/tests/test_raids_flow.py::test_trap_ransom_and_rescue_result_paths -vv -s`.

- Через scripts/daily-work-publish.ps1 опубликованы docs/daily/2026-05-28-safiu.md, docs/daily/2026-05-29-safiu.md и docs/daily/2026-05-30-safiu.md; все страницы созданы, небезопасных маркеров в Public digest не найдено.

- В aibot добавлен COPYWRITING_AUTO_GENERATION_ENABLED: core/config.py, app/worker/main.py, .env.example, tests/test_worker_loop.py. Флаг выключает worker-created daily LLM jobs, но оставляет TTL cleanup и обычный polling очереди. Проверка: .venv/Scripts/python.exe -m pytest tests/test_worker_loop.py — 13 passed. Добавлен learning patch .ai-factory/patches/2026-05-31-15.53.md; выполнен targeted GBrain sync для aibot-code.

- На dev окружении выполнен idempotent seed для 16 активных `qa_`/`smoke_` пользователей: создано 7 тестовых `bot_users`-связей для XDV, выставлено минимум 1 000 000 XDV, grade/profile_grade 5000, 5 token-slots, 20 `slot_token`, по 1000 raid resources, активная Pro subscription на 90 дней и по 3 raid-пэта (`jetboa`, `capybara`, `badger`). Проверка агрегатов: 16/16 linked, XDV >= 1 000 000, profile/token slots/resources/pets/subscription OK, failures=0; dev fast timers активны (`RAIDS_DEV_FAST_TIMER_SECONDS=30`).

- На dev окружении выполнен повторный idempotent seed для 11 реальных тестировщиков из переданного списка: пользователи найдены по Telegram ID, выставлены минимум 1 000 000 XDV, grade/profile_grade 5000, 5 token-slots, 20 `slot_token`, по 1000 raid resources, активная Pro subscription на 90 дней. Для каждого обеспечена доступная копия `jetboa`, `capybara` и `badger`; добавлено 24 недостающих доступных user_character. Проверка агрегатов: 11/11 ready, failures=0.

- Обновлен docs/runbooks/copywriting-production-runtime.md: добавлен раздел Auto-generation feature flag для COPYWRITING_AUTO_GENERATION_ENABLED, отключения/включения worker-created daily LLM jobs, временной остановки worker и предупреждения о уже queued/retry_pending/processing задачах. В публичную часть не добавлялись SSH-команды, IP и пути к ключам.

- Запушены aibot dev commits bf9ba07 (global generation kill switch) и diaweb dev commits 875cea9/88b9d34 (BFF dev-only COPYWRITING_GENERATION_ENABLED gate + env example). На dev-хосте /home/diaweb обновлен до 88b9d34, frontend/.env.production получил COPYWRITING_GENERATION_ENABLED=false, diaweb-dev пересобран через docker compose -f docker-compose.dev-traefik.yml up -d --build diaweb, health=healthy. Важно: dev/prod diaweb используют общий copywriting-api, поэтому глобальный aibot env на /srv/aibot не выключался, чтобы не отключить prod.
