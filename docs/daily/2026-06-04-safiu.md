# Daily Work - 2026-06-04 - safiu

## Public digest

- Для тестового Tribute rollout в адвент-календаре убрано искусственное требование привязанного Telegram ID: авторизованные пользователи теперь могут выбрать Tribute без Telegram-привязки.
- Проверена связка старой выгрузки рейдов с production-пользователями: все пэты из выгрузки найдены и привязаны к существующим пользователям.
- Подготовлен dry-run top-50 пользователей для XDV-компенсации старых длинных рейдов перед фактическим начислением.
- Выполнено XDV-начисление компенсации пользователям за длинные legacy-рейды Telegram-бота.
- Открыт доступ к новым рейдам для всех авторизованных пользователей; временный gate сохранен только для фабрики.
- В новых рейдах временно скрыт USDT-режим, чтобы пользователи запускали рейды только через XDV-механику.

## Internal log

- Обновлена Tribute eligibility-модель: authenticated Advent checkout использует локальный `customerId` вида `diaverse_user:<user_uuid>`, `users.tg_user_id` больше не требуется для capability/default provider/checkout start. Guest Tribute checkout остается выключенным для MVP с reason `guest_checkout_not_supported`.
- Обновлены `CabinetTributeService`, Advent capability logic, Tribute schemas, backend/frontend tests и rollout docs. Проверки: `pytest tests/test_cabinet_tribute_payments.py tests/test_cabinet_advent_payments.py tests/test_tribute_integration.py -q`, Ruff по Tribute/advent scope, `npm test -- __tests__/modules/payments/helpers.test.ts`.
- Выполнен read-only аудит production DB по `raids-tg-bot.csv`: 781/781 `user_character_id` найдены в `user_characters`, у всех есть связанный `users.uuid`; найдено 144 уникальных владельца. Сегменты: 45 USDT/1d, 11 XDV/1d, 38 XDV/7d, 687 XDV/30d. Дополнительно отмечено: у 4 владельцев нет `tg_user_id`, у 1 нет `bot_user_id`; это не блокирует существование пользователей, но важно для уведомлений/операционного follow-up.
- Сформирован read-only расчет top-50 компенсаций по 7/30-дневным legacy-рейдам: применены base по локации, duration/progress/NFT/golden multipliers, ceiling по строке и caps. Реальное начисление не выполнялось.
- Проведено production XDV-начисление с operation key `legacy_tg_raids_xdv_20260604_v1`: 135 пользователей, 720 legacy-рейдов, 1 908 224 XDV. Создана audit-таблица операции, транзакционная проверка подтвердила совпадение фактических балансов с расчетными. Последующая read-only сверка показала 1 текущий баланс, уже измененный после начисления; `user_history` подтвердил компенсационную запись по этой строке. 5 eligible-строк одного пользователя без `bot_user_id` остались в ручном follow-up на 21 000 XDV.
- Сохранены и запушены payment feature branches: `diaweb` commit `d11932d`, `diaverseapi` commit `347cabb7`. Затем `diaweb/dev` обновлен изменением `feat(raids): open raids to authenticated users`, запушен и влит в `diaweb/master`; `diaverseapi/dev` обновлен из remote и влит в `diaverseapi/main`. Проверки: frontend cabinet gate tests `53 passed`, backend raids tests `42 passed`.
- На production включены raid rollout флаги для backend и frontend, созданы backup-копии env-файлов, пересозданы backend runtime containers и пересобран `diaweb`. Проверки: env внутри контейнеров показывает включенные raid flags, `diaweb` healthy, backend containers running, внешний `/ru/raids` без cookies редиректит на login как auth-only route.
- В `diaweb` добавлен временный фронтовый флаг `RAID_USDT_MODE_ENABLED=false`, скрывающий USDT dispatch-card, USDT fallback entry и USDT-текст в raid location intel; backend-контракт оставлен без изменений. Commit `ba28ed2` запушен в `dev`, влит в `master` merge commit `55afc56`, production `diaweb` пересобран и здоров. Проверки: targeted raids Vitest `43 passed`, `npm run typecheck`, локальный `npm run build`, внешний `/ru/raids` без cookies редиректит на login.
