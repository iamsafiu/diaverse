# Daily Work - 2026-05-25 [safiu]

## Public digest

- Подготовлен подробный план реализации новой web-фабрики Diaverse: backend-экономика, web-интерфейс, layered asset scene, временные ассеты, проверки и этапы внедрения.

- Начата реализация новой web-фабрики: зафиксированы экономические решения, добавлены backend/frontend API-типы, backend router skeleton, rollout-gate, versioned catalog и проверки каталога.

- В backend-фабрику добавлен безопасный payment path: real-money покупки идут через существующий cabinet payments domain `factory`, без shop-псевдотоваров и без новых валют.

- Добавлены проверки платежного контура фабрики: provider registry, finalizer registry, paid-session replay, stale order и amount mismatch теперь покрыты тестами.

- Добавлен серверный адаптер уведомлений фабрики: backend сможет отдавать toast-подсказки и безопасно создавать cabinet notification без риска сорвать основную команду.

- Добавлен state snapshot service для web-фабрики: `/state` теперь собирает профиль, карту, здания, задания, склад, балансы, подписку, подсказки обновления и transient toast-события для UI.

- Добавлены первые команды web-фабрики: открытие фабрики и апгрейд уровня теперь проходят через backend с идемпотентностью, проверками требований и оплатой существующими игровыми долларами, кирпичами или real-money checkout.

- Добавлены backend-команды цехов фабрики: строительство ресурсной части, апгрейд ресурсного уровня, строительство производственной части и снос с возвратом 50% затрат.

- Добавлены складские команды фабрики: ресурсы и игровые доллары накапливаются лениво по server time, склад останавливается после 10 часов, а перенос в инвентарь использует существующие балансы игрока.

- Добавлены команды отсеков фабрики: покупка пакета 1-5, поэтапная прокачка, уровни 6/7 с Токеном слота, ранний доступ и real-money checkout теперь проходят через backend.

- Добавлен backend-жизненный цикл крафта фабрики: старт, очередь, сбор, кулдаун после сбора, возврат queued-заданий и settlement при истечении подписки теперь обрабатываются сервером.

- Добавлены специальные правила кирпичного цеха: выбранные фрагменты питомцев превращаются в кирпичи, подписка повышает шанс удачного x3 результата, а взрывоопасный исход отправляет цех в ремонт.

- Добавлена сборка Токена слота: backend проверяет ингредиенты, списывает компоненты и выдает готовый токен через существующий инвентарь без новой валюты.

- Уточнен план web-фабрики Diaverse после сверки с ТЗ: добавлены требования по полному catalog, экономике Токена слота, активному крафту, onboarding, карте, очередям, ассетам и проверкам.

- Улучшен план web-фабрики вторым senior-проходом: добавлены route/auth/cache/nav интеграции, payment-domain contract, XDV adapter, импульсы, resource/drop processors и rollout-gate.

- Зафиксировано решение по игровым долларам фабрики: новая валюта не создается, фабрика использует существующий игровой баланс через отдельный backend adapter.

- Удалена старая backend-фабрика: API и фоновые задачи больше не регистрируются, старый код убран, подготовлена миграция для удаления legacy-таблиц, изменения перенесены из dev в main.

- Исправлена авторизация через Telegram для возвращающихся пользователей: веб-кабинет больше не должен оставлять человека в новом пустом аккаунте, если устройство уже было привязано к старому аккаунту.


- Подготовлен быстрый план вывода подписки Trade Master в веб-магазин Step Pass: товар должен появиться в витрине, покупка должна выдавать доступ, а биржа должна распознавать активный Trademaster.




## Internal log

- Factory web planning: перечитаны `docs/tasks/fabric/README.md`, `research1.md`, `factory-mechanics-final.md`, `factory-designer-brief-levels-1-2-8.md`; создан workspace full plan `.ai-factory/plans/feature-factory-web-engine.md` на 47 задач. В `diaweb` и `diaverseapi` созданы ветки `feature/factory-web-engine` от актуального `origin/dev`; оба child repo чистые. План фиксирует `diaverseapi/app/factory` как новый bounded context, `/v1/cabinet/factory` + `/api/cabinet/factory`, ledger/idempotency/row locks/catalog snapshots, subscription/payment/inventory integration, frontend option A layered scene renderer и placeholder asset pack.

- Factory web implementation start: в плане закрыты Tasks 1-6. В `.ai-factory/plans/feature-factory-web-engine.md` зафиксированы решения по `$`/real money/game dollars/bricks, XDV, subscription expiry, entity mapping, impulse ownership и rollout/cache/nav policy. В `diaverseapi` добавлены `app/factory` schemas, API router `/v1/cabinet/factory/catalog|state`, `FACTORY_WEB_ENABLED`, rollout guard, no-store behavior, catalog schema/loader/validator, `factory_catalog.v1.yaml`, money/pricing value objects и `GameDollarBalanceAdapter` поверх существующего `BotUser.dao_balans`. В `diaweb` добавлены shared `frontend/modules/factory/types.ts`. Проверено: `python -m pytest app/factory/tests/test_catalog.py -q` (`8 passed`), `ruff check app/factory app/core/settings.py app/core/features.py app/routers/v1/endpoints.py`, `py_compile` targeted factory/core/router files, snake_case TypeScript contract scan.

- Factory web Task 15: в `diaverseapi` зарегистрирован generic cabinet payment domain `factory` в `CabinetPaymentDomainCode`, provider registry и finalizer registry; создан `FactoryPaymentGateway`, `FactoryPaymentService`, `FactoryPaymentFinalizer`, source ref `factory:{order_uuid}`, routing на `/{lang}/factory/payment`, provider quote reuse Pay1Time/Prodamus/Zion, `FactoryPaymentOrder` link to `cabinet_payment_sessions` with FK/index. Finalizer валидирует paid status, owner, source amount/currency и expiry; без подключенного paid-command applier переводит заказ в review_required вместо фиктивного fulfillment. Проверено: targeted `ruff check` и `py_compile`.

- Factory web Task 15A: добавлены `app/factory/tests/test_payment_service.py` и расширены `tests/test_cabinet_payment_sessions.py`. Тесты проверяют регистрацию `factory` в payment providers только для authenticated actor, регистрацию `FactoryPaymentFinalizer`, успешную paid finalization через injected applier, duplicate replay без повторного применения, missing order, amount mismatch и safe review_required при default missing applier. Проверено: `pytest app/factory/tests/test_payment_service.py tests/test_cabinet_payment_sessions.py -q` (`9 passed`) и targeted Ruff.

- Factory web Task 16: добавлены `app/factory/infrastructure/notifications.py`, `FactoryNotificationHintRead`, `FactoryNotificationCode/Severity` и `app/factory/tests/test_notifications.py`. Адаптер строит transient toast payloads для state/command response и при `persistent=True` использует `CabUserNotificationService` с `source_domain="factory"` и stable idempotency key. Проверено: `ruff check app/factory/infrastructure/notifications.py app/factory/schemas.py app/factory/tests/test_notifications.py`, `pytest app/factory/tests/test_notifications.py -q` (`5 passed`), `py_compile app/factory/infrastructure/notifications.py app/factory/schemas.py`.

- Factory web Task 17: добавлен `FactoryStateService`, подключен `GET /v1/cabinet/factory/state` через `get_factory_state_service`, расширен `FactoryStateResponse.next_refresh_at`. Snapshot читает catalog + persisted factory rows, не создает профиль на GET, возвращает no-profile `open_factory`, building/compartment states по catalog/persisted rows, inventory excerpts, warehouse pending balances, jobs/actions, subscription/impulse state и transient notifications для ready craft/warehouse stopped. Проверено: `pytest app/factory/tests/test_state_service.py -q` (`3 passed`), `pytest app/factory/tests -q` (`47 passed`), `ruff check app/factory`, targeted `py_compile`.

- Factory web Task 18: добавлен `FactoryCommandService`, схемы `FactoryOpenRequest`/`FactoryLevelUpgradeRequest`, зависимости и endpoints `POST /v1/cabinet/factory/open`, `POST /v1/cabinet/factory/levels/upgrade`. `open` явно создает профиль и warehouse balances; `upgrade_level` проверяет sequential target, catalog requirements, idempotency replay/conflict, списывает существующие `game_dollar`/`brick` через `FactoryInventoryGateway` или создает real-money checkout через payment domain `factory`. `FactoryPaymentFinalizer` по умолчанию теперь применяет paid pending command через command service; explicit deferred applier оставлен для тестов review_required. Проверено: `pytest app/factory/tests/test_command_service.py app/factory/tests/test_payment_service.py -q` (`9 passed`), `pytest app/factory/tests -q` (`52 passed`), `ruff check app/factory`, targeted `py_compile`.

- Factory web Task 19: добавлен `FactoryBuildingService`, request-схемы и endpoints `POST /buildings/{building_key}/resource/build`, `/resource/upgrade`, `/production/build`, `/demolish`. Строительство ресурсной части списывает catalog costs `impulse` + XDV, апгрейд ресурсной части списывает XDV и не может превышать уровень фабрики, production-building construction списывает catalog costs с `slot_token`, снос возвращает 50% по spend snapshots с округлением whole resources вверх. Multi-price команды делают preflight всех балансов до первого debit, чтобы не было частичных списаний. Проверено: `pytest app/factory/tests/test_building_service.py -q` (`6 passed`), `pytest app/factory/tests -q` (`58 passed`), `ruff check app/factory`, targeted `py_compile`.

- Factory web Task 20: добавлен `FactoryWarehouseService`, `FactoryWarehouseTransferRequest`, endpoints `POST /warehouse/transfer-to-storage` и `/warehouse/transfer-to-inventory`. Lazy settlement считает accrual активных resource buildings по catalog daily rate, subscription resource multiplier и 10h cap; pending переносится в factory storage, stopped сбрасывается после transfer-to-storage, transfer-to-inventory кредитит whole resource units через `FactoryInventoryGateway` и оставляет fractional remainder. Game-dollar warehouse accrual включается с factory level 6 и при collect/autocollect идет в существующий gaming balance через gateway, не создавая новую валюту. Trademaster autocollect подключен на storage transfer. Проверено: `pytest app/factory/tests/test_warehouse_service.py -q` (`4 passed`), `pytest app/factory/tests -q` (`62 passed`), `ruff check app/factory`, targeted `py_compile`.

- Factory web Task 21: добавлен `FactoryCompartmentService`, `FactoryCompartmentUpgradeRequest`, `FactoryCompartmentPurchaseMode`, repository `add_compartment`, dependency и endpoint `POST /buildings/{building_key}/compartments/{compartment_key}/upgrade`. Реализованы package 1-5, sequential single-level upgrades, level 6/7 line unlocks with `slot_token`, price mapping to existing `game_dollar`, `brick` and real-money checkout, no new currency/wallet. Real-money path preflights token, создает checkout только на real-money часть, а token debit выполняет paid-command finalizer; при paid callback добавлен dispatcher `upgrade_compartment`. Early-access x2 сохраняется как state metadata для будущего craft, stale `early_access` нормализуется при достижении regular unlock level. Проверено: `pytest app/factory/tests/test_compartment_service.py -q` (`7 passed`), `pytest app/factory/tests -q` (`69 passed`), `pytest app/factory/tests tests/test_cabinet_payment_sessions.py -q` (`74 passed`), `ruff check app/factory`.

- Factory web Task 22: добавлен `FactoryCraftingService`, `FactoryCraftJobCreateRequest`, `FactoryCraftJobActionRequest`, `quote_factory_timer`, endpoints `POST /craft-jobs`, `POST /craft-jobs/{job_id}/collect`, `POST /craft-jobs/{job_id}/cancel`. Старт крафта резервирует wired resource inputs, очередь gated подпиской, сбор выдает wired outputs и запускает cooldown только после collect, running/ready/collected задания нельзя отменять, queued cancel возвращает резерв и пропускает pre-cancel autostart settlement. `/state` лениво переводит ready jobs, запускает следующий queued job после cooldown и отменяет subscription-dependent queued/running jobs при expiry с refund. Entity-рецепты без адаптера пока блокируются стабильной ошибкой, чтобы не списывать/выдавать пустые сущности. Проверено: `pytest app/factory/tests/test_crafting_service.py -q` (`8 passed`), `pytest app/factory/tests/test_crafting_service.py app/factory/tests/test_state_service.py -q` (`11 passed`), `pytest app/factory/tests -q` (`77 passed`), `pytest app/factory/tests tests/test_cabinet_payment_sessions.py -q` (`82 passed`), `ruff check app/factory`.

- Factory web Task 23: добавлен `app/factory/domain/policies.py` для brick craft outcome, серверные random rolls в `FactoryCraftingService`, поддержка `input_overrides.fragment_key|fragment_rarity`, snapshot `output_snapshot.brick_special`, 25% базовый x3 luck и 50% x3 при активной factory-подписке, 5% explosive outcome с 12h repair on collect и блокировкой craft во время repair. `rare_pet_fragment`, `epic_pet_fragment`, `legendary_pet_fragment` добавлены в `ResourceType`, factory migration enum/resource seeds и state balance excerpts. Проверено: `pytest app/factory/tests/test_policies.py app/factory/tests/test_crafting_service.py app/factory/tests/test_inventory_gateway.py app/factory/tests/test_award_resource_support.py -q` (`26 passed`), `pytest app/factory/tests -q` (`85 passed`), `pytest app/factory/tests tests/test_cabinet_payment_sessions.py -q` (`90 passed`), `ruff check app/factory app/shards_and_resources/models.py migrations/versions/factory_web_state_20260525.py`.

- Factory web Task 24: добавлен `FactorySlotTokenService.assemble_slot_token`, `FactorySlotTokenAssembleRequest`, dependency и endpoint `POST /v1/cabinet/factory/slot-tokens/assemble`. Сборка берет `slot_token.assembly_inputs` из catalog, делает preflight всех балансов, списывает `token_details`, `rare_evogen`, `synthesis_core`, начисляет `slot_token`, сохраняет idempotency payload и replay без повторных inventory movements. `rare_evogen` и `synthesis_core` добавлены в resource seed coverage/state excerpts. Проверено: `pytest app/factory/tests/test_slot_token_service.py app/factory/tests/test_inventory_gateway.py app/factory/tests/test_award_resource_support.py -q` (`17 passed`), `pytest app/factory/tests -q` (`88 passed`), `pytest app/factory/tests tests/test_cabinet_payment_sessions.py -q` (`93 passed`), `ruff check app/factory app/shards_and_resources/models.py migrations/versions/factory_web_state_20260525.py`.

- Factory web plan spec-alignment: `.ai-factory/plans/feature-factory-web-engine.md` дополнен разделом `Spec Alignment Requirements`; зафиксированы full backend catalog levels 1-8, slot-token drop/chest economy with x1/x5/x10 modifiers, queued-only craft cancel, cooldown-on-collect, subscription expiry settlement, early-access normalization, level 1/2 progression fixtures, option A layered map pan/swipe, Figma/art handoff with density assets, A18 onboarding, A13 queued-slot menu, A16 toast collapse, A12 subscription states and level 8 map-only scope. Проверено: `docs-health.ps1` 71 md / 113 links / 0 warnings / 0 errors.

- Factory web plan improve pass: `.ai-factory/plans/feature-factory-web-engine.md` расширен с 47 до 52 задач. Добавлены Task 2A rollout/route/cache/nav policy, Task 12A impulse collection adapter, Task 14A resource/award/loot processor wiring, Task 15A payment registry/finalizer contract tests, Task 32A diaweb auth-only/private-cache/navigation/i18n shell wiring. Усилены инварианты по `CabinetPaymentDomainCode`, provider `domain_codes`, finalizer registry, canonical XDV debit path, no guest factory state, feature flags, event-token collision safety, Alembic/delete/merge sentinels и mobile developer handoff.

- Factory game-dollar architecture: план обновлен после raw verification `diaverseapi/app/internal/models.py`, `security/services.py`, `fitness_pass/processor.py`, `cabinet/shop/money.py`. Зафиксировано, что игровые доллары фабрики должны использовать существующий `BotUser.dao_balans` / `UserRead.gaming_balance`; новая factory-local валюта или `ResourceType` не создается. Добавлен `GameDollarBalanceAdapter`, pending warehouse accrual остается factory state до collect/autocollect, а spendable balance credit/debit идет через существующий gaming balance с mirror-записями в `factory_ledger_entries`.

- `docs/tasks/fabric`: из двух plain-text `.doc` источников созданы `factory-mechanics-final.md` и `factory-designer-brief-levels-1-2-8.md`, добавлен `README.md` раздела, в `research1.md` добавлен H1 для docs-health. Вертикальные табличные блоки пересобраны в Markdown pipe-tables: 27 таблиц в mechanics doc и 17 таблиц в designer brief. Прямые markdown-ссылки на кириллические `.doc` заменены на code text, чтобы не ломать checker. Проверено: `docs-health.ps1` 71 md / 113 links / 0 warnings / 0 errors, `gbrain-sync.ps1 -SourceId diaverse-docs`, `gbrain-health.ps1`. GBrain sync предупредил, что оба fabric md крупные и их можно будет дополнительно разбить, но импорт завершился без errors.

- `diaverseapi`: удален legacy пакет `app/factory`, отключены factory router и Redis worker из runtime registries, account deletion/merge переведены с deleted ORM model на transitional raw SQL cleanup при наличии старых таблиц. Добавлена Alembic migration `drop_legacy_factory_20260525` для `factory_*`/`factories` таблиц и `workshop_type`, удалены `reset_factory.sql` и устаревшая factory frontend API note. Проверено: `rg` по live factory imports/routes clean, Ruff clean, py_compile clean, Alembic head/offline SQL clean, `diaweb/frontend` без factory dependency, `gbrain-sync.ps1 -SourceId diaverseapi-code`, `gbrain-health.ps1`. После rebase на актуальный `origin/dev` migration down_revision перенесен на `pet_events_audit_20260525`; commit `c8c850b8` запушен в `diaverseapi/dev`. После успешной dev-сборки выполнен merge `origin/dev` в `main`, конфликт modify/delete по `app/factory/usecases.py` разрешен в сторону удаления фабрики; проверки после merge: strict conflict marker scan, `git diff --cached --check`, py_compile, Ruff, Alembic heads/offline SQL. Merge commit `d33e24a0` запушен в `diaverseapi/main`. Targeted pytest заблокирован отсутствующим локальным PostgreSQL/Docker; отдельно найден unrelated merge sentinel `user_public_username_history`.

- `diaverseapi`: обновлены локальные `dev` и `main`, исправлен `LinkUserUseCase` для `tg_pure_login` и `tg_switch`: перед переносом `user_devices` удаляются только конфликтующие строки source-user с уже существующим `(device_id, user_id)` у winner-user, затем переносятся оставшиеся устройства. Добавлены логи `[FIX:auth-device-transfer]`, metadata `moved_devices`/`discarded_duplicate_devices` и тесты `tests/test_link_user_device_transfer.py`. Коммиты запушены: `dev` `eae68228`, `main` `9c72e28a`. Проверено: targeted pytest, ruff и compileall; отдельный старый `test_logout_usecase.py` остается с unrelated fake-session mismatch.

- `diaverseapi`: обновлены локальные `dev` и `main` поверх чужой миграции `pet_events_comments_20260525`, исправлен `CabCryptonService.get_state`: terminal request из старого `window_id` игнорируется во время cooldown нового окна, чтобы frontend не показывал старую заявку вместо каталога. Добавлен regression test `test_state_ignores_stale_terminal_request_from_previous_window`. Коммиты запушены: `dev` `92b66049`, `main` `10f2717d`. Проверено: `pytest tests/test_cabinet_crypton.py -q` (`31 passed`) и `ruff check app/cabinet/offers/crypton/service.py tests/test_cabinet_crypton.py`.

- Windows CLI agent stack: установлен `Microsoft.PowerShell 7.6.2.0` через winget, `Alacritty 0.17.0` через winget, `JetBrainsMono Nerd Font 3.3.0` через winget. Добавлен `%APPDATA%\\alacritty\\alacritty.toml` с shell `pwsh`, working directory `C:\\Users\\Indigo\\Desktop\\diaverse`, JetBrainsMono NF, scrollback 20000, search/vi/new-window keybindings. Обновлен `Documents\\PowerShell\\Microsoft.PowerShell_profile.ps1`: UTF-8 encodings, PSReadLine history prediction с fallback для non-interactive запуска, `cdiaverse` helper. Созданы Desktop shortcuts `Diaverse Alacritty.lnk` и `Diaverse Codex.lnk`. Проверено: `pwsh` reports 7.6.2/utf-8, `alacritty 0.17.0`, Alacritty config starts with short exit command.

- Alacritty scroll tuning: `%APPDATA%\\alacritty\\alacritty.toml` обновлен с `scrolling.history = 100000`; добавлены keybindings `Shift+PageUp/PageDown`, `Ctrl+Shift+PageUp/PageDown`, `Alt+Shift+Up/Down` для scrollback. Проверено коротким запуском Alacritty с текущим config.

- Codex scrollback fix for Alacritty: в `C:\\Users\\Indigo\\.codex\\config.toml` добавлен `[tui] alternate_screen = "never"`, чтобы Codex запускался в inline mode и сохранял вывод в обычный terminal scrollback Alacritty. Проверено: `codex --strict-config --help` проходит.

- Alacritty mouse scroll bindings: в `%APPDATA%\\alacritty\\alacritty.toml` добавлены wheel bindings для `ScrollLineUp/Down`, `Shift+Wheel` для page scroll и `Ctrl+Shift+Wheel` для top/bottom. Проверено коротким запуском Alacritty с текущим config.

- Subagent A/B benchmark: запущены два isolated explorer agents по одинаковым 3 вопросам (`club10000 membership truth`, `aibot deployment/runtime`, `TelegramService ownership/use`). Raw/no-GBrain agent: 32 команды, 20 raw-файлов, first-pass search ~278-295ms, высокий шум broad `rg`, точность 3/3 с одной корректной оговоркой про legacy Club10000 local state. GBrain-first agent: 32 команды, 8 GBrain pages/symbol pages + 13 raw files, GBrain get/code lookups ~3.8-13.5s каждый, raw checks ~46-546ms, точность 3/3, лучшее попадание в canonical docs, но 4 exploratory GBrain searches timed out/были лишними. Итог: GBrain не ускоряет миллисекундный локальный grep, но работает как navigation/canonical-doc layer; финальный паттерн остается `list/search -> get -> raw verify`.

- GBrain lookup pattern hardening: обновлены `AGENTS.md`, `.ai-factory/RULES.md`, skill-context для `aif-explore`, `aif-plan`, `aif-implement`, `aif-fix`, `aif-review`, `aif-verify`, а также `docs/knowledge-system.md`. Зафиксировано правило `list/search -> get canonical page -> raw verify`; `query` оставлен только как optional broad discovery. Исправлен `scripts/gbrain-sync.ps1`: пустой `ExcludeDirs` допустим, `-SourceId diaverse-docs,diaverse-aif` разбирается как список. Проверено: `docs-health.ps1`, `gbrain-sync.ps1 -SourceId diaverse-docs,diaverse-aif -SkipDryRun`, `gbrain-health.ps1`.

- GBrain A/B final benchmark: 3 вопроса (`aibot` runtime, club membership source of truth, domains/ports). No-GBrain через `rg` + raw docs: Q1 250ms/382 lines/40.7k chars, Q2 147ms/782 lines/56.8k chars, Q3 131ms/314 lines/47.0k chars. GBrain full route (`query/search/get/verify`): Q1 18.4s/90 lines/4.4k chars, Q2 17.8s/683 lines/35.0k chars, Q3 18.2s/108 lines/10.1k chars. GBrain optimized route (`list filtered/get/verify`): Q1 6.9s/62 lines/4.0k chars, Q2 6.7s/663 lines/34.6k chars, Q3 7.0s/102 lines/10.0k chars. Вывод: `rg` быстрее на локальном диске, GBrain уменьшает шум и быстрее выводит к canonical docs, но `query` без embeddings ненадежен; recommended agent path is `list/search -> get -> raw verify`.

- GBrain fix: обновлены `scripts/gbrain.ps1`, `scripts/gbrain-health.ps1`, `scripts/gbrain-sync.ps1` и `docs/knowledge-system.md`. Wrapper добавил source override, stale PGLite lock cleanup и watchdog для native `search`, который возвращает stdout и убивает подвисший Bun-процесс. Health теперь проверяет `get`, `search`, `query` по `diaverse-docs`. `gbrain-sync.ps1` импортирует docs через временное зеркало без `docs/daily/**` и чистит `daily/*` pages из GBrain. Проверено: `gbrain get infrastructure/services/aibot --source diaverse-docs`, `gbrain search "club membership" --source diaverse-docs --limit 3`, `gbrain query "where is aibot deployed" --source diaverse-docs --no-expand --limit 3`, `gbrain-health.ps1`, `docs-health.ps1`, `gbrain-sync.ps1 -SourceId diaverse-docs -SkipDryRun`; `daily/*` не виден в обычном `list/get`.

- GBrain A/B benchmark: на 3 вопросах (`aibot` runtime, prod/dev API domains, club membership source of truth) raw `rg` оказался быстрее по wall-clock времени, но часто возвращал шум из старых runbook/task документов; GBrain `list --source` помогал быстрее выбрать canonical docs. Бенчмарк выявил проблему с source-scoped `get/query` и подвисанием `search` на PGLite; она исправлена отдельным GBrain fix выше. Точные токены среда не показывает; proxy metrics: elapsed time, command count, output lines/chars, files verified.

- Tasks 8-9 server infrastructure docs: пройдены `docs-health.ps1`, `server-inventory.ps1 -Help`, `server-inventory.ps1 -HostAlias diaverse-dev -DryRun`, targeted sensitive scans по `docs/infrastructure` и `scripts/server-inventory.ps1`; выполнены `gbrain-sync.ps1 -SourceId diaverse-docs`, `gbrain-sync.ps1 -SourceId diaverse-aif`, `gbrain-health.ps1`. GBrain docs sync сохранил известный parser warning по daily file, health checks passed.

- Tasks 5-7 server infrastructure docs: выполнен read-only SSH inventory по `diaverse-prod`, `diaverse-dev`, `diaverse-bots`; snapshots оставлены в `.tmp/server-inventory/`. Обновлены `docs/infrastructure/servers/*.md`, `deployment-matrix.md`, `domains-and-ports.md`, `services/*.md`; зафиксированы compose projects, контейнеры, route owners, network names, health states и review items по direct host ports/unknown listeners без raw env/secret values.

- Task 4 server infrastructure docs: добавлен `docs/infrastructure/inventory-checklist.md`, обновлен `docs/infrastructure/README.md` с командами для SSH aliases, review matrix по snapshot-файлам, redaction rules и verify workflow перед commit.

- Task 3 server infrastructure docs: добавлен `scripts/server-inventory.ps1` с read-only SSH inventory, сохранением sanitized snapshots в `.tmp/server-inventory/`, редактированием env-like значений и проверенными режимами `-Help`/`-DryRun`; удаленная команда передается через base64 pipe, чтобы избежать shell quoting сбоев.

- Task 2 server infrastructure docs: обновлены `docs/README.md`, `docs/documentation-system.md`, `AGENTS.md`; добавлена навигация на `docs/infrastructure/README.md` и правило, что raw inventory snapshots остаются в `.tmp/server-inventory/`, а в docs попадают только curated non-secret topology facts.

- Task 1 server infrastructure docs: добавлены `docs/infrastructure/README.md`, `deployment-matrix.md`, `domains-and-ports.md`, server docs для prod/dev/bots-landing и service docs для `diaweb`, `diaverseapi`, `aibot`, `club10000-bot`, reverse proxy. Документы пока содержат шаблоны и pending live inventory, без raw SSH commands/key paths/env/secrets.

- AIF fast plan: `.ai-factory/PLAN.md` перезаписан под задачу server infrastructure documentation inventory. Scope root-only: `docs/infrastructure`, `scripts/server-inventory.ps1`, docs navigation, docs-health, sensitive audit, GBrain sync/health, commit/push root repo. План фиксирует запрет на destructive SSH actions, raw env/secrets/private key paths в committed docs и public daily.

- Task 10 GBrain migration: после docs/AIF правок выполнены targeted sync `gbrain-sync.ps1 -SourceId diaverse-docs` и `gbrain-sync.ps1 -SourceId diaverse-aif`; повторно пройдены `docs-health.ps1`, `gbrain-health.ps1` и active surface audit. Готовится root commit `chore: replace graphify with gbrain knowledge layer`; child repositories не staging/commit.

- Task 9 GBrain migration: `rg -n "Graphify|graphify|graphify-out|\\.graphifyignore"` по active surface (`AGENTS.md`, `README.md`, `docs`, `.ai-factory`, `.codex`, `scripts`, `.mcp.json`, `.gitignore`) с исключением daily/current migration plan/old plans/patches/history вернул no matches. Дополнительно очищены `docs/club.md` и `docs/research/auth/fix-auth.md` от legacy operational references.

- Task 8 GBrain migration: `scripts/docs-health.ps1` очищен от legacy exclusion, `scripts/gbrain-health.ps1` проверяет wrapper/version, `doctor --fast`, source IDs, отсутствие legacy MCP entry, docs/AIF list smoke, `code-def CopywritingDailyView`, `code-def TelegramService`, `code-refs TelegramService`; проверки прошли: docs-health 55 md / 39 links / 0 warnings / 0 errors, gbrain-health passed with no-embedding keyword search skipped by design.

- Task 7 GBrain migration: обновлены `README.md`, `docs/README.md`, `docs/documentation-system.md`; добавлен `docs/knowledge-system.md` с source IDs, bootstrap/sources/sync/health командами, lookup examples, no public connector/no auto-capture/no embeddings baseline и troubleshooting.

- Task 6 GBrain migration: обновлены `AGENTS.md`, `.ai-factory/DESCRIPTION.md`, `.ai-factory/ARCHITECTURE.md`, `.ai-factory/RULES.md`, `.ai-factory/RESEARCH.md` и skill-context для `aif-plan`, `aif-implement`, `aif-verify`, `aif-review`, `aif-fix`, `aif-explore`. Активные правила теперь GBrain-first, source-code-final-authority, без public HTTP/MCP connector и без auto-capture; исторические research sessions оставлены как история.

- Task 5 GBrain migration: `.codex/hooks.json` очищен от Graphify PreToolUse reminder, `.codex/skills/graphify` удален, `scripts/sync-aif-skills.ps1` больше не overlay-ит внешний Graphify skill; `diaweb/.agents/skills/graphify` отсутствует, поэтому sync не должен восстановить Graphify из source skills.

- Task 4 GBrain migration: из `.mcp.json` удален Graphify MCP server, из `.gitignore` убраны Graphify-specific исключения, после проверки resolved paths внутри workspace удалены `.graphifyignore`, `.tools/graphify`, `graphify-out`, `graphify-out.rar`, `scripts/graphify-build.ps1`, `scripts/graphify-update.ps1`, `scripts/graphify-rebuild.py`.

- Task 3 GBrain migration: выполнен no-embed sync/import для `diaverse-docs`, `diaverse-aif`, `diaweb-code`, `diaverseapi-code`, `aibot-code`, `club10000-bot-code`; `gbrain-health.ps1 -SkipGraphifyAudit` прошел wrapper/version, `doctor --fast`, source presence, docs/AIF list smoke, `code-def CopywritingDailyView`, `code-def TelegramService` и `code-refs TelegramService`. Keyword `search` оставлен opt-in через `-RunSearchSmoke`, потому что no-embedding search в текущем workspace может зависать; `docs/daily/2026-05-25-safiu.md` при import пропускается GBrain parser edge case, поэтому daily не считается canonical knowledge source.








- `aibot`: исправлена идемпотентность daily chat situation jobs после статуса `sent`, добавлен recovery для `raw_text`/markdown JSON, quality gate против пустой заглушки при достаточном числе сообщений, компактный Telegram formatter без блока "Что отвечать"; на сервере отправлен один обновленный compact report, worker healthy, новых дублей jobs не появилось. Graphify не обновлялся по просьбе.
- Проверки: `aibot` targeted pytest прошел 38 тестов; `diaweb` copywriting vitest прошел 107 тестов, `npm run typecheck` прошел, `npm run lint` прошел с существующими warnings; `ruff` недоступен в локальной `.venv`. Graphify не обновлялся по прямой просьбе.
- `aibot`: добавлены ORM-модель `CopywritingChatSituationReport`, SQL-миграция `20260525_0001_copywriting_chat_situation_reports.sql` и экспорт модели из `db`.
- Таблица хранит дату отчета, статус, окно источников, счетчики сообщений, структурированный JSON, форматированный текст, Telegram delivery metadata, `job_id`, pending jobs и idempotency key.
- `aibot`: добавлен `ChatSituationReportRepository` с идемпотентным созданием дневного отчета, lookup/update методами и mark_* переходами для `generating`, `ready`, `source_not_ready`, `sending`, `sent`, `failed`.
- `aibot`: добавлен `chat_situation_report.py` с ensure/enqueue/execute use cases, source-readiness проверкой, анонимизацией входных сообщений, LLM JSON-нормализацией и HTML-safe formatter для Telegram.
- `aibot`: `SourceRepository` получил `list_active_enabled_chats()` для выбора только включенных активных source-чатов.
- `aibot`: добавлены `CHAT_SITUATION_REPORTS_*` настройки и `send_chat_situation_report.py`; sender использует `TelegramService(bot_token=...)`, поддерживает topic thread id, фиксирует `sending/sent/failed` и не импортирует `send_ops_alert`.
- `aibot`: добавлен worker processor `generate_chat_situation_report`, регистрация в processor registry и планировщик `ensure_daily_chat_situation_report_job_scheduled()` на `03:10 Europe/Moscow`.
- `aibot`: добавлены схемы и routes `chat_situation_reports.py`, endpoints `GET/POST /internal/v1/chat-situation-reports/today` и `POST /internal/v1/chat-situation-reports/{report_id}/send`; router зарегистрирован в `app/api/main.py`.
- `diaweb`: добавлены BFF routes для `/api/staff/copywriting/chat-situation-reports/...`, `ChatSituationReport*` типы, API client функции и React Query хуки `useToday/Ensure/SendChatSituationReport`.
- `diaweb`: `CopywritingDailyView.tsx` подключает chat situation hooks и рендерит `ChatSituationReportPanel` внутри вкладки рекомендаций; добавлены action handlers с sanitized logging.
- На prod выполнен Crypton reset для `tg_user_id=6585430610` и `tg_user_id=8057982030`: активных `cab_crypton_requests` после операции `0`, latest claimed windows `0`, созданы новые active windows `cbe11530-544c-4d3d-bae3-68ac0d6dc158` и `733004ab-5abd-488c-adc6-cbfe25e75539` со `status=available`, `expires_at=2026-05-25 15:00:00+00` (`20:00 Asia/Yekaterinburg`). Изменений кода и Graphify update не было.

- Настроен author-aware Daily Work publisher: обновлены scripts/daily_work_publish.py, scripts/daily-work-add.ps1, scripts/daily-work-publish.ps1, .codex/skills/daily-work/SKILL.md, docs/daily/TEMPLATE.md, docs/daily/DEVELOPER_SETUP.md, AGENTS.md и .ai-factory/skill-context/aif-implement/SKILL.md. В .env.daily-work добавлен DAILY_WORK_AUTHOR=safiu. Проверено: py_compile, scripts/test_daily_work_publish.py, status, dry-run, publish page_id=151715841. Graphify не обновлялся по просьбе пользователя.

- Создан/перезаписан docs/forDenisDaily.md в UTF-8. Док включает setup .env.daily-work, команды status/add/publish, daily rules, готовый .codex/skills/daily-work/SKILL.md, AGENTS.md snippet и TEMPLATE.md. Проверена корректность markdown code fences. Graphify не обновлялся.

- Обновлен docs/forDenisDaily.md: удалены требования создавать личный Atlassian API token, добавлено пояснение, что DAILY_WORK_CONFLUENCE_EMAIL должен соответствовать владельцу token, а DAILY_WORK_AUTHOR=denis задает секцию автора. Исправлена формулировка про корень workspace.

- Исправлен scripts/daily_work_publish.py: добавлен fallback merge по h2-заголовку автора для Confluence storage без HTML-комментариев, добавлен тест на удаление дублей секции автора, пройдены py_compile и scripts/test_daily_work_publish.py.

- В diaverseapi выполнены fetch/pull fast-forward для dev до 7ce6b341 и обновление локальной main до 02de845b; после обновления запущен scripts/graphify-update.ps1, рабочее дерево diaverseapi чистое.

- Перенесены 44 файла из diaweb/docs в корневые docs/{architecture,product,features,runbooks,research,logs,assets}; в diaweb/docs оставлен README-указатель. Добавлены docs/README.md, docs/documentation-system.md и scripts/docs-health.ps1. Обновлены AGENTS.md, diaweb/AGENTS.md и diaweb/README.md. Проверено: docs-health 54 markdown files, 32 локальные ссылки, 0 warnings, 0 errors. Graphify update был прерван и затем остановлен по просьбе пользователя, граф не обновлялся.

- Инициализирован root git repo C:\Users\Indigo\Desktop\diaverse на ветке main. Добавлены .gitignore deny-by-default и .gitattributes; root repo трекает docs, .ai-factory, .codex/skills, scripts, AGENTS.md, README.md и workspace config. Child repos diaweb, diaverseapi, aibot, club10000-bot, .env*, graphify-out, временные профили и screenshots игнорируются. Создан приватный GitHub repo https://github.com/iamsafiu/diaverse через GitHub API/Git Credential Manager. Graphify не обновлялся по просьбе пользователя.

- Root repo diaverse запушен в `iamsafiu/diaverse` на `main`; `diaweb` docs-only commit `b4a113f` запушен в `dev`. HTTPS push на GitHub возвращал remote 500, поэтому локальные remotes переведены на SSH; Graphify не обновлялся по просьбе пользователя.

- Создан новый fast plan `.ai-factory/PLAN.md` для миграции Graphify -> GBrain: локальный CLI-first bootstrap, регистрация sources для `docs`, `.ai-factory` и четырех child repos, dry-run/smoke sync, удаление Graphify MCP/scripts/hooks/skills/artifacts, перевод AI Factory/AGENTS/docs на GBrain-first правила, финальный audit без запуска Graphify.

- Task 1 GBrain migration: добавлены `scripts/gbrain.ps1` и `scripts/gbrain-bootstrap.ps1`; bootstrap нашел Bun `1.3.14`, установил локальный clone `garrytan/gbrain` в `.tools/gbrain/repo` через `bun install --ignore-scripts`, проверил `gbrain 0.41.2.0`. Состояние GBrain по умолчанию изолировано через `GBRAIN_HOME=.tools/gbrain/home`; публичные HTTP/MCP сервисы и daemon не запускались.

- Task 2 GBrain migration: инициализирован project-local PGLite brain в `.tools/gbrain/home/.gbrain` с `embedding_disabled=true`, установлен `search.mode=conservative`, зарегистрированы sources `diaverse-docs`, `diaverse-aif`, `diaweb-code`, `diaverseapi-code`, `aibot-code`, `club10000-bot-code`. `.gbrain-source` в child repos не создавались.

- AIF fast plan: `.ai-factory/PLAN.md` перезаписан под задачу Trade Master в web Step Pass store. План зафиксировал prod read-only findings: в `market_products` и `cab_shop_items/offers` есть только Basic/Pro, `Trade Master` subscription не имеет `factory_trademaster`, exchange проверяет `UserSubscription`, а web shop grant создает `PersonalFitnessPass`. Scope: `diaverseapi` registry/data migration/entitlement/exchange recognition/grant tests, `diaweb` i18n/helpers/card rendering/tests, targeted verification, prod-safe postchecks и GBrain sync.
