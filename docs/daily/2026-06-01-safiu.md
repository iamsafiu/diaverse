# Daily Work - 2026-06-01 [safiu]

## Public digest


- Подготовлен план изменения клубного онбординга: участники, пришедшие через Telegram-ссылку приглашения, должны сразу получать активный доступ, участвовать в парах и попадать в рейтинги.
- В бэкенд-контракт клуба добавлен отдельный источник доступа "По приглашению", чтобы такие участники не попадали в статус "в чате без основания".
- Обработчик клубных Telegram-событий в backend теперь отличает вход через ссылку приглашения и запускает штатную активацию, приветствие и назначение пары только для таких входов.
- Telegram-адаптер клуба начал передавать backend факт входа через ссылку приглашения для событий входа в группу.
- В staff-интерфейсе клуба появился понятный источник доступа "По приглашению" для участников, активированных через Telegram-ссылку.
- Добавлены проверки клубного lifecycle: вход без ссылки остается на проверке, вход по ссылке активируется, назначает пару и ставит уведомления в outbox.
- Обновлена клубная документация: invite-link вход теперь описан как отдельный onboarding path, отличающийся от неизвестных входов без ссылки.
- Изменения по клубному invite-link онбордингу запушены в рабочие репозитории; `aibot` обновлен на сервере и сервисы копирайтинга пересобраны.
- Подготовлен план перехода клубной пересборки пар на 14-дневный цикл с публикацией текста и AI-картинки в общей Telegram-группе.

- Опубликован безопасный публичный дайджест Daily Work за 2026-05-31 в Confluence.
- Исправлен доступ сотрудников к staff-разделам клуба и копирайтинга после выдачи прав: интерфейс теперь опирается на актуальные права backend, а не на устаревшие роли в сессии.
- Добавлена защита от обратного сценария: после отзыва роли старые токены пользователя инвалидируются.
- Начата реализация 14-дневной пересборки клубных пар: backend теперь считает срок от последней успешной пересборки, готовит данные поста и передает публикацию текста с AI-картинкой в общий Telegram-чат.
- В настройках клуба добавлено отдельное поле промпта для картинки пересборки пар; референсы остаются общими с картинками рейтингов.
- Фабрика и рейды временно скрыты для обычных пользователей: разделы и прямые маршруты доступны только сотрудникам и администраторам.
- Добавлена документация с инструкцией, как будущему AI-агенту отключить временное ограничение и вернуть доступ обычным авторизованным пользователям.
- Dev-ветки backend и frontend влиты в production-ветки: в прод уехали рейды, фабрика, клубные улучшения, staff-access fix и временное ограничение фабрики/рейдов для сотрудников.
- Повторно опубликован текущий список клубных пар с AI-картинкой в общую Telegram-группу без пересборки пар.
- После корректировки prompt сгенерирована и опубликована еще одна версия картинки со списком текущих клубных пар.
- Дефолтный период клубной пересборки пар изменен на 15 дней; список пересборки и картинка теперь используют Telegram-ники участников в формате `@username`.

## Internal log


- Создан fast-план `.ai-factory/PLAN.md` для фичи Club invitation access from Telegram links.
- План затрагивает `diaverseapi/app/club`, `aibot/app/clubbot` и `diaweb/frontend/modules/club`; `club10000-bot` не входит в scope.
- Зафиксировано ключевое ограничение: активировать только join/join-request с Telegram `invite_link`, а no-link unknown joins и roster-only участников оставлять review-only.
- В `diaverseapi/app/club` добавлен `ClubMembershipSource.invitation`, staff label `По приглашению`, lifecycle state `invitation_active`/`invitation_pending` и исключение invitation-доступа из флагов `unknown_telegram_join` и `active_without_payment_or_exception`.
- В `diaverseapi/app/club/service.py` добавлена обработка `invite_link` для join request и chat member events: новые invite-link участники создаются как `source=invitation`, затем активируются через существующий `activate_membership`; no-link joins остаются `telegram_join/pending_verification`.
- В `aibot/app/clubbot/handlers.py` добавлена безопасная передача `invite_link` для `chat_member` updates и boolean-логирование `has_invite_link`; тесты `aibot/tests/test_clubbot_handlers.py` покрывают link/no-link варианты.
- В `diaweb/frontend/modules/club` обновлены source/access-state типы, исключен `invitation` из ручных источников, добавлен UI label `По приглашению` и frontend-тесты для отображения и фильтра `source=invitation`.
- В `diaverseapi/tests/test_club_service.py` добавлены focused tests для `invitation_active`, invite-link join request, invite-link member join, update существующего `telegram_join`, manual-exclusion guard и active membership inclusion; локальный срез `pytest ... -k "invitation or invite_link ..."` прошел: 9 passed.
- Verification: `diaverseapi` `pytest tests/test_club_service.py -q` -> 61 passed, `pytest tests/test_club_admin_api.py -q` -> 7 passed, `ruff check app/club ...` -> passed; `aibot` clubbot tests -> 10 passed, ruff недоступен в venv; `diaweb/frontend` club vitest -> 44 passed, `npm run typecheck` -> passed; GBrain sync выполнен для измененных источников.
- Push/deploy: `diaverseapi` dev -> `fb952d70`, `aibot` dev -> `5abe9b6`, `diaweb` dev -> `33a58dd`, root docs -> `31d7fab`; на сервере `/srv/aibot` выполнен fast-forward pull до `5abe9b6` и `docker compose -f docker-compose.prod.yml up -d --build copywriting-api copywriting-worker copywriting-userbot`, все три контейнера healthy.
- Создан новый fast-план `.ai-factory/PLAN.md` для Club Pairing Rollover Images And 14-Day Cadence. План заменил предыдущий fast-plan файл, как предусмотрено режимом `aif-plan fast`, и описывает scope для `diaverseapi`, `aibot`, `diaweb`, docs и GBrain follow-up.

- Выполнен preflight для docs/daily/2026-05-31-safiu.md: public_chars=2364, internal_chars=6996, unsafe_public_markers=0; страница Confluence обновлена, page_id=155451393.
- Расследован stale-role инцидент для staff access: в production DB права `club` и `copywriting` были выданы, но активный access token продолжал нести старый набор ролей. В `diaweb/frontend/proxy.ts` staff routes переведены на backend RBAC после hydration, а `frontend/app/api/staff/copywriting/_auth.ts` больше не отказывает по JWT roles до запроса `/v1/staff/access/me`.
- В `diaverseapi/app/cabinet/admin/service.py` добавлен bump `users.token_version` при отзыве роли через admin service, чтобы старые повышенные сессии не продолжали работать после revoke.
- Проверки: `npm run test -- --run __tests__\proxy.test.ts __tests__\app\api\staff\copywriting\auth.test.ts` -> 27 passed; `npm run lint -- --file proxy.ts --file app\api\staff\copywriting\_auth.ts --file __tests__\proxy.test.ts --file __tests__\app\api\staff\copywriting\auth.test.ts` -> passed; `.venv\Scripts\python.exe -m pytest tests\test_cabinet_admin_service.py tests\test_cabinet_rbac_service.py` -> 10 passed; `.venv\Scripts\python.exe -m ruff check app\cabinet\admin\service.py tests\test_cabinet_admin_service.py` -> passed.
- Push/cherry-pick: `diaweb` dev `c0178de`, default branch `master` `7b1314d`; `diaverseapi` dev `6fb51035`, `main` `a37160ab`.
- В `diaverseapi` добавлен period-based путь пересборки пар: daily scheduler tick, due-date от legacy `last_monthly_pairing_at`/`last_monthly_pairing_month`, metadata `last_pairing_rollover_*`, payload для retry и fallback plain-text outbox без `message_thread_id`.
- В `diaverseapi/app/club/aibot_client.py` добавлен signed publish request для pairing rollover с отдельным prompt config и shared `leaderboard_image_reference_paths`.
- В `aibot` существующий club leaderboard asset pipeline расширен `asset_kind=pairing_rollover`, добавлены context fields для prompt/caption и route `/internal/club/leaderboards/pairing-rollovers/publish`.
- В `diaweb/frontend/modules/club` добавлено поле `pairing_rollover_image_prompt_template`, сохранение нового text-template alias и обновлены user-facing labels с monthly на пересборку пар.
- Preflight: Python `py_compile` для измененных backend/aibot модулей прошел; прямой `npx tsc --noEmit` в `diaweb` не подходит для проекта, нужна штатная команда `npm run typecheck`.
- Проверки pairing rollover: `diaverseapi` `pytest tests\test_club_service.py tests\test_club_auth_and_clubbot.py -q` -> 72 passed; `ruff check app\club app\core\broker_app.py tests\test_club_service.py tests\test_club_auth_and_clubbot.py` -> passed; `aibot` `pytest tests\test_club_leaderboard_image.py -q` -> 14 passed; `diaweb/frontend` ClubSettingsPanel vitest -> 2 passed; targeted ESLint по измененным club-файлам -> passed; GBrain sync выполнен для `diaverseapi-code`, `aibot-code`, `diaweb-code`, `diaverse-docs`, `diaverse-aif`.
- Ограничения проверки: `aibot` ruff недоступен в текущем venv (`No module named ruff`); `diaweb` `npm run typecheck` падает на существующей ошибке в `app/api/staff/copywriting/_auth.ts`, не в измененных club-файлах.
- В `diaweb/frontend/modules/cabinet` добавлен temporary staff-only gate для `/factory` и `/raids`: desktop/mobile навигация требует `hasStaffAccess`, а `routeAccess.ts` помечает эти пути как staff-only для client redirect на `/offers` без JWT-role block в proxy.
- Документация отката добавлена в `docs/runbooks/temporary-factory-raids-staff-gate.md` и связана из `docs/README.md`.
- Проверки: `npm run test -- --run __tests__\modules\cabinet\routeAccess.test.ts __tests__\modules\cabinet\CabinetTopbar.test.tsx __tests__\modules\cabinet\BottomNav.test.tsx __tests__\modules\cabinet\CabinetLayout.test.tsx` -> 44 passed; targeted ESLint по измененным cabinet-файлам -> 0 errors, 2 existing test mock warnings; `docs-health.ps1` -> 0 errors, 1 existing missing-H1 warning.
- Выполнен dev -> prod merge: `diaverseapi` `origin/dev` в `main` merge commit `6ae917c5`, `diaweb` `origin/dev` в default branch `master` merge commit `6d32bc4`. В `aibot` отдельной `main` нет; зависимый endpoint pairing rollover уже находится в `origin/dev` `c6abfd3`.
- `diaverseapi` merge принес Alembic `migrations/versions/raid_mechanics_v1_20260528.py` с `down_revision=runtime_settings_20260528`; `tests/test_alembic_graph.py` подтвердил single-head. Перед реальным merge дополнительно проверялся временный worktree `origin/main + origin/dev`: merge без конфликтов, club/alembic tests прошли.
- Проверки backend после merge: `pytest tests/test_alembic_graph.py tests/test_club_service.py tests/test_club_auth_and_clubbot.py -q` -> 86 passed; `pytest app/raids/tests --ignore=app/raids/tests/test_account_lifecycle.py -q` -> 71 passed; `ruff check app\club app\raids app\core\broker_app.py app\core\settings.py tests\test_alembic_graph.py tests\test_club_service.py tests\test_club_auth_and_clubbot.py` -> passed. Полный рейдовый срез с `test_account_lifecycle.py` дал 157 passed и 3 failures из-за отсутствующей локальной БД `diaverse_test`.
- В `diaweb` merge-конфликты были в club UI/types/tests; resolution сохранил `invitation` labels/access-state и новые `pairing_rollover` settings. Дополнительно исправлен typecheck в `frontend/app/api/staff/copywriting/_auth.ts`: WebCrypto теперь получает Node `BufferSource` вместо `Uint8Array<ArrayBufferLike>`.
- Проверки frontend после merge: targeted vitest suite -> 13 files / 119 tests passed; `npm run typecheck` -> passed; targeted ESLint по измененным club/cabinet/proxy/auth файлам -> passed. Первый широкий vitest прогон имел один timeout в `cabinet-raids-page`, изолированный повтор этого файла прошел 3/3.
- Проверка aibot dependency: `pytest tests\test_club_leaderboard_image.py -q` -> 14 passed. GBrain sync выполнен для `diaweb-code` и `diaverseapi-code`.
- На production `aibot` выполнен fast-forward до `c6abfd3` и пересборка `copywriting-api`, `copywriting-worker`, `copywriting-userbot`; все сервисы healthy.
- Для Club10000 восстановлен payload из текущих активных пар без вызова `run_periodic_rollover`: 15 активных групп, period_key `2026-06-01`, `message_thread_id` не передавался.
- Pairing rollover asset `3b2b1c23-c2dd-4c31-8e2a-df6ec6a2865b` сгенерировал картинку и опубликован через userbot: Telegram message_id `1509`, `publish_status=published`, `topic_strategy=none`; backend metadata обновлена финальным статусом.
- После изменения `pairing_rollover_image_prompt_template` повторно собран payload текущих 15 пар и создан новый pairing rollover asset `e7d62f37-e893-4043-93cf-1f493180322b`; публикация через userbot завершилась Telegram message_id `1510`, `publish_status=published`, `topic_strategy=none`. Активные группы не пересоздавались.
- Изменен backend default `pairing_rollover_interval_days` с 14 на 15 в `diaverseapi/app/club/pairing.py` и `admin_api.py`; periodic rollover pair lines получили отдельный label path с приоритетом `telegram_username`.
- Обновлены связанные prompt/preview тексты в `aibot` и `diaweb`, документация `docs/club.md`, а также targeted tests: backend due-date теперь ожидает `2026-06-16` от anchor `2026-06-01`; пары в payload проверяются как `@username`.
- Проверки: `diaverseapi` pairing rollover pytest -> 7 passed, Ruff -> passed; `aibot` club leaderboard image tests -> 14 passed, `py_compile` -> passed, Ruff недоступен в venv; `diaweb/frontend` ClubSettingsPanel vitest -> 2 passed; `docs-health.ps1` -> 0 errors, 1 existing missing-H1 warning.
