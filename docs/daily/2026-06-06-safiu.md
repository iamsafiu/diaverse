# Daily Work - 2026-06-06 - safiu

## Public digest

- Исправлена адаптивность модальных окон Crypton: крестик закрытия остается видимым на мобильных устройствах и на десктопах с небольшой высотой экрана.
- Подготовлены тестовые аккаунты для ручной проверки первых двух уровней фабрики: начислены тестовые ресурсы и игровые балансы.
- Подготовлен план реализации рейдовых ваучеров: премиум-рейды будут запускаться по локационным ваучерам с расходом одного ваучера на одного отправленного питомца.
- Уточнен план внедрения рейдовых ваучеров: добавлены проверки корректного списания, отображения нехватки ваучеров и сохранности пользовательского состояния.
- Начато внедрение рейдовых ваучеров: добавлен backend-контракт для локационных ваучеров и будущего отображения их количества в состоянии рейдов.
- Добавлена backend-таблица рейдовых ваучеров: grants привязаны к raid profile, источнику начисления и будущему run/participant при списании.
- Подготовлен backend-сервис начисления рейдовых ваучеров: grants создаются идемпотентно по source-unit и могут быть посчитаны, заблокированы и списаны для будущего premium raid start.
- В состояние рейдов добавлен inventory рейдовых ваучеров по локациям: frontend сможет показывать доступное количество и требование для premium-режима.
- Запуск premium-рейда на backend переведен с нового USDT payment order на проверку и списание рейдовых ваучеров: один grant списывается на одного участника.
- Account merge для raid state расширен: при переносе raid profile теперь учитываются иммунитеты и ваучеры, а metadata sentinel дополнительно закрыл две auth-bot таблицы.
- В item catalog и fulfillment добавлена поддержка рейдовых ваучеров без продажи в магазине: staff/support смогут выдавать grants, а shop sale rows не создавались.
- Backend-логика рейдовых ваучеров покрыта focused тестами: проверены начисление, inventory, блокировка при нехватке и списание одного ваучера на одного участника.
- Frontend рейдов переведен на premium voucher UX: режим `USDT` показывается пользователю как `Премиум`, отображает количество ваучеров по локации, блокирует отправку при нехватке и ведет в раздел магазина рейдов.
- Завершена реализация рейдовых ваучеров без магазина и компенсации: backend списывает один ваучер на одного участника, frontend показывает premium-режим и сохраняет контекст при server-side blocked ответе.
- Изменения по рейдовым ваучерам закоммичены и отправлены в `dev` в backend/frontend репозиториях.
- Рейдовые ваучеры добавлены в backend-проекцию мобильного инвентаря: доступные ваучеры отображаются как предметы по локациям с количеством и метаданными.

## Internal log

- В `diaweb` обновлены стили `frontend/modules/crypton/components/cryptonOfferModal.module.css`: основная Crypton-модалка и request-dialog получили ограничения по `100dvh`, safe-area-aware позиционирование и внутренний вертикальный скролл.
- Добавлен регрессионный CSS-тест в `frontend/__tests__/modules/crypton/CryptonOfferModal.test.tsx`.
- Проверки: `npx vitest run __tests__/modules/crypton/CryptonOfferModal.test.tsx`, `npx eslint __tests__/modules/crypton/CryptonOfferModal.test.tsx`.
- Для 12 указанных тестовых аккаунтов выполнено транзакционное начисление: `impulse +120000`, `brick +100000`, `slot_token +8`, `rare_pet_fragment +5`, XDV `+250000`, game dollars `+2000`. Сброс фабричного прогресса не выполнялся в этой операции.
- Обновлен fast-план `.ai-factory/PLAN.md` для задачи raid vouchers: новая backend-сущность `RaidVoucherGrant`, списание одного grant на одного `RaidParticipant`, inventory в raid state, premium UI в `diaweb`; магазин, checkout, компенсация CSV и иконки оставлены вне текущего скоупа.
- Улучшен `.ai-factory/PLAN.md` после `aif-improve`: добавлены задачи для `migrations/env.py` и model contract tests, lifecycle/account merge переноса `raid_voucher_grants`, refactor-safe получения `RaidParticipant` при создании run, замены старых USDT payment-required тестов, frontend business-blocked handling и premium entry независимо от XDV slots.
- Task 1 raid vouchers: добавлен `diaverseapi/app/raids/domain/vouchers.py`, расширены raid-схемы полями `RaidVoucherGrantRead`, `RaidVoucherInventoryRead`, `RaidErrorCode.insufficient_raid_voucher` и `RaidStateResponse.voucher_inventory`; выполнен compile-check через `.venv\Scripts\python.exe`.
- Task 2 raid vouchers: добавлены `RaidVoucherGrantStatus`, модель `RaidVoucherGrant`, миграция `20260606_raid_voucher_grants.py`, регистрация в `migrations/env.py` и обновление `app/raids/tests/test_models.py`; проверки: `compileall`, `pytest app/raids/tests/test_models.py -q`, `alembic heads --verbose`, `alembic upgrade exchange_desired_evolution_20260605:raid_voucher_grants_20260606 --sql`, identifier length guard.
- Task 3 raid vouchers: добавлены repository methods для `RaidVoucherGrant` (`add`, `get_by_source`, `list_available`, `count_available_by_location`, `consume_voucher_grants`) и сервис `app/raids/services/voucher_grant_service.py`; проверки: `compileall` и импорт `RaidVoucherGrantService/RaidRepository`.
- Task 4 raid vouchers: `RaidStateService` теперь загружает available voucher grants, группирует их по локациям, заполняет `voucher_inventory` и зеркалит count/item metadata в `RaidLocationRead.metadata`; обновлен fake repository и добавлен state unit test. Проверки: `compileall`, `pytest app/raids/tests/test_state_service.py -q`.
- Task 5 raid vouchers: `RaidCommandService.start_raid` для `mode_key=usdt` lock-ает available grants по локации, возвращает `insufficient_raid_voucher` при нехватке, создает обычный `RaidRun`, получает созданных participants через новый внутренний `RaidRunCreationResult` и списывает grants с `consumed_run_id/participant_id/user_character_id`; внешний `_create_run` сохранен для paid finalizer. Проверки: `compileall` для `command_service.py` и `payment_service.py`.
- Task 6 raid vouchers: `_count_raid_state_rows` теперь учитывает `raid_immunity_effects`, `raid_immunity_grants`, `raid_voucher_grants`; `_merge_raid_state` переносит эти модели на winner; lifecycle test fixture расширен new grant/effect rows; `tests/test_merge_account_coverage.py` покрывает новые raid tables и ранее неклассифицированные `auth_bot_broadcast_contacts`/`auth_bot_tg_outbox`. Проверки: `compileall`, `pytest tests/test_merge_account_coverage.py -q`; `pytest app/raids/tests/test_account_lifecycle.py -q` заблокирован окружением, потому что локальные БД `diaverse` и `diaverse_test` отсутствуют.
- Task 7 raid vouchers: добавлен `ItemCatalogItemType.raid_voucher`, aliases, non-sellable virtual catalog entries для трех локаций, title resolver и fulfillment handler `grant_raid_voucher_handler`, который вызывает `RaidVoucherGrantService`; registry помечает тип grantable. Проверки: `compileall` и smoke-test `load_virtual_entries(None, 'ru')` на 3 active entries с `is_sellable=False`.
- Task 8 raid vouchers: добавлены `tests/test_raid_voucher_items.py`, обновлены `app/raids/tests/test_command_start.py`, `test_raids_flow.py`, `test_state_service.py`, `test_models.py`, `test_account_lifecycle.py`, `tests/test_merge_account_coverage.py`; проверки: `compileall`, `pytest tests/test_raid_voucher_items.py -q`, `pytest tests/test_raid_immunity_items.py tests/test_raid_voucher_items.py -q`, `pytest app/raids/tests/test_command_start.py app/raids/tests/test_raids_flow.py app/raids/tests/test_state_service.py app/raids/tests/test_models.py -q`, `pytest tests/test_merge_account_coverage.py -q`. Совмещенный запуск с merge sentinel выявляет order-dependent metadata gap в старых Club/Crypton таблицах, не связанных с ваучерами; одиночный sentinel проходит.
- Tasks 9-12 raid vouchers: в `diaweb` добавлены типы `RaidVoucherInventory/RaidVoucherGrant`, premium copy/i18n, включен `RAID_USDT_MODE_ENABLED`, premium entry отвязан от XDV slots, dispatch/pet/confirm UI показывает voucher requirement, `status=blocked` с `insufficient_raid_voucher` оставляет dispatch context и показывает CTA `/{lang}/shop/raids`; обновлены raid fixtures/tests. Проверки: `npm run typecheck`, `npm run test -- --run __tests__/modules/raids`, `npm run test -- --run __tests__/app/api/cabinet/raids`, `npm run lint` (0 errors, существующие warnings).
- Task 13 raid vouchers: финальная backend проверка: `pytest tests/test_raid_voucher_items.py tests/test_raid_immunity_items.py -q`, `pytest app/raids/tests/test_command_start.py app/raids/tests/test_raids_flow.py app/raids/tests/test_state_service.py app/raids/tests/test_models.py -q`, `pytest tests/test_merge_account_coverage.py -q`, Alembic offline SQL и identifier length guard прошли. DB lifecycle test остается заблокирован отсутствующими локальными БД `diaverse/diaverse_test`; combined backend run с merge sentinel по-прежнему падает на старых Club/Crypton таблицах, не относящихся к ваучерам.
- Commit/push: `diaverseapi` commit `34a34494 feat: add raid vouchers` pushed to `origin/dev`; `diaweb` commit `4d61729 feat: show raid vouchers in premium raids` pushed to `origin/dev`.
- Mobile inventory voucher projection: в `diaverseapi/app/inventories/schemas.py` добавлен `RaidVoucherInventoryItem`, в `app/inventories/services.py` добавлен `get_raid_vouchers()` через `RaidRepository.get_profile()` и `count_available_voucher_grants_by_location()`, а `/inventories/pilot` и deprecated `/inventories` теперь возвращают `raid_vouchers`. Добавлен `tests/test_raid_voucher_inventory_projection.py`. Проверки: `compileall` для затронутых файлов, `pytest tests/test_raid_voucher_inventory_projection.py -vv`, `pytest tests/test_raid_voucher_items.py app/raids/tests/test_state_service.py::test_state_service_exposes_raid_voucher_inventory_by_location -q`, `pytest app/raids/tests/test_command_start.py::test_start_usdt_raid_blocks_without_location_voucher app/raids/tests/test_command_start.py::test_start_usdt_raid_consumes_one_voucher_per_pet -q`, `pytest app/raids/tests/test_raids_flow.py::test_usdt_start_blocks_without_raid_voucher app/raids/tests/test_raids_flow.py::test_usdt_start_consumes_raid_vouchers_per_pet -q`.
