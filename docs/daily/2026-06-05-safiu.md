# Daily Work - 2026-06-05 - safiu

## Public digest


- Обновлен Telegram gateway для уведомлений Криптона: runtime переключен на ту же forum-группу, где публикуется ежедневная обстановка.
- Уведомления Криптона подключены к отдельному топику, а нерелевантные клубные предупреждения больше не засоряют чат.
- Для оффера Криптона добавлено персональное распределение сотрудников по случайным дням 30-дневного цикла, чтобы событие не появлялось всем одновременно.
- Исправлена ошибка frontend lint в staff Crypton-панели: проверка больше не падает на синхронизации состояния заявки.
- Для staff-пользователя вручную активировано новое окно оффера Криптона без влияния на остальных пользователей.
- Для еще одного staff-пользователя обновлено состояние Crypton: старая зависшая заявка закрыта, создано новое активное окно оффера.
- Telegram-уведомление о новой заявке Криптона сделано более операционным: теперь сразу видно пользователя, предмет, количество, цену, скидку и ссылку в staff shop.
- Изменения уведомлений Криптона запушены в backend и `aibot`; серверный Telegram gateway обновлен и проверен.
- Модалка Криптона разделена на первое предложение и уведомление о решении: pending-заявки больше не вызывают повторный popup, а одобрение, встречная цена и отклонение показываются отдельным статусным текстом один раз.
- Веб-уведомления Криптона приведены к операционному формату: статусные CTA больше не выглядят как повторная авторизация, а staff alert показывает пользователя, товар, количество, цену, скидку и ссылку на заявку в staff shop.
- Подготовлен отдельный план Telegram-уведомлений о решениях Криптона: пользователь должен получать статус заявки с картинкой и ссылкой на свою страницу офферов.
- Для Telegram-уведомлений о решениях Криптона добавлена backend-очередь доставки: auth-бот сможет безопасно забирать, подтверждать и ретраить сообщения пользователям.
- Решения Криптона теперь ставятся в очередь персональных Telegram-уведомлений пользователю: одобрение, встречная цена и отклонение будут сопровождаться товаром, ценой, скидкой, картинкой и ссылкой на страницу офферов.
- Auth-бот подготовлен к отправке персональных уведомлений Криптона: он будет забирать очередь, отправлять сообщение с картинкой или текстовым fallback и подтверждать доставку.
- Проверки персональных Telegram-уведомлений Криптона закрыты на backend и auth-боте.
- Для персональных Telegram-уведомлений Криптона картинка зафиксирована как единое изображение Криптона, без отправки изображения выбранного товара.
- Рейдовые иммунитеты подключены к каталогу предметов и выдаче наград: 9 новых предметов по локациям и длительности теперь могут начисляться как реальные заряды иммунитета.
- Для старых уже забранных наград Advent добавлен безопасный backfill рейдовых иммунитетов: сначала можно проверить dry-run, затем дочислить недостающие предметы без дублей.
- В рейды добавлена команда активации иммунитета: выбранный предмет расходуется, таймер по локации продлевается, а обновленное состояние сразу возвращается клиенту.
- Активный иммунитет теперь применяется при старте рейда: шанс ловушки по защищенной локации становится 0%, а проверка ловушки не планируется.
- Иммунитет теперь защищает и при неудачном спасении: источник защиты сохраняется отдельно для питомца и предмета, а фоновые проверки ловушек используют стартовый snapshot.
- Frontend-контракт рейдов расширен для иммунитетов: добавлены BFF-прокси, API-клиент, типы состояния и mutation для активации предмета.
- В интерфейсе рейдов добавлено использование иммунитетов без чекбоксов: при выборе режима виден активный срок, доступны кнопки продления на 1/7/30 дней, а на главном экране появилась активная иконка с деталями иммунитета.
- Реализация рейдовых иммунитетов закрыта targeted проверками backend и frontend.
- Изменения рейдовых иммунитетов и параллельные изменения Telegram-уведомлений Криптона запушены; backend `dev` дополнительно влит в `main`.

- Исправлено начисление рейдовых шкатулок: теперь выигранные в рейдах шкатулки попадают в инвентарь вместе с XP, ресурсами и особым лутом. Выполнена компенсация ранее невыданных рейдовых шкатулок.
- Исправлен рейдовый иммунитет питомцев к ловушкам: кастомные и русские имена больше не ломают защиту. Ошибочные ловушки сняты, будущие ошибочные проверки очищены, недостающие рейдовые шкатулки по ability-множителю компенсированы.
- В админке магазина добавлена быстрая кнопка дублирования бандлов: копия создается из строки списка и получает название с порядковым суффиксом `копияN`.
- Подготовлен быстрый план реализации рейдовых иммунитетов по локациям: предметы на 1/7/30 дней, продление таймера, защита при старте рейда и спасении, отображение в выборе режима и на главном экране рейдов.
- План рейдовых иммунитетов уточнен до production-подхода: отдельный домен предметов, безопасное продление таймера, backfill старых наград, idempotent-активация и точные UI-состояния без чекбоксов.
- Начата реализация рейдовых иммунитетов: добавлена backend-основа для предметов и активных таймеров по локациям.

## Internal log


- На сервере `aibot` обновлен до commit `8ff79a4`, добавлен `OPS_ALERTS_CRYPTON_MESSAGE_THREAD_ID=0` как безопасный fallback, `OPS_ALERTS_TELEGRAM_CHAT_ID` переключен на effective daily chat из `CHAT_SITUATION_REPORTS_TELEGRAM_CHAT_ID`, пересоздан `copywriting-api`; health probe вернул `status=ok`. Проверка Bot API: старый ops chat был обычной group без topics, daily chat является forum supergroup; recent updates пока не содержат `message_thread_id`, поэтому Crypton topic id нужно получить через сообщение в нужном топике или Bot API topic creation.
- После команды `/crypton_topic` через Bot API найден `message_thread_id=58428`. В `aibot` добавлен allowlist `OPS_ALERTS_ALLOWED_EVENT_CODES`, commit `53a0512` запушен в `dev` и развернут на сервере. Runtime настроен на `OPS_ALERTS_ENABLED=true`, `OPS_ALERTS_CRYPTON_MESSAGE_THREAD_ID=58428`, `OPS_ALERTS_ALLOWED_EVENT_CODES=crypton.request.submitted`; `copywriting-api` healthy. Из Telegram удалены сообщения `58444`, `58445`, `58446`, `58447` для `club.member.silent`.
- В `diaverseapi/app/cabinet/offers/crypton/service.py` lazy immediate window заменен на persistent scheduled window: `scheduled_at` выбирается детерминированным hash-распределением по пользователю и дате цикла в пределах 30 дней, missed unclaimed окна истекают после локального cutoff 20:00, `shown` окно после завершенного cooldown освобождает следующий цикл. В `diaweb/frontend/modules/crypton/components/CryptonOfferGate.tsx` добавлен refetch-таймер по scheduled Crypton window и уточнено блокирование shop promo. Проверки: `ruff check app/cabinet/offers/crypton/service.py tests/test_cabinet_crypton.py`, `pytest tests/test_cabinet_crypton.py` (41 passed), `tsc --noEmit --pretty false`; ESLint не запустился из-за отсутствующего `eslint.config.*` при ESLint 9. GBrain sync выполнен по `diaverseapi-code,diaweb-code`.
- В `diaweb/frontend/modules/staff-shop/components/CryptonRequestsPanel.tsx` убран `setState` внутри `useEffect`: экспортируемый компонент теперь remount-ит внутреннюю панель по `initialRequestId`, сохраняя reset deep-link состояния без `react-hooks/set-state-in-effect`. Добавлен тест в `frontend/__tests__/modules/staff-shop/CryptonRequestsPanel.test.tsx` на смену initial request id. Проверки: `npm run lint -- modules/staff-shop/components/CryptonRequestsPanel.tsx __tests__/modules/staff-shop/CryptonRequestsPanel.test.tsx`, `npm test -- __tests__/modules/staff-shop/CryptonRequestsPanel.test.tsx` (4 passed), `npx tsc --noEmit --pretty false`, полный `npm run lint` (0 errors, 75 warnings). Коммиты: `diaweb/master` `06e0407`, `diaweb/dev` `05cc543`. GBrain sync выполнен по `diaweb-code`.
- На production backend DB целевой staff-пользователь найден по Telegram identity и проверен как `superadmin`. Предыдущее активное Crypton window `shown` переведено в `expired` с metadata ручной реактивации, создано новое окно `available` до локального cutoff 20:00. Постпроверка: активных Crypton windows для пользователя `1`, активных Crypton requests `0`.
- Для следующего production staff-пользователя обнаружена старая Crypton request `checkout_pending` с неоплаченной payment session `awaiting_payment` от 2026-05-26; она блокировала новый popup. Payment session переведена в `expired`, request переведена в `expired`, cooldown старого consumed window очищен, создано новое `available` window. Direct service check `CabCryptonService.get_state()` вернул `availability=available`, `popup_available=True`, `can_claim_shown=True`, active requests `0`.
- В `diaverseapi` для `crypton.request.submitted` metadata расширена `user_id`, `user_tg_id` или verified `user_email`; `CabLogNotificationPolicy` пропускает эти поля в безопасный context. В `aibot` добавлен отдельный compact formatter для Crypton Telegram alert: пользователь, предмет, количество, proposed/market price, discount percent от market price и ссылка на `/ru/staff/shop?tab=crypton`. Проверки: `diaverseapi` target pytest по Crypton metadata и logging notifications (7 passed), `diaverseapi` targeted Ruff, `aibot` `tests/test_ops_alerts_sender.py` (9 passed), `aibot` `py_compile`. Коммиты/пуш не выполнялись из-за параллельной работы второго агента и смены `diaverseapi` на `main`.
- Коммиты запушены: `diaverseapi/main` `62e0d623`, cherry-pick в `diaverseapi/dev` `b8d75587`, `aibot/dev` `e1874fb`. `aibot` на сервере обновлен fast-forward `53a0512 -> e1874fb`, `copywriting-api` пересобран, runtime сохранил Crypton topic id `58428` и allowlist `crypton.request.submitted`, контейнер healthy.
- Реализован persisted ack для Crypton decision popup: `diaverseapi` добавил `decision_popup_available`, endpoint `/state/claim-decision-shown` и хранение claim по статусу заявки в `CabCryptonRequest.metadata_json`; `diaweb` добавил BFF route, API/mutation/types, разделение `CryptonOfferGate` на `initial`/`decision` и статусную copy в `CryptonOfferModal`. Проверки: `pytest tests/test_cabinet_crypton.py -q` (45 passed), targeted backend Ruff, `npm run typecheck`, `npm test -- __tests__/modules/crypton/CryptonOfferGate.test.tsx __tests__/modules/crypton/CryptonOfferModal.test.tsx` (8 passed), `git diff --check`. GBrain sync выполнен по `diaweb-code` и `diaverseapi-code`.
- В `diaverseapi` Crypton decision/fulfillment notifications получили русские заголовки и explicit `action.label`; staff logging alert schema расширена `staff_action_url`/`staff_action_label`, а summary новой заявки теперь содержит пользователя, предмет, количество, цену и discount percent. В `diaweb` `NotificationBell` разделил fallback CTA для `login` и `navigate`, добавлен блок Crypton request details в staff logging detail panel. Проверки: `pytest tests/test_cabinet_crypton.py tests/test_cabinet_logging_notifications.py -q` (52 passed), targeted Ruff, `vitest` по NotificationBell/LoggingDashboard (19 passed), `npm run typecheck`, `git diff --check`. GBrain sync не запускался, чтобы не смешивать параллельные незакоммиченные изменения второго агента в тех же child repos.

- diaverseapi: dev commit d0b15205, prod cherry-pick 184cbd76. Добавлен RaidChestGateway и regression tests для claim flow и маппинга uncommon -> unusual. Проверки: targeted pytest app/raids/tests/test_settlement_service.py app/raids/tests/test_raids_flow.py passed; Ruff passed. Полный app/raids/tests: 97 passed, 3 account_lifecycle tests blocked by missing local DB diaverse. Prod compensation inserted 611 lootbox_user rows: basic 194, unusual 164, rare 153, epic 97, legendary 3. GBrain targeted sync attempted but blocked by embedding config, retry with no-embed blocked by sync lock.
- Исправлен bug рейдовых pet abilities: `trap_immunity` и `chest_chance_multiplier` теперь матчятся по `Character.kind`/`pet_kind`, а не по `pet_name_snapshot`; fallback по имени оставлен для старых сценариев. Коммиты: `diaverseapi/dev` `ed4985c6`, `diaverseapi/main` merge `04367705`, `diaweb/master` merge `a52fa7b`. Проверки: backend targeted pytest 63 passed на dev, backend merge pytest 163 passed, Ruff passed; frontend targeted vitest 60 passed. Prod correction: 4 ошибочные active traps сняты, 23 ошибочные scheduled trap checks очищены, 1 rescue attempt отменен и rescuer возвращен, 1 уже claimed участник доначислен через claim gateway. Дополнительная deterministic chest compensation по исправленному ability-множителю: 93 claimed участника проверены, 51 получил 88 шкатулок; повторный pass обработал еще 5 новых/непомеченных участников без дополнительных начислений. Финальная сверка: active bad traps 0, active bad scheduled checks 0, unmarked claimed ability participants 0.
- Реализовано дублирование shop bundle: в `diaverseapi/app/cabinet/shop/admin_service.py` добавлен `duplicate_item`, который копирует `CabShopItem`, fulfillment lines и offers, генерирует уникальный `source_ref` и `title_override` вида `<название> копияN`; в `admin_api.py` добавлен `POST /v1/admin/shop/items/{item_id}/duplicate`. В `diaweb` добавлены `duplicateShopAdminItem`, mutation в `ShopAdminPage` и кнопка `Дубль` только для `source_type=bundle` в `ShopListingTable`. Проверки: `pytest tests/test_cabinet_shop_admin.py -q` (48 passed), targeted backend Ruff passed, `npm run test -- __tests__/modules/staff-shop/ShopAdminPage.test.tsx __tests__/modules/staff-shop/shop-admin-api.test.ts` (31 passed). `npm run typecheck` не прошел из-за параллельных изменений в cabinet notification test fixtures (`navigateCta`), не связанных с bundle duplication.
- Создан fast AIF plan `.ai-factory/PLAN.md` для механики raid location immunities. План затрагивает `diaverseapi` и `diaweb`, фиксирует 9 item keys, отдельный backend-домен иммунитетов, endpoint активации, продление `active_until`, применение иммунитета в `command_service` и `trap_service`, UI в `RaidDispatchStep`/`RaidHud`, тесты и Alembic SQL verification. Продуктовый код не менялся; отмечены существующие dirty raid-файлы в `diaverseapi`, которые нужно сохранить при реализации.
- Выполнен `$aif-improve` для `.ai-factory/PLAN.md`: план переписан с двухтабличной моделью `RaidImmunityGrant`/`RaidImmunityEffect`, first-class `raid_immunity` catalog type, совместимостью старых Advent/boost ids, idempotent activation command через `RaidIdempotencyRecord`, backfill ранее log-only наград, включением immunity expiry в `next_refresh_at`, snapshot enforcement для trap checks, точными frontend route/API/mutation/UI/i18n точками и расширенным списком backend/frontend тестов. Повторная проверка показала `diaverseapi` clean, поэтому старый риск про dirty raid-файлы убран.
- Выполнен targeted GBrain sync для `diaverse-aif`: обновленный `.ai-factory/PLAN.md` импортирован, ошибок синхронизации нет.
- Task 1 raid immunity implementation: в `diaverseapi` добавлен `app/raids/domain/immunities.py`, ORM-модели `RaidImmunityGrant`/`RaidImmunityEffect`, repository methods для выдачи/списков/lock/consume/extend, Alembic revision `raid_immunity_items_20260605` с `down_revision=exchange_instance_id_20260604`, короткими именами constraints/indexes и `metadata_json` -> DB `metadata`. Проверки: `compileall app/raids migrations/versions/20260605_raid_immunity_items.py`, `alembic heads`, `alembic upgrade exchange_instance_id_20260604:raid_immunity_items_20260605 --sql` passed.
- Crypton status Telegram notification plan перенесен в `.ai-factory/plans/crypton-status-telegram-notifications.md`, а active fast plan `.ai-factory/PLAN.md` восстановлен под raid immunity для работы другого агента. Crypton-план фиксирует outbox-подход: `diaverseapi` владеет очередью/получателями/миграцией и enqueue из Crypton decision flow, `diaverse-auth-bot` поллит signed claim/ack/nack endpoints и отправляет `send_photo`/fallback `send_message`. В требования добавлена user-facing ссылка через `CABINET_PUBLIC_BASE_URL` на `/{lang}/offers` вместо staff/admin ссылок, а также fallback image URL для Telegram.
- Task 1-2 Crypton status Telegram notifications: в `diaverseapi` добавлены `auth_bot_tg_outbox`, Alembic revision `auth_bot_tg_outbox_20260605`, сервис `AuthBotTelegramOutboxService`, signed endpoints `/v1/auth/internal/auth-bot-outbox/claim|ack|nack`, env-параметры retry/max-attempts/fallback image. Проверки: `compileall app/security/models.py app/security/schemas.py app/security/auth_bot_outbox.py app/security/api.py`, `alembic heads`, `alembic upgrade raid_immunity_items_20260605:auth_bot_tg_outbox_20260605 --sql` passed.
- Task 3-4 Crypton status Telegram notifications: `CabCryptonService.decide_admin_request()` после web notification non-fatal ставит `crypton_status_notification` в auth-bot outbox с idempotency `crypton:decision:telegram:<request>:<status>`. Payload включает status/title/summary, item title, quantity, proposed/market/accepted/decision price, discount text, optional reason/expires, image URL или `CRYPTON_TELEGRAM_FALLBACK_IMAGE_URL`, и user-facing `/{lang}/offers` link через `CABINET_PUBLIC_BASE_URL` с RU default. Проверка: `compileall app/cabinet/offers/crypton/service.py` passed.
- Task 5-7 Crypton status Telegram notifications: в `diaverse-auth-bot` добавлены signed outbox методы claim/ack/nack, env `AUTH_BOT_OUTBOX_WORKER_ID|POLL_SECONDS|BATCH_SIZE|LEASE_SECONDS`, сервис `AuthBotOutboxDeliveryService` с `send_photo` и fallback `send_message`, inline-кнопка offers URL и background polling task в `main.py`. Проверка: `python -m compileall app` passed.
- Task 8-10 Crypton status Telegram notifications: добавлены backend tests для auth-bot outbox и Crypton telegram payload/enqueue, auth-bot tests для signed claim/ack/nack и delivery formatter/executor. Проверки: `diaverseapi` targeted pytest 56 passed, targeted Ruff passed, Alembic SQL for `auth_bot_tg_outbox_20260605` passed, `diaverse-auth-bot` pytest 10 passed, `git diff --check` passed with line-ending warnings only. `python -m pip install -e ".[dev]"` выполнен локально для auth-bot verification; generated `egg-info` diff очищен. GBrain sync выполнен для `diaverse-auth-bot-code`; `diaverseapi-code` sync не запускался, чтобы не смешивать параллельные dirty-изменения другого агента.
- Crypton Telegram image requirement updated: `diaverseapi` добавил primary env `CRYPTON_TELEGRAM_IMAGE_URL`, payload больше не использует `item_snapshot_json.image_url`, а `CRYPTON_TELEGRAM_FALLBACK_IMAGE_URL` оставлен только как compatibility fallback. Локально найден `diaweb/frontend/public/offers/crypton/crypton-bg.webp`; ожидаемый prod URL для env: `https://diaverse.app/offers/crypton/crypton-bg.webp`. Проверки: targeted Crypton/outbox pytest 52 passed, targeted Ruff passed.
- Task 2 raid immunity implementation: добавлен first-class `raid_immunity` item type, 9 активных virtual catalog entries и legacy aliases для старых Advent ids, fulfillment handler создает `RaidImmunityGrant`, Advent reward routing переводит legacy `boost/custom` иммунитеты в реальные grant rows. Проверки: `.venv\Scripts\python.exe` catalog smoke check показал 9 active entries и legacy caves alias -> canonical key, Alembic offline SQL для `raid_immunity_items_20260605` passed.
- Task 3 raid immunity implementation: добавлен `app/cabinet/offers/advent/raid_immunity_backfill.py` и CLI `tools/backfill_raid_immunity_rewards.py`. Backfill читает только `UsersRewardsInfo` с `daily_reward`, распознает legacy/canonical immunity через общий resolver, использует deterministic `source_ref/source_line_key` и общий `RaidImmunityGrantService`, dry-run считает `would_create`, live требует `--yes-i-know`. Проверки: compileall, synthetic candidate smoke, targeted Ruff passed.
- Task 4 raid immunity implementation: расширены `app/raids/schemas.py` (`activate_immunity`, immunity inventory/effects DTO, error codes), `RaidStateService` возвращает active effects и available grants, location metadata содержит effective trap chance и action metadata, `next_refresh_at` учитывает `active_until`. Добавлен `RaidImmunityService` с `RaidIdempotencyRecord` replay/lock flow и route `POST /immunities/activate`. Проверки: targeted compileall, targeted Ruff, schema smoke import passed.
- Task 5 raid immunity implementation: `RaidCommandService._create_run()` читает active `RaidImmunityEffect` под lock, сохраняет `raid_item_immunity` в `RaidRun.state`, пишет start trap snapshot в `RaidParticipant.trap_roll_metadata`, выставляет `trap_chance_percent_snapshot=0` при item immunity и пропускает schedule `trap_check_at`. `RaidStateService._participant_read()` теперь возвращает trap snapshot metadata. Проверки: targeted compileall/Ruff по command/state services passed.
- Task 6 raid immunity implementation: `RaidTrapCheckService` роллит ловушку по `RaidParticipant.trap_chance_percent_snapshot` и сохраняет previous start snapshot в roll metadata; `RaidTrapService.reveal_rescue()` проверяет pet immunity и active `RaidImmunityEffect`, сохраняет `rescuer_immunity_sources`/`rescuer_immunity` и сохраняет старый boolean `rescuer_protected_by_immunity` для совместимости. Проверки: targeted compileall/Ruff по trap services passed.
- Task 7 raid immunity implementation: в `diaweb` добавлен route handler `app/api/cabinet/raids/immunities/activate/route.ts`, `activateRaidImmunity()` в API client, `RaidActivateImmunityInput`, optional `immunity_effects`/`immunity_inventory` DTO для backend rollout skew, и `useRaidImmunityActivationMutation()` в command mutations. Проверки: targeted ESLint passed, `npm run typecheck -- --pretty false` passed.
- Task 8-10 raid immunity implementation: в рейдовом UI добавлен блок иммунитета при выборе режима без чекбокса, кнопки активации предметов на 1/7/30 дней, отображение active-until и HUD-иконки с модалкой. Backend и frontend покрыты targeted тестами.
- Root GBrain sync выполнен только для `diaverse-docs` и `diaverse-aif`; code sources не синхронизировались, чтобы не захватить параллельные dirty-изменения другого агента.
- Push/merge: root `main` получил coordination commit, `diaverseapi/dev` получил combined raid immunity + auth-bot outbox commit и влит в `diaverseapi/main` merge commit, `diaweb/dev` получил raid immunity UI + Crypton asset commit, `diaverse-auth-bot/feature/auth-tgbot` получил outbox delivery commit. После merge `diaverseapi` возвращен на локальную ветку `dev`.
