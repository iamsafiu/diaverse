# Daily Work - 2026-06-08 - safiu

## Public digest

- Криптон сделан реально доступным для всех авторизованных пользователей: новые окна появляются сразу, а уже отложенные окна раскрыты для показа.

## Internal log

- Найдена причина, почему Криптон не появился после снятия staff-only ограничения: runtime был обновлен, но старая lazy-schedule логика распределяла окна на 30 дней. В `diaverseapi` изменена механика создания lazy window: spread сокращен до 1 дня, до cutoff новое окно создается сразу `available`, добавлена audit metadata `immediate_public_rollout`. Проверки: backend Ruff, `pytest tests\test_cabinet_crypton.py -q`, compileall. Hotfix запушен в `dev` и cherry-pick в `main`; prod runtime обновился через CI. Существующие scheduled windows переведены в `available` с audit metadata и временным cutoff bypass; prod smoke для обычного пользователя без staff-роли вернул `popup_available=true`.
