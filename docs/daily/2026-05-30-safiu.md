# Daily Work - 2026-05-30 [safiu]

## Public digest

- Добавлен безопасный dev-режим ускорения таймеров рейдов для тестирования полного цикла запуска и получения наград.

## Internal log

- В `diaverseapi` добавлены backend-настройки `RAIDS_DEV_FAST_TIMERS_ENABLED` и `RAIDS_DEV_FAST_TIMER_SECONDS`, helper `app/raids/domain/dev_tuning.py` и подключение override при расчете `RaidRun.completes_at`.
- Dev-конфигурация сервера обновлена перед пушем; значения не публикуются в daily.
- Проверка: `python -m compileall app\raids app\core\settings.py` прошла. `pytest` локально недоступен в текущем Python окружении.
