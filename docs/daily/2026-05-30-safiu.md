# Daily Work - 2026-05-30 [safiu]

## Public digest

- Добавлен безопасный dev-режим ускорения таймеров рейдов для тестирования полного цикла запуска и получения наград.
- Подготовлен быстрый план улучшения понятности рейдов: карта остается главным экраном, а механики, награды, риски и бонус серии раскрываются через нижнюю шторку.
- Улучшен интерфейс рейдов: игрок видит смысл локации, бонус серии, риски, награды, подходящую редкость пэта и формулы итогов без ухода от композиции карты с нижней шторкой.
- Доработана шторка рейдов: при нажатии на карту или таб она сразу раскрывается наверх, стрелка плавно меняет направление, а вход на островах остается в XDV.
- Оффер Криптона скрыт на всех страницах рейдов, включая вложенные raid-маршруты.
- Исправлен UX завершенных рейдов: награды больше не открываются автоматически при выборе биома, завершенный слот подсвечивается и открывает результат только по клику.
- Упрощена подача информации о рейдовых локациях: разведка и награды объединены в одну кнопку `О локации`, а повторяющиеся формулы наград скрыты.
- Для тестового аккаунта обновлена фабрика до полного состояния, чтобы можно было проверять все уровни, здания и производственные отсеки.
- Для тестового аккаунта сброшены занятые рейдовые слоты, чтобы можно было заново отправлять пэтов в рейды.

## Internal log

- В `diaverseapi` добавлены backend-настройки `RAIDS_DEV_FAST_TIMERS_ENABLED` и `RAIDS_DEV_FAST_TIMER_SECONDS`, helper `app/raids/domain/dev_tuning.py` и подключение override при расчете `RaidRun.completes_at`.
- Dev-конфигурация сервера обновлена перед пушем; значения не публикуются в daily.
- Проверка: `python -m compileall app\raids app\core\settings.py` прошла. `pytest` локально недоступен в текущем Python окружении.
- В `.ai-factory/PLAN.md` записан fast-план `Raid Mechanics Clarity UI` для `diaweb` с optional backend fallback для mode-specific шансов.
- Начата реализация UI-плана рейдов: добавлен frontend helper для форматирования бонуса серии, цен, множителей режимов и превью наград без изменения backend-механик.
- На острова рейдовой карты добавлены компактные бейджи серии, риска ловушки и XDV-входа; название `Стрик` заменяется в UI на `Бонус серии`.
- Нижняя шторка рейдов получила брифинг выбранной локации и вкладки `Слоты / Разведка / Награды` с превью ресурсов, шкатулок и особого лута.
- Карточки пэтов и шаг отправки рейда теперь объясняют подходящую редкость, цену x2/x3, множители XP/шкатулок, риск ловушки и прогноз ключевых наград.
- Карточки ловушек и модалка итогов рейда дополнены формулами выкупа, спасения, XP, ресурсов, шкатулок и особого лута.
- Backend для текущей UI-итерации не менялся: frontend рассчитывает mode-specific превью шансов из уже доступных базовых данных каталога.
- Верификация raid UI: `npm run typecheck` прошел, `npm test -- __tests__/modules/raids` прошел 33/33, targeted `npm run lint -- --file ...` для новых/измененных raid-файлов прошел. Полный `npm run lint` ранее был остановлен по таймауту локального запуска, поэтому заменен targeted lint; локальный dev server не запускался.
- UI follow-up: `RaidSlotsSheet` теперь по клику переходит из `peek` сразу в `full`, вкладки шторки тоже раскрывают `full`; `RaidLocationStack` открывает карту в `full` и не возвращает сценарные действия в среднее положение `slots`. `RaidLocationCard` всегда показывает вход в XDV и переносит бейджи серии/ловушки/XDV под строку входа.
- Проверка follow-up: `npm run typecheck`, `npm test -- __tests__/modules/raids` 33/33 и targeted lint по `RaidSlotsSheet`, `RaidLocationStack`, `RaidLocationCard` прошли.
- В `CryptonOfferGate` добавлен route guard для `/[lang]/raids` и `/[lang]/raids/*`, чтобы глобальный оффер Криптона не появлялся поверх рейдов. Проверки: `npm test -- __tests__/modules/crypton/CryptonOfferGate.test.tsx`, `npm run typecheck`, targeted lint по `CryptonOfferGate.tsx`.
- Raid slot completion fix: удален автопоказ `completed_runs` от выбранной локации; `RaidSlotGrid` связывает слот с active/completed run по `run_id`, `participant_id` или pet id. Активный слот показывает биом (`В рейде: Оазис`), завершенный слот получает glow-state (`Готово: Оазис`) и открывает `RaidResultModal` только по клику. Проверки: `npm run typecheck`, `npm test -- __tests__/modules/raids` 33/33, targeted lint по `RaidLocationStack`, `RaidSlotsSheet`, `RaidSlotGrid`, `RaidSlotCard`.
- Dev DB support update: для `tg_user_id=1064316520` выполнен идемпотентный full factory upgrade с audit marker `manual_full_factory_upgrade_20260530_tg1064316520`. Dry-run прошел с rollback, затем commit: `factory_level=8`, `buildings=12`, `resource_buildings_level8=6`, `production_buildings_active=8`, `compartments_level7=20`, `warehouse_balances=7`; отдельный post-check подтвердил профиль `active`, `onboarding_completed=true`, timers cleared.
- Dev DB support update: для `tg_user_id=8571201012` сброшены занятые raid slots через перевод `2` `raid_runs` и `2` `raid_participants` из `active` в `cancelled`; активных traps/rescue attempts не было. Audit marker `manual_raid_slot_reset_20260530_tg8571201012`; post-check: `locked_participants=0`, `locked_runs=0`, `active_traps=0`.
- Raid location info UI: `RaidSlotsSheet` убирает вкладки `Слоты / Разведка / Награды` и открывает `RaidLocationInfoModal` по кнопке `О локации`; модалка собирает summary, intel и `RaidRewardPreview`. `mechanicsDisplay` больше не возвращает noop-формулы для `x1` и неизменившихся значений; `RaidRewardPreview`, `RaidDispatchStep` и `RaidResultModal` скрывают дубли вида `1-2 -> 1-2` / `1-2 x 1x = 1-2`. Активный `.ai-factory/PLAN.md` обновлен, чтобы будущая реализация не возвращала композицию с тремя вкладками. Проверки: `npm run typecheck`, `npm test -- __tests__/modules/raids` 34/34, targeted lint по измененным raid-файлам.
