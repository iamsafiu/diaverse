# Daily Work - 2026-05-24 [safiu]

## Public digest

- Поправили верстку inline-цены в админке магазина: кнопка `OK` больше не вылезает из колонки цены в соседний столбец.

- Исправили включение товаров в админке магазина: если товар был выключен, включение колонок `Маг.` или `Спец.` теперь также активирует сам товар, а лишние catalog-пояснения убраны из таблицы и формы редактирования.

- В админке магазина обычную USDT-цену теперь можно видеть и менять прямо в таблице товаров; скидочная цена остается в форме редактирования.


- Исправили логику активации предметов в админке магазина: ручная USDT-цена теперь может разрешать продажу предметов, у которых игровая source-цена равна нулю, а админка получает причины блокировки и быстрее загружает каталог.

- Ускорили админку магазина: переключатели больше не ждут лишние публичные обновления, поиск в каталоге стал мягче за счет задержки ввода, а строки товара показывают причину блокировки или разрешение ручной USDT-цены.

- Добавили проверку исправлений магазина: покрыли тестами ручную USDT-активацию, жесткие блокировки, быстрый scoped catalog lookup и поведение админских переключателей без лишнего ожидания.


- Исправили хранение загруженных изображений для бандлов и товаров магазина: новые картинки теперь сохраняются в постоянном хранилище и не должны пропадать после деплоя или пересоздания контейнеров.

- Обновили доступность Crypton-оффера для повторного показа в кабинете: окно оффера снова активно до дневного cutoff.

- Упростили первое окно Криптона: убрали лишние подписи и пояснения, оставили основной переход к офферу и обновили русское название.

- Повторно открыли тестовое окно Криптона для проверки модалки после обновления.

- Вернули легенду в окно Криптона и сделали мобильную модалку полноэкранной без внешних отступов.

- Снова открыли тестовое окно Криптона для повторной проверки модалки.

- Перенесли кнопку в окно Криптона под основной текст, убрали отдельную правую область и увеличили изображение.

- Обновили тестовое состояние Криптона после создания заявки: закрыли pending-заявку и открыли новое окно для повторного показа.

- Привели блок статуса заявки Криптона к общему визуальному стилю сайта.

- Исправили повторную генерацию картинки рейтинга клуба: кнопка перегенерации теперь запускает новый AI-ассет, а не показывает старую картинку из кеша.

- Исправили отображение имен в рейтинге клуба: участники с Telegram-именем теперь показываются в рейтингах по имени, а не по старому связанному username.

## Internal log
- Исправлен приоритет лейблов рейтинга клуба в `diaverseapi`: `ClubMembershipDisplayLabel` разделяет manual label и linked-user fallback, `ClubLeaderboardService` и `ClubPairingService` теперь выбирают manual display -> Telegram first/last name -> Telegram username -> linked user fallback. Добавлены `[FIX:club-leaderboard-labels]` logs и регрессионные тесты для индивидуального и buddy рейтинга с кейсом `Ирина` вместо linked handle. Проверено `poetry run ruff check app/club/repositories.py app/club/leaderboards.py app/club/pairing.py tests/test_club_leaderboards.py`, `poetry run python -m pytest tests/test_club_leaderboards.py -q`, `poetry run python -m pytest tests/test_club_service.py -q`.



- Task 1 обновил `AGENTS.md` и `.ai-factory/skill-context/aif-implement/SKILL.md`.
- Правило разделяет публичные заметки и внутренние implementation details, а также запрещает попадание secrets, raw env values, private SSH details, internal IPs и infrastructure-only context в публичную публикацию.

- Task 2 добавил `docs/daily/TEMPLATE.md`, `scripts/daily-work-add.ps1`, `scripts/daily-work-publish.ps1` и `scripts/daily_work_publish.py`. Publish читает только `Public digest`, поддерживает dry-run, по умолчанию блокирует unsafe public markers и логирует только наличие config без env values.

- Task 3 создал `.codex/skills/daily-work/SKILL.md` и `agents/openai.yaml`. Validation прошел через Python из `aibot/.venv`, потому что стандартный Windows python shim был без PyYAML.

- Task 4 обновил `aibot/core/config.py`, `aibot/services/confluence_service.py` и `aibot/tests/test_confluence_service.py`. Добавлен выбор `source='daily_work'` с daily parent/label config и сохранено default update behavior. Проверено через `pytest tests/test_confluence_service.py`.

- Task 5 добавил `aibot/app/domain/daily_work_prompt.py` и подключил daily_work_prompt defaults/logging через `app/api/routes/styles.py`. Обновлены `tests/test_style_routes.py`, проверено через `pytest tests/test_style_routes.py`.

- Task 6 добавил migration `20260524_0001_copywriting_daily_work_posts.sql`, модель `CopywritingDailyWorkPost`, `DailyWorkPostRepository`, `app/application/use_cases/daily_work.py`, регистрацию worker processor и `tests/test_daily_work.py`. Проверено через `pytest tests/test_daily_work.py tests/test_confluence_service.py tests/test_style_routes.py`.

- Task 7 добавил `app/api/schemas/daily_work.py`, `app/api/routes/daily_work.py`, зарегистрировал router в `app/api/main.py`, расширил `tests/test_daily_work.py` и `tests/test_service_entrypoints.py`. Проверено через `pytest tests/test_daily_work.py tests/test_service_entrypoints.py tests/test_confluence_service.py tests/test_style_routes.py`.

- Task 8 завершен. Изменены `diaweb/frontend/app/api/staff/copywriting/daily-work-documents/recent/route.ts`, `daily-work-documents/[pageId]/post/route.ts`, `daily-work-posts/[postId]/route.ts`, `modules/copywriting/types.ts`, `api.ts`, `hooks.ts`, `daily-work-route.test.ts`, `CopywritingDailyPolling.test.ts`. Проверено через `npm test -- __tests__/app/api/staff/copywriting/daily-work-route.test.ts __tests__/modules/copywriting/CopywritingDailyPolling.test.ts`.

- Task 9 завершен. Добавлен `diaweb/frontend/modules/copywriting/components/DailyWorkPanel.tsx` и подключен в `CopywritingDailyView`. Обновлены `GlobalStyleEditor`, i18n types/dictionaries и tests. Проверено через `npm test -- __tests__/modules/copywriting/CopywritingDailyView.test.tsx __tests__/modules/copywriting/GlobalStyleEditor.test.tsx __tests__/modules/copywriting/CopywritingDailyPolling.test.ts __tests__/app/api/staff/copywriting/daily-work-route.test.ts`.

- Task 10 завершен. Добавлен `scripts/test_daily_work_publish.py`, обновлен `aibot/docs/web-copywriting-service.md`, прогнаны workspace script checks, skill validation, aibot pytest suite, diaweb typecheck/lint/tests, changed-file sensitive marker scan и `scripts/graphify-update.ps1`.

- Остановлен diaweb dev server на port 3000. Созданы commits: `aibot` `2bc4982 feat(copywriting): add daily work generation flow` и `diaweb` `3a99683 feat(copywriting): add daily work staff tab`.

- Запушены `aibot` dev commit `2bc4982`, `diaweb` dev commit `3a99683` и `diaweb` master commit `73352c9` для CI/CD. На сервере обновлен `aibot` из dev, применена migration `20260524_0001_copywriting_daily_work_posts.sql`, пересобраны и перезапущены `copywriting-api` и `copywriting-worker`, health probes прошли. Создана Confluence-иерархия `Daily docs -> Daily Work`, настроены локальный `.env.daily-work` и server Daily Work parent settings. Graphify update был намеренно остановлен и пропущен по просьбе пользователя.

- Updated daily-work skill instructions, AIF implement hook, AGENTS.md, docs/daily/TEMPLATE.md, script fallback template, and translated docs/daily/2026-05-24-safiu.md. Re-published the Russian public digest to Confluence. Graphify intentionally skipped.

- Tasks 1-6: updated `diaverseapi` shop admin activation policy, catalog sellability metadata, admin read fields, item catalog provider dispatch, scoped admin catalog hydration, and mutation refresh logging. Fast checks passed with `poetry run ruff check app/cabinet/shop/admin_service.py app/cabinet/shop/admin_schemas.py app/cabinet/shop/catalog_rules.py app/cabinet/item_catalog/service.py app/cabinet/item_catalog/providers.py` and `poetry run python -m compileall app/cabinet/shop/admin_service.py app/cabinet/item_catalog/service.py app/cabinet/item_catalog/providers.py`.

- Fixed aibot MissingGreenlet on Daily Work POST by refreshing CopywritingDailyWorkPost after enqueue flush before API response serialization. Commit fde8026 pushed to aibot dev and deployed to /srv/aibot. Rebuilt/recreated copywriting-api and copywriting-worker; health probes passed. Did not run Graphify. Did not run production POST smoke to avoid creating an extra generation job.

- Tasks 7-9: reconciled existing `diaweb` changes for staff shop admin performance and UX. `ShopAdminPage.tsx` now uses targeted query invalidation, optimistic row cache updates, and background storefront invalidations. `ItemCatalogPicker.tsx` debounces search and keeps previous catalog data. `ShopListingTable.tsx`, `ShopListingEditor.tsx`, and `shop-admin-types.ts` surface catalog sellability reasons and staff-price override state.

- Tasks 10-12: added backend tests in `diaverseapi/tests/test_cabinet_shop_admin.py` and `diaverseapi/tests/test_cabinet_item_catalog.py` for update-item override/rollback, missing source hard block, excluded pet override, provider dispatch, scoped admin hydration, and sync-preview broad catalog behavior. Added frontend tests in `diaweb/frontend/__tests__/modules/staff-shop/ShopAdminPage.test.tsx` and `ItemCatalogPicker.test.tsx` for non-blocking availability toggles, sellability notices, editor context, and debounced search. Focused checks passed with `poetry run python -m pytest tests/test_cabinet_shop_admin.py tests/test_cabinet_item_catalog.py -q` and `npm run test -- __tests__/modules/staff-shop/ShopAdminPage.test.tsx __tests__/modules/staff-shop/ItemCatalogPicker.test.tsx`.

- Final shop admin implementation checks: backend Ruff passed, backend pytest passed on `dev` and again after cherry-pick to `main`; frontend Vitest and `npm run typecheck` passed on `dev` and again after cherry-pick to `master`. Pushed `diaverseapi/dev` commit `04fb1596`, `diaverseapi/main` commit `ce2d35c8`, `diaweb/dev` commit `1140dd4`, and `diaweb/master` commit `ce43c17`. Graphify update intentionally skipped by direct user instruction.

- Добавлен inline edit обычной цены в `diaweb/frontend/modules/staff-shop/components/ShopListingTable.tsx` и отдельная мутация в `ShopAdminPage.tsx`, которая обновляет или создает catalog external offer через `display_price_amount` и не отправляет `manual_discount_*`. Сортировка по цене теперь использует обычную цену, а скидочная цена скрыта из таблицы. Проверено `npm run lint -- modules/staff-shop/components/ShopListingTable.tsx modules/staff-shop/components/ShopAdminPage.tsx __tests__/modules/staff-shop/ShopAdminPage.test.tsx`, `npm run test -- __tests__/modules/staff-shop/ShopAdminPage.test.tsx`, `npm run typecheck` на `diaweb/dev` и после cherry-pick на `diaweb/master`. Pushed `diaweb/dev` commit `58688bf` and `diaweb/master` commit `1ae65e0`. Graphify intentionally skipped by direct user instruction.

- Исправлен frontend-баг staff shop availability: `ShopAdminPage.tsx` теперь добавляет `is_active: true` при включении `is_storefront_visible` или `is_special_offer_eligible` у inactive-строки, поэтому backend больше не сбрасывает галочку из-за неактивного item. `ShopListingTable.tsx` и `ShopListingEditor.tsx` больше не показывают видимые catalog/override пояснения. Добавлены регрессионные тесты для включения `Маг.` и `Спец.` у inactive staff-priced item. Проверено `npm run lint -- modules/staff-shop/components/ShopListingTable.tsx modules/staff-shop/components/ShopListingEditor.tsx modules/staff-shop/components/ShopAdminPage.tsx __tests__/modules/staff-shop/ShopAdminPage.test.tsx`, `npm run test -- __tests__/modules/staff-shop/ShopAdminPage.test.tsx`, `npm run typecheck`. Graphify intentionally skipped by direct user instruction.

- Исправлена CSS-регрессия inline price editor в `ShopListingTable.tsx`: убран жесткий `min-w-[132px]`, добавлены `w-full max-w-[136px] min-w-0`, сжимаемый input и компактные currency/button widths. Проверено `npm run lint -- modules/staff-shop/components/ShopListingTable.tsx __tests__/modules/staff-shop/ShopAdminPage.test.tsx`, `npm run test -- __tests__/modules/staff-shop/ShopAdminPage.test.tsx`, `npm run typecheck`. Graphify intentionally skipped by direct user instruction.
- Исправлено persistent-хранение staff shop uploads в `diaverseapi`: добавлен `SHOP_UPLOADS_DIR`, отдельный static mount `/static/shop/uploads`, upload теперь пишет в `/app/uploads/shop-uploads`, а compose/CI создают и монтируют `/home/storage/shop-uploads`. Добавлен регрессионный тест `test_service_upload_shop_image_uses_configured_persistent_upload_dir`. Проверено `poetry run python -m pytest -q tests/test_cabinet_shop_admin.py -k upload_shop_image -s`, `poetry run ruff check app/core/settings.py app/cabinet/shop/admin_service.py tests/test_cabinet_shop_admin.py`, `poetry run ruff check app/main.py --ignore E402`. Запушено `diaverseapi/dev` `5fdc43bf` и `diaverseapi/main` `88c556a6`; dev/prod runtime compose обновлен, backend-сервисы пересозданы, `/static/shop/uploads` проверен через временный файл с HTTP 200. Graphify intentionally skipped by direct user instruction.

- На prod вручную сброшено Crypton offer window `c765f927-5509-4308-823c-ab739d394906` для `tg_user_id=8057982030`: перед изменением подтверждено отсутствие активных `cab_crypton_requests`, затем в одной транзакции выставлен `status=available`, очищены `shown_at`, `show_claimed_at`, `cooldown_until`, обновлен `expires_at=2026-05-24 15:00:00+00` (`20:00 Asia/Yekaterinburg`). Изменений кода не было.

- Обновлена Crypton-модалка в `diaweb`: из `CryptonOfferModal.tsx` удалены eyebrow/title/typewriter/cooldown/panel copy и explanatory hint, из `cryptonOfferModal.module.css` удален `.imageRing::before`, в `ru.json` видимые `Crypton` заменены на `Криптон`, ненужные modal copy очищены. Проверено `npm run test -- __tests__/modules/crypton/CryptonOfferModal.test.tsx __tests__/modules/crypton/crypton-i18n.test.ts`, `npm run lint -- modules/crypton/components/CryptonOfferModal.tsx modules/crypton/components/cryptonOfferModal.module.css __tests__/modules/crypton/CryptonOfferModal.test.tsx __tests__/modules/crypton/crypton-i18n.test.ts` (CSS module ignored by ESLint config), `npm run typecheck`. Запушено `diaweb/dev` `40e260b` и `diaweb/master` `9992f4d`. Graphify был остановлен после прерывания и повторно не запускался по просьбе пользователя.

- Повторно сброшено Crypton offer window `c765f927-5509-4308-823c-ab739d394906` для retest после того, как браузер уже вызвал `claim-shown` в `2026-05-24T12:08:30Z`: выставлен `status=available`, очищены `shown_at`, `show_claimed_at`, `cooldown_until`, `expires_at` оставлен на `2026-05-24 15:00:00+00`. Изменений кода и Graphify update не было.

- В `diaweb` восстановлена левая legend copy в `CryptonOfferModal.tsx`: `Персональный сигнал`, заголовок `Криптон`, animated typewriter и cooldown hint под изображением. Правый блок остался только с CTA. В `cryptonOfferModal.module.css` мобильная модалка растянута на `100vw`/`100dvh` без overlay padding и с `border-radius: 0`; `.imageRing::before` не возвращался. Проверено `npm run test -- __tests__/modules/crypton/CryptonOfferModal.test.tsx __tests__/modules/crypton/crypton-i18n.test.ts`, `npm run lint -- modules/crypton/components/CryptonOfferModal.tsx modules/crypton/components/cryptonOfferModal.module.css __tests__/modules/crypton/CryptonOfferModal.test.tsx __tests__/modules/crypton/crypton-i18n.test.ts` (CSS module ignored by ESLint config), `npm run typecheck`. Запушено `diaweb/dev` `424de45` и `diaweb/master` `d352a80`. Graphify не запускался по просьбе пользователя.

- Повторно сброшено Crypton offer window `c765f927-5509-4308-823c-ab739d394906` для `tg_user_id=8057982030`: активных `cab_crypton_requests` нет, окно возвращено в `available`, `shown_at`, `show_claimed_at`, `cooldown_until` очищены, `expires_at=2026-05-24 15:00:00+00`. Изменений кода и Graphify update не было.

- В `diaweb` обновлена структура Crypton-модалки: удален `actionPane`/`entryPanel`, CTA `На черный рынок` перенесен сразу после `cooldownHint`, desktop и mobile используют единую широкую колонку, shell width увеличен до `min(1360px, 100%)`, `.imageRing` выставлен `width: min(360px, 100vw)`, `.cryptonImage` `width/height: 100%`, кнопке добавлен `padding-inline: 25px`. Проверено `npm run test -- __tests__/modules/crypton/CryptonOfferModal.test.tsx __tests__/modules/crypton/crypton-i18n.test.ts`, `npm run lint -- modules/crypton/components/CryptonOfferModal.tsx modules/crypton/components/cryptonOfferModal.module.css __tests__/modules/crypton/CryptonOfferModal.test.tsx __tests__/modules/crypton/crypton-i18n.test.ts` (CSS module ignored by ESLint config), `npm run typecheck`. Запушено `diaweb/dev` `516d574` и `diaweb/master` `706065d`. Graphify не запускался.

- На prod повторный Crypton reset для `tg_user_id=8057982030`: обычный reset был остановлен из-за активной pending-заявки `a9fc87e4-2d6b-41ed-a2b1-1a58b4b7a9e5`; заявка переведена в `cancelled`, старое окно `c765f927-5509-4308-823c-ab739d394906` переведено в `cancelled`, создано новое активное окно `0a30f286-25e9-43c6-9458-127cf395d3bd` со `status=available`, `expires_at=2026-05-24 15:00:00+00`, `latest_claimed_count=0`, активных заявок `0`. Изменений кода и Graphify update не было.

- В `diaweb` обновлен CSS блока `crypton-status-panel`: pending/status panel получил общий glass/neon стиль Crypton request dialog, градиентный фон, glow/border, styled badge/title/copy, summary cards в стиле price grid и reason box. Проверено `npm run test -- __tests__/modules/crypton/CryptonOfferBuilder.test.tsx __tests__/modules/crypton/CryptonOfferModal.test.tsx`, `npm run lint -- modules/crypton/components/cryptonOfferModal.module.css modules/crypton/components/CryptonOfferBuilder.tsx __tests__/modules/crypton/CryptonOfferBuilder.test.tsx` (CSS module ignored by ESLint config), `npm run typecheck`. Запушено `diaweb/dev` `20547bb` и `diaweb/master` `6522ec4`. Graphify не запускался.

- Исправлен force regenerate flow для картинок рейтинга клуба. В `diaverseapi` добавлен `force_regenerate`, новый `:regen:<token>` idempotency key для aibot, сброс stale publish/image metadata после принятия новой генерации и publish override на текущий `aibot_idempotency_key`. В `diaweb` кнопка готовой картинки отправляет `{ force_regenerate: true }`. Проверено `poetry run python -m pytest tests/test_club_leaderboards.py -q`, `poetry run ruff check app/club/aibot_client.py app/club/leaderboards.py app/club/admin_api.py app/club/schemas.py tests/test_club_leaderboards.py`, `poetry run python -m pytest tests/test_club_auth_and_clubbot.py -q`, `npm run test -- __tests__/modules/club/ClubLeaderboardsPanel.test.tsx`, `npm run typecheck`; Graphify обновлен через `scripts/graphify-update.ps1`.

- Фикс картинок рейтинга клуба запушен в dev и production ветки обоих репозиториев: `diaverseapi/dev` `65ed0034`, `diaverseapi/main` `734e8cb6`, `diaweb/dev` `e65a40f`, `diaweb/master` `0fa3c0f`. На production-ветках повторно проверено `poetry run python -m pytest tests/test_club_leaderboards.py -q`, `poetry run ruff check app/club/aibot_client.py app/club/leaderboards.py app/club/admin_api.py app/club/schemas.py tests/test_club_leaderboards.py`, `npm run test -- __tests__/modules/club/ClubLeaderboardsPanel.test.tsx`, `npm run typecheck`.
