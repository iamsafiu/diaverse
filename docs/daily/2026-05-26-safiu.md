# Daily Work - 2026-05-26 [safiu]

## Public digest



- Продвинута backend-часть веб-фабрики: добавлена поддержка временных сотрудников-бустеров для ускорения, увеличения выхода и снижения затрат. Найм использует существующие игровые доллары, без новой фабричной валюты.
- Усилены backend-проверки фабрики: добавлены сценарии на повтор команд, защиту от двойных действий, дневной лимит импульсов и корректное снятие early-access ограничений после повышения уровня.
- Закрыт пропуск в backend API фабрики: добавлен маршрут для получения импульсов по шагам и тесты на приватный no-store контракт, rollout-disabled состояние и auth-only поведение.
- Усилены миграционные проверки фабрики: добавлен offline sentinel на лимит имен PostgreSQL и подтверждена SQL-компиляция factory migration.
- Начата frontend-интеграция фабрики: добавлен same-origin BFF слой `/api/cabinet/factory/*` для проксирования приватных factory API без хранения ответов в кеше.
- Добавлен frontend-клиент фабрики: типизированные запросы состояния/каталога, команды с idempotency, React Query hooks и серверное время для таймеров.
- Добавлены frontend-тесты фабричного BFF и API-клиента: закреплены приватные заголовки, no-store поведение, idempotency и нормализация ошибок.
- Подготовлен временный asset pack фабрики для слоёной сцены: карты, состояния зданий, иконки, бейджи и эффекты с manifest для будущей замены финальным артом.
- Подключена базовая оболочка веб-фабрики в кабинете: маршрут защищён авторизацией, не кешируется как публичная страница и готов к включению через rollout-флаг.
- Добавлена первая мобильная оболочка веб-фабрики: карта, склад, маршруты цехов/отсеков и стартовый онбординг на 4 подсказки.
- Добавлен слоёный рендер карты фабрики: фон, здания, состояния, эффекты, кликабельные области и подсказки теперь строятся из manifest.
- Собран экран центрального склада веб-фабрики: накопление, хранилище, таймер остановки, автосборщик, переносы ресурсов и безопасное пустое состояние.
- Собран экран ресурсного цеха веб-фабрики: строительство, улучшение, лимиты уровня, стоимость, производственная часть и состояния "в разработке" теперь отображаются из состояния сервера и каталога.
- Собраны экраны производственного цеха и отсека фабрики: строительство production-части, ранний доступ, карточки отсеков, запуск крафта, таймеры, сбор готового результата и удаление queued-слота работают через существующие backend-контракты.
- Добавлен фабричный drawer инвентаря на вебе: он показывает фабричную секцию существующего инвентаря, фильтры по типам предметов и выбор материалов для крафта без отдельного web/factory inventory.

- Добавлены ключевые диалоги веб-фабрики: улучшения, снос, сборка токена слота, подписки, работники-бустеры и состояния "в разработке" теперь подключены к существующим backend-командам и балансам.
- Добавлен стек событий и анимационный слой веб-фабрики: уведомления группируются в компактные карточки, а основные действия фабрики получили reduced-motion-safe анимационные hooks.
- Усилено frontend-покрытие веб-фабрики: добавлены проверки карты, ошибок, событий очереди/подписки и итоговый targeted suite по factory-экранам.
- Усилена доступность веб-фабрики: диалоги и инвентарь теперь удерживают фокус внутри панели, закрываются с клавиатуры, а мобильная оболочка закреплена как узкая центрированная колонка.
- Пройден финальный frontend-gate веб-фабрики: lint, typecheck, targeted tests и production build проходят на текущей реализации.
- Зафиксирован интеграционный smoke-путь веб-фабрики без браузерного запуска: auth/rollout/navigation, склад, крафт, токены, подписки и payment-финализация сопоставлены с тестовым покрытием.
- Добавлен канонический developer/user handoff по веб-фабрике: архитектура, API, каталог, ассеты, Figma, мобильные границы и support/debug процедуры собраны в отдельный feature-док.
- Обновлён локальный GBrain по фабрике после стабилизации кода и документации, чтобы следующие AI-сессии видели актуальную архитектуру и smoke-доказательства.
- Завершён финальный cross-repo аудит веб-фабрики: backend/frontend проверки зелёные по factory-сценариям, а браузерная проверка намеренно пропущена по текущей инструкции.
- Веб-фабрика включена на dev-стенде: frontend и backend rollout-флаги выставлены, web-контейнер пересобран, приватный роут фабрики теперь проходит до авторизационного слоя.
- Устранён dev-блокер запуска фабрики: backend enum подписок дополнен фабричными feature keys, миграционный граф снова сведён к одному head, factory state успешно собирается на dev.
- Главный экран веб-фабрики переведён ближе к игровому виду из ТЗ: вместо кабинетной сборки подключена полноэкранная мобильная карта с цехами, складом, верхней игровой шапкой и нижней покупкой уровня.
- Исправлен главный экран веб-фабрики: карта зафиксирована на весь экран без внутреннего перетаскивания, кабинетная обвязка скрыта на входе в фабрику, подписи цехов переведены на русский.
- Возвращена нижняя кабинетная навигация на главный экран фабрики: скрытым остаётся только верхний бар с логотипом, скачиванием и уведомлениями.
- Информационные окна над цехами главного экрана фабрики приведены к игровому A2-формату из ТЗ: показывают иконку, название, состояние, таймеры/прогресс при наличии данных и кнопку сбора готового результата в склад.
- Экран центрального здания фабрики переработан под A3 из ТЗ: склад получил собственную игровую шапку, крупную иллюстрацию здания, разделы «На складе» и «В хранилище», предупреждение о лимите 10 часов и правильные пустые состояния.
- Исправлена видимость главной карты фабрики: fullscreen-контейнер больше не схлопывает ширину, карта занимает всё доступное пространство над нижней навигацией без кабинетных бордеров, footer и лишних padding.
- Окно покупки следующего уровня фабрики приведено к A4 из ТЗ: теперь это игровая карточка перехода с превью следующей карты, крупной ценой, альтернативой в кирпичах, требованиями с галочками/крестиками и кнопкой «Купить и перейти».

## Internal log



- Закрыта Task 25 в `.ai-factory/plans/feature-factory-web-engine.md`: добавлены catalog-driven booster rules, `FactoryBoosterService`, endpoint `POST /v1/cabinet/factory/boosters/hire`, settlement истекших бустеров, сериализация `boosters` в state response и применение booster modifier snapshot в crafting quote.
- Проверки diaverseapi: `pytest app/factory/tests/test_catalog.py app/factory/tests/test_booster_service.py app/factory/tests/test_crafting_service.py app/factory/tests/test_state_service.py -q` -> 28 passed; `pytest app/factory/tests -q` -> 92 passed; `pytest app/factory/tests tests/test_cabinet_payment_sessions.py -q` -> 97 passed; `ruff check app/factory app/shards_and_resources/models.py migrations/versions/factory_web_state_20260525.py` -> passed.
- Закрыта Task 26: расширены `test_command_service.py`, `test_crafting_service.py`, `test_impulse_service.py` проверками idempotency replay/locked/conflict, collect-too-early, double-collect, impulse daily cap/replay и early-access normalization. Проверки: targeted command/crafting/impulse/warehouse/building tests -> 37 passed; `pytest app/factory/tests -q` -> 99 passed; Ruff по factory -> passed.
- Закрыта Task 27: добавлен `test_api.py`, `FactoryImpulseClaimRequest`, dependency `get_factory_impulse_service`, endpoint `POST /v1/cabinet/factory/impulses/claim`. Проверки: API/impulse tests -> 12 passed; `pytest app/factory/tests -q` -> 108 passed; `pytest app/factory/tests tests/test_cabinet_payment_sessions.py -q` -> 113 passed; Ruff по factory -> passed.
- Закрыта Task 28: добавлен Alembic identifier-limit sentinel в `tests/test_alembic_graph.py`; `alembic heads` -> `factory_web_state_20260525 (head)`; `alembic upgrade pet_events_comments_20260525:factory_web_state_20260525 --sql` -> compiled; `pytest tests/test_alembic_graph.py tests/test_merge_account_coverage.py -q` -> 12 passed. `tests/test_delete_user_fk_coverage.py` попытка: не пройден локально из-за отсутствия запущенного PostgreSQL и `docker`; оставить обязательным CI/pre-merge DB-backed check.
- Закрыта Task 29: добавлены `frontend/app/api/cabinet/factory/_utils.ts` и route handlers для catalog/state/open/levels/buildings/compartments/warehouse/impulses/craft-jobs/slot-tokens/boosters. Проверка: `npm run lint -- app/api/cabinet/factory modules/factory/types.ts` -> passed.
- Закрыта Task 30: добавлены `frontend/modules/factory/constants.ts`, `api.ts`, `hooks/useFactoryState.ts`, `hooks/useFactoryMutations.ts`, `hooks/useFactoryTimers.ts`, `index.ts`; расширен `types.ts` под backend state/jobs/boosters/notifications и command inputs. Проверки: `npm run lint -- modules/factory app/api/cabinet/factory` -> passed; `npm run typecheck` -> passed.
- Закрыта Task 31: добавлены `frontend/__tests__/app/api/cabinet/factory/proxy-utils.test.ts`, `route.test.ts`, `frontend/__tests__/modules/factory/factory-api.test.ts`. Проверки: `npm run test -- __tests__/app/api/cabinet/factory __tests__/modules/factory` -> 13 passed; `npm run lint -- modules/factory app/api/cabinet/factory __tests__/app/api/cabinet/factory __tests__/modules/factory` -> passed; `npm run typecheck` -> passed.
- Закрыта Task 32: добавлены временные SVG assets в `frontend/public/factory/{maps,buildings,resources,production,subscriptions,ui,effects}` и typed manifest `frontend/modules/factory/assetManifest.ts` со scene/hotspot/Figma handoff metadata. Проверки: `npm run lint -- modules/factory` -> passed; `npm run typecheck` -> passed; asset count under `public/factory` -> 44 files.
- Закрыта Task 32A: `/[lang]/factory` добавлен в auth-only proxy/routeAccess, private cache suffixes, gated BottomNav/CabinetTopbar с `NEXT_PUBLIC_FACTORY_WEB_ENABLED`, `nav-factory.svg`, i18n labels и route-motion ranking. Добавлена минимальная страница `app/[lang]/(cabinet)/factory/page.tsx` для controlled rollout-disabled state. Проверки: targeted nav/proxy/cache tests -> 87 passed; targeted lint -> no errors, 2 existing `next/image` mock warnings; `npm run typecheck` -> passed.
- Закрыта Task 33: добавлены route pages `/[lang]/factory`, `/factory/warehouse`, `/factory/workshops/[buildingKey]`, `/factory/workshops/[buildingKey]/compartments/[compartmentKey]`; компоненты `FactoryShell`, `FactoryUnavailableState`, `FactoryOnboardingOverlay`; factory i18n copy; localStorage persistence onboarding по `profile_id` с уважением backend `profile.onboarding_completed`. Проверки: targeted factory/nav/proxy/cabinet tests -> 95 passed; targeted lint -> no errors, 3 `next/image` mock warnings; `npm run typecheck` -> passed.
- Закрыта Task 34: добавлены `FactoryScene`, `FactoryHotspotLayer`, `BuildingInfoBubble`, `factoryScene.module.css`; сцена использует manifest hotspots, поддерживает background/building/state/effect layers, focus/hover bubble, bounded pointer pan, disabled hotspots для level 8 map-only, WARNING на missing mappings и DEBUG interactions за `NEXT_PUBLIC_FACTORY_DEBUG_INTERACTIONS`. Проверки: targeted scene/shell/nav/proxy/cabinet tests -> 99 passed; targeted lint -> no errors, 2 `next/image` mock warnings; `npm run typecheck` -> passed.
- Закрыта Task 35: добавлены backend metadata `last_accrual_at`/`stop_at`/`remaining_stop_seconds`/`cap_seconds` для складских балансов и warehouse stop hints в `next_refresh_at`; добавлен `FactoryWarehouseScreen`, i18n copy, route wiring через `FactoryShell`, transfer actions и focused tests. Проверки: `pytest app/factory/tests -q` -> 108 passed; `ruff check app/factory` -> passed; `npm run test -- __tests__/app/api/cabinet/factory __tests__/modules/factory` -> 28 passed; targeted frontend lint -> no errors, 3 `next/image` mock warnings; `npm run typecheck` -> passed.
- Закрыта Task 36: добавлен `ResourceWorkshopScreen`, route wiring через `FactoryShell`, i18n copy и tests. Экран читает catalog/state, показывает resource part status/level/output, construction costs, XDV upgrade costs, factory-level cap, progress placeholder, demolition entry без destructive submit до Task 39, production part card и in-development states. Проверки: `npm run test -- __tests__/app/api/cabinet/factory __tests__/modules/factory` -> 33 passed; targeted frontend lint -> no errors, 4 `next/image` mock warnings; `npm run typecheck` -> passed.
- Закрыта Task 37: добавлены `catalogView`, `ProductionWorkshopScreen`, `CompartmentScreen`, route `/factory/workshops/[buildingKey]/production`, wiring A5 -> A6 -> A7, i18n copy и focused tests. A7 использует существующий `inputOverrides` contract для выбора материалов и не вводит отдельный web/factory inventory domain. Проверки: `npm run test -- __tests__/app/api/cabinet/factory __tests__/modules/factory` -> 42 passed; targeted frontend lint -> no errors, 6 `next/image` mock warnings; `npm run typecheck` -> passed.
- Закрыта Task 38: добавлен `FactoryInventoryDrawer`, browse mode из шапки фабрики, selection mode из A7, фильтры `resources/fragments/slot_tokens/token_details/products`, карточка описания предмета, missing material hints и блокировка start/queue при нехватке баланса. Источник данных - существующие `FactoryStateSnapshot.balances` с `metadata.source=user_inventory`, warehouse balances исключены; новый inventory domain не создавался. Проверки: `npm run test -- __tests__/app/api/cabinet/factory __tests__/modules/factory` -> 44 passed; targeted frontend lint -> no errors, 7 `next/image` mock warnings; `npm run typecheck` -> passed.

- Проверен docs/daily/2026-05-25-safiu.md через scripts/daily_work_publish.py status --date 2026-05-25. Причина блокировки: unsafe_public_markers=secret_marker, Public digest line 31, совпадение по английскому слову token в технической формулировке; Internal log не публиковался.

- Для docs/daily/2026-05-25-safiu.md заменена публичная формулировка с английским token-маркером на русскую, затем выполнены status --date 2026-05-25 и daily-work-publish.ps1 -Date 2026-05-25. Confluence outcome=updated page_id=151715841.
- Закрыта Task 39: добавлены `FactoryUpgradeDialog`, `DemolitionDialog`, `SlotTokenDialog`, `SubscriptionDialog`, `FactoryBoosterPanel`, `InDevelopmentPanel`; `FactoryShell`, A5, A6 и A7 теперь открывают эти панели вместо disabled-заглушек. Slot-token assembly и material/booster flows используют существующие balances/resources (`game_dollar`, `slot_token`, `token_details` и др.), без нового inventory/currency domain. Проверки: `npm run test -- __tests__/app/api/cabinet/factory __tests__/modules/factory` -> 49 passed; targeted frontend lint -> no errors, 8 `next/image` mock warnings; `npm run typecheck` -> passed.
- Закрыта Task 40: добавлены `FactoryToastStack` и `hooks/useFactoryAnimations.ts`, i18n `factory.toast`, data-target hooks в shell/scene/A5/A6/A7/warehouse. Toast stack показывает 3 новейших уведомления и collapsed count; animation hook выводит события из backend notifications и state-маркеров, уважает `prefers-reduced-motion`, lazy-loads GSAP и логирует только one-time WARNING для отсутствующих targets. Проверки: `npm run test -- __tests__/app/api/cabinet/factory __tests__/modules/factory` -> 51 passed; targeted frontend lint -> no errors, 8 `next/image` mock warnings; `npm run typecheck` -> passed.
- Закрыта Task 41: проведён аудит factory frontend tests по checklist; добавлены bounded pan assumptions для `FactoryScene`, controlled error/retry state для `FactoryShell`, queue/subscription expiry toast assertions. Existing suite уже покрывает warehouse actions, craft lifecycle, no active-craft cancel, queued-slot remove/refund, onboarding completion, toast collapse, slot-token dialog и error normalization. Проверки: `npm run test -- __tests__/app/api/cabinet/factory __tests__/modules/factory` -> 54 passed; targeted frontend lint -> no errors, 8 `next/image` mock warnings; `npm run typecheck` -> passed.
- Закрыта Task 42: добавлен `useFactoryDialogFocusTrap` и подключён к `FactoryUpgradeDialog`, `DemolitionDialog`, `SlotTokenDialog`, `SubscriptionDialog`, `FactoryInventoryDrawer`; добавлены tests на Tab-cycle, Escape close, drawer keyboard close, `prefersFactoryReducedMotion` и responsive shell constraints. Проверки: `npm run test -- __tests__/app/api/cabinet/factory __tests__/modules/factory` -> 58 passed; targeted frontend lint -> no errors, 8 существующих `next/image` mock warnings; `npm run typecheck` -> passed. Browser viewport smoke не выполнялся по прямой инструкции пользователя; временный dev-сервер на 3100 остановлен, порт подтверждён закрытым.
- Закрыта Task 43: выполнен финальный frontend verification gate. Проверки: `npm run lint` -> passed с 65 существующими warning по широкому фронтенду; `npm run typecheck` -> passed; `npm run test -- __tests__/app/api/cabinet/factory __tests__/modules/factory` -> 58 passed; `npm run build` -> passed, factory routes/BFF endpoints emitted как dynamic routes.
- Закрыта Task 44: добавлен smoke evidence document `docs/tasks/fabric/factory-web-integration-smoke.md` и frontend regression на `rollout_enabled=false` в `FactoryShell.test.tsx`. Browser/Playwright smoke не выполнялся по прямой инструкции пользователя. Проверки: frontend smoke subset -> 102 passed; backend smoke subset -> 58 passed, 193 existing dependency/deprecation warnings; targeted frontend lint -> no errors, 10 existing `next/image` mock warnings; `npm run typecheck` -> passed.
- Закрыта Task 45: добавлен `docs/features/factory.md` с architecture/API/rollout/cache/catalog/payment/resources/impulse/assets/Figma/mobile/support coverage и ссылка в `docs/README.md`. Проверка: `powershell -ExecutionPolicy Bypass -File .\scripts\docs-health.ps1` -> OK, 73 markdown files, 114 local links, 0 warnings, 0 errors.
- Закрыта Task 46: выполнены targeted GBrain sync для `diaverse-aif`, `diaweb-code` и `diaverse-docs` после factory code/docs updates. Все sync-команды завершились успешно, без embeddings и без capture разговоров; остались только существующие content-sanity warnings на большие документы.
- Закрыта Task 47: финальный аудит без браузера по прямой инструкции пользователя. Удалены временные `diaweb/factory-dev-3100.*.log`, порт 3100 не занят. Backend: `pytest app/factory/tests -q` -> 108 passed; `pytest tests/test_cabinet_payment_sessions.py tests/test_alembic_graph.py -q` -> 15 passed; factory/payment/alembic ruff target -> passed; `alembic heads` -> `factory_web_state_20260525 (head)`; offline SQL upgrade -> compiled. Frontend: `npm run typecheck` -> passed; `npm run test -- __tests__/app/api/cabinet/factory __tests__/modules/factory` -> 59 passed. Residual: broader `pytest tests/test_cabinet_payment_sessions.py tests/test_alembic_graph.py tests/test_merge_account_coverage.py -q` failed in `test_merge_account_coverage.py` on unclassified non-factory `cab_crypton_*` and `club_*` tables; not hidden in factory work.
- Смержено и запушено без ручного deploy: `diaweb` `feature/factory-web-engine` -> `dev`, merge commit `a33c8af`, feature commit `c465621`; `diaverseapi` `feature/factory-web-engine` -> `dev`, merge commit `cbf1d1b3`, feature commit `b814f268`. Итоговый статус обоих product repos: `dev...origin/dev`, clean.
- На dev-сервере включены флаги `FACTORY_WEB_ENABLED=true` в `/home/config/.env` и `NEXT_PUBLIC_FACTORY_WEB_ENABLED=true` в `/home/diaweb/frontend/.env.production`; backend services `api`, `worker`, `scheduler` перезапущены, `diaweb-dev` пересобран и поднят через `docker-compose.dev-traefik.yml`. Проверки: `diaweb-dev` healthy; backend containers up; `https://dev.diaverse.app/api/health` -> OK; `https://dev.diaverse.app/ru/factory` без cookies -> `307` на login; `https://dev.diaverse.app/api/cabinet/factory/state` без cookies -> `401 Missing cabinet access cookie`, то есть rollout уже не блокирует.
- После пользовательского traceback найден первичный SQL-блокер: `subscriptionfeature` enum не содержал factory subscription keys, из-за чего `FactorySubscriptionResolver` ронял транзакцию, а следующий запрос баланса давал вторичный `InFailedSQLTransactionError`. Добавлены и запушены в `diaverseapi/dev` миграции `factory_subscription_features_20260526.py`, `merge_factory_feedback_heads_20260526.py`, `factory_subscription_feature_keys_20260526.py`; текущий backend head `ba1cc000`. Проверки: локально `alembic heads` -> `factory_sub_feature_keys_20260526`; `pytest tests/test_alembic_graph.py app/factory/tests/test_subscriptions.py app/factory/tests/test_state_service.py -q` -> 20 passed; ruff по миграциям/sentinel -> passed. Dev DB: `alembic_version=factory_sub_feature_keys_20260526`; direct `FactoryStateService.get_state` для пользователя из traceback -> `rollout_enabled=True`, `balances=17`, `buildings=12`; публичный `/ru/factory` без cookies -> `307` на login, BFF `/api/cabinet/factory/state` без cookies -> `401`, без factory SQL traceback.
- По запросу пользователя переработан только главный экран `diaweb` factory map. Загруженные PNG/AVIF ассеты конвертированы в WebP с latin/kebab-case именами и подключены через `frontend/modules/factory/assetManifest.ts`: `maps/level-1.webp`, `buildings/*-workshop.webp`, `buildings/warehouse.webp`. `FactoryShell` для `view.kind === "map"` теперь рендерит fullscreen/mobile map вместо кабинетных tabs/lists/balances; `FactoryScene` использует портретную карту и постоянные `BuildingInfoBubble`. Коммит `df599a1 feat(factory): make main screen map first` запушен в `diaweb origin/dev`. Проверки до запрета на browser smoke: `npm run test -- __tests__/modules/factory/FactoryScene.test.tsx __tests__/modules/factory/FactoryShell.test.tsx` -> 14 passed; targeted lint -> no errors, 4 existing `next/image` mock warnings; `npm run typecheck` -> passed. Browser/dev-server проверка не выполнялась по прямой инструкции "не проверяй"; случайно стартованный port 3100 остановлен, dev logs удалены. Оригинальные русскоязычные исходники оставлены untracked в `diaweb/frontend/public/factory`.
- Точечный follow-up по главному экрану фабрики: `CabinetLayout` для `/ru/factory` переведён в fullscreen shell без `CabinetTopbar`, footer и bottom nav; `FactoryScene` лишён pointer pan и больше не сдвигает canvas; `FactoryShell` убрал `min-h-[720px]`; `FactoryHotspotLayer` подставляет русские map labels/status для `lang=ru`. Коммит `cae4c60 fix(factory): lock main map fullscreen` запушен в `diaweb origin/dev`. Проверки: targeted factory/cabinet tests -> 27 passed; `npm run typecheck` -> passed; targeted lint -> no errors, 2 existing `next/image` mock warnings. Browser/dev-server проверка не запускалась. GBrain обновлён по `diaweb-code` и `diaverse-aif`.
- Исправлена избыточная правка layout: для `/ru/factory` `CabinetTopbar` остаётся скрытым, но `BottomNav` и `CabinetFooter` снова рендерятся; factory map shell переведён на `h-full` внутри доступной области layout. Коммит `a32a211 fix(factory): restore bottom navigation` запушен в `diaweb origin/dev`. Проверки: targeted `FactoryShell` + `CabinetLayout` tests -> 22 passed; `npm run typecheck` -> passed; targeted lint -> no errors, 1 existing `next/image` mock warning. Browser/dev-server проверка не запускалась. GBrain обновлён по `diaweb-code` и `diaverse-aif`.
- По A2-блоку главного экрана фабрики заменён `BuildingInfoBubble`: добавлены HUD-окно, кликабельный переход в цех, ready-pulse, upgrade-spinner, progress bar, активные линии, локализация resource labels и временный `Собрать` -> `/{lang}/factory/warehouse`. Логика состояния собрана в `FactoryHotspotLayer` на базе `FactoryStateSnapshot.jobs`, compartment cooldown/repair и building metadata timers/progress без фейковых значений. Коммит `9464f8f fix(factory): align map info bubbles with brief` запушен в `diaweb origin/dev`. Проверки: `pnpm exec vitest run __tests__/modules/factory/FactoryScene.test.tsx` -> 6 passed; `pnpm exec vitest run __tests__/modules/factory/FactoryShell.test.tsx` -> 9 passed; `pnpm typecheck` -> passed; targeted eslint -> no errors, 1 existing `next/image` mock warning. GBrain обновлён по `diaweb-code` и `diaverse-docs`.
- По A3-экрану склада в `diaweb` перестроен `FactoryWarehouseScreen`: убран общий empty placeholder, обе секции склада рендерятся всегда, добавлены локальная шапка `Назад / Склад фабрики / ?`, кнопка инвентаря, help-панель, крупный WebP warehouse asset, timer notice внутри `На складе`, состояние `≤2 часа` через `Поторопись`, stopped-текст и A3-формулировки кнопок `Отправить в хранилище` / `Отправить в инвентарь`. `FactoryShell` больше не добавляет общий factory header/tabs/balances на warehouse route; `CabinetLayout` скрывает `CabinetTopbar` и даёт fullscreen shell для `/factory/warehouse`. Коммит `f90e824 fix(factory): rebuild warehouse screen from brief` запушен в `diaweb origin/dev`. Проверки: `pnpm exec vitest run __tests__/modules/factory/FactoryWarehouseScreen.test.tsx` -> 3 passed; `FactoryShell.test.tsx` -> 9 passed; `CabinetLayout.test.tsx` -> 13 passed; `pnpm typecheck` -> passed; targeted eslint -> no errors, 2 existing `next/image` mock warnings.
- Исправлен layout-блокер карты фабрики: `CabinetRouteTransition` получил явную `width: 100%`, fullscreen `main` теперь прокидывает `w-full`/`flex-1` в route wrappers, factory map shell убран из `max-w-[430px]`, а `CabinetFooter` скрыт на factory fullscreen routes, оставляя только `BottomNav`. Проверки: `pnpm exec vitest run __tests__/modules/cabinet/CabinetLayout.test.tsx` -> 13 passed; `FactoryShell.test.tsx` -> 9 passed; `FactoryScene.test.tsx` -> 6 passed; `pnpm typecheck` -> passed; targeted eslint -> no errors, только существующие `next/image` mock warnings и CSS ignored warnings.
- По A4 в `diaweb` выделен factory-level вариант `FactoryUpgradeDialog`: заголовок теперь `Переход на уровень {level} фабрики`, добавлены превью следующего уровня/Цеха жизненной силы, крупная цена `$...`, кликабельная альтернатива кирпичами, список требований из catalog/state с выполненными и невыполненными пунктами, ссылка `Подробнее о требованиях`, закрытие по overlay и CTA `Купить и перейти`. `FactoryShell` подключил `useFactoryCatalog` для сборки требований перехода из `requirements_for_next_level`. Проверки: `pnpm exec vitest run __tests__/modules/factory/FactoryDialogs.test.tsx __tests__/modules/factory/FactoryShell.test.tsx __tests__/modules/factory/FactoryScene.test.tsx` -> 23 passed; `pnpm typecheck` -> passed; targeted eslint -> no errors, 3 existing `next/image` mock warnings.
