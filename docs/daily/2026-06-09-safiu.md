# Daily Work - 2026-06-09 - safiu

## Public digest

- Восстановлен повторный показ Crypton для пользователей: активные кулдауны сброшены, пользователи снова получают доступное окно оффера.
- Улучшена Crypton-модалка: клик по любой области карточки Crypton теперь открывает оффер, крестик по-прежнему только закрывает окно.
- Сделан заметнее текст в Crypton-модалке: ключевые фразы с предложением цены теперь крупнее и контрастнее.

## Internal log

- Выполнен продовый reset состояния Crypton через backend DB-доступ: создан backup затронутых таблиц, отменена 1 pending-заявка, закрыты старые active windows, очищены cooldown_until и созданы 289 новых available окон с временным cutoff bypass.
- Проверка после reset: 289 available/unclaimed окон, 0 активных кулдаунов, 0 активных Crypton-заявок; sample `CabCryptonService.get_state` вернул `popup_available=True`.
- В `diaweb` изменен `CryptonOfferModal`: добавлен прозрачный click-target по всей карточке, навигация переведена на `router.push`, CTA стала кнопкой с тем же handler, крестик не вызывает переход. Прогнаны Crypton frontend tests и lint; dev/master запушены, prod frontend пересобран и проверен по health и bundle marker.
- В `diaweb` усилен CSS `cryptonOfferModal`: `.typewriter` увеличен до `clamp(22px, 3vw, 34px)` с контрастной подложкой, увеличены eyebrow/cooldown/CTA и mobile overrides. Прогнаны Crypton frontend tests и lint; dev/master запушены, prod frontend пересобран и проверен по health и bundle marker.
