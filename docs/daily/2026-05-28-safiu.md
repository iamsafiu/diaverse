# Daily Work - 2026-05-28 [safiu]

## Public digest






<!-- Только безопасные публичные заметки на русском. Этот раздел можно публиковать в Confluence и использовать для генерации постов. -->

- Опубликован Daily Work за 2026-05-27 в Confluence: публичный digest перенесён на страницу дня, внутренний технический лог остался локальным.
- Уточнено поведение рекламных окон в кабинете: на экране фабрики и внутри страниц Crypton не показываются всплывающие предложения магазина и Crypton, а появление Crypton блокирует рекламное окно магазина до конца текущей браузерной сессии.
- Исправлено поведение Crypton: истёкшие предложения больше не остаются на странице офферов с нулевым таймером, а в каталоге заявки можно указывать большее количество и цену ниже рекомендованной без автоподстановки.
- На карте фабрики упрощены плашки над зданиями и скорректировано расположение цехов и центрального здания.
- Начислены административные тестовые питомцы для проверки седьмой эволюции и легендарных мутаций.
- Начислена дополнительная административная пачка тестовых питомцев для проверки седьмой эволюции у другого пользователя.

- Добавлен dev-режим ускорения фабрики: постройка, крафт и кулдауны могут ограничиваться одной минутой, а добыча ресурсов ускоряется отдельным множителем для тестового окружения.

- В dev-ветку backend отправлен релиз с правками Crypton и dev-ускорением фабрики для тестирования.
- Подготовлен быстрый план доработки интерфейса фабрики: упрощение инвентаря, корректные иконки ресурсов, компактная строка балансов и более наглядный прогресс на плашках карты.
- В инвентаре фабрики скрыты фрагменты и питомцы из обычного просмотра, при этом выбор материалов для рецептов сохранён.
- На карте фабрики под заголовком добавлена компактная строка балансов ресурсов, а игровые доллары теперь используют отдельную долларовую иконку вместо XDV.
- Плашки зданий на карте фабрики стали короче: убраны длинные подписи про уровень, добычу и подсказку постройки.
- Прогресс стройки, улучшения и производства на карте фабрики перенесён из внутренней полосы в заметную обводку плашки.
- Доработки интерфейса фабрики проверены целевыми тестами и линтингом.
- Для импульсов, токенов слота и деталей токена слота подключены новые WebP-иконки.
- Карта фабрики больше не растягивается, цеха выровнены по ячейкам, а плашки показывают живой прогресс и кнопку сбора прямо на карте.
- Добыча ресурсов фабрики теперь визуально обновляется без перезагрузки: таймер, обводка прогресса и накопленное количество пересчитываются на ходу.
- Backend-ветка `dev` фабрики и связанных правок слита в `main` для релизного продвижения.
- Карта фабрики снова занимает всю доступную область, плашки зданий стали компактнее и показывают только название с таймером, а сбор ресурсов на карте появляется только от одной целой единицы.

- На dev подготовлены тестовые балансы для группы QA, чтобы пройти и проверить фабрику первого уровня.

- Приведён в порядок рабочий документ по рейдам: механика «Путь к приключениям» теперь оформлена как читаемая спецификация с таблицами, правилами расчёта, ловушками, наградами и вопросами для реализации.
- Исправлено окно прокачки отсека фабрики: при прокрутке длинного диалога пользователь больше не возвращается самопроизвольно наверх.
- В документ по рейдам добавлена понятная мобильная структура интерфейса: рейдовая карта, раскрытая локация, выбор пэта, ловушки и экран итогов.
- Добавлен отдельный игровой гид по рейдам: как игрок проходит рейдовый цикл, какие механики участвуют и какие вопросы нужно уточнить перед реализацией.
- В рейдовом ТЗ закрыты спорные правила v1: DCR и игровые доллары не участвуют в механике рейдов, подписочный рейд длится 12 часов, а стрики, скидка Оазиса, токен-слоты и шкатулки получили конкретные правила.
- Подготовлен полный multi-repo план реализации рейдов v1: backend-домен, API, платежи, ловушки, награды, мобильный интерфейс и проверки разложены по этапам.
- Усилен план реализации рейдов: добавлены проверки платежного домена, безопасности аккаунта, блокировок пэтов между механиками и стабильных таймеров интерфейса.
- Начата реализация рейдов v1: добавлен backend-каталог локаций, режимов доступа, цен, слотов, наград, шкатулок, ловушек и особого лута.
- Для рейдов добавлен слой чистых расчётов: цены XDV/USDT, слот-лимиты, стрики, скидка Оазиса, награды, шкатулки, ловушки, выкуп и спасение теперь описаны кодом и покрыты тестами.
- Добавлены backend-шлюзы рейдов к существующим балансам, ресурсам, подпискам и питомцам: рейды используют XDV из BotUser, ресурсы из UserResource, XP конкретного пэта и учитывают pack/rent/raid блокировки.
- Внешние действия с питомцами защищены от конфликтов с рейдами: нельзя отправить рейдового пэта в стаю, аренду, активный слот или слияние, пока он занят активным рейдом, ловушкой или спасением.
- Собран backend state service для рейдов: экран получает профиль, HUD-балансы, локации, слоты, доступных пэтов, активные/готовые рейды, ловушки и ближайшее время обновления.
- Реализован старт рейда на backend: команда проверяет idempotency, режим, локацию, пэтов, слот-лимит, подписку, списывает XDV или создаёт USDT payment order, сохраняет run/participants и обновляет стрик.
- Реализовано получение наград за завершённый рейд: claim начисляет XP и ресурсы один раз, сохраняет результат бросков, поддерживает ловушки после награды и защищён от повторного начисления.
- Добавлена backend-логика ловушек рейдов: выкуп списывает XDV и освобождает пэта, спасатель проходит 50% проверку, а истёкшая ловушка переводит состояние в lost без физического удаления питомца.
- Реализована разблокировка постоянных XDV-слотов за Токен слота: сервис тратит ресурс, соблюдает максимум +5 и возвращает обновлённое состояние рейдов.
- USDT-режим рейдов подключён к общей оплате кабинета: создаётся checkout-сессия домена raids, paid-финализация запускает рейд, а гостевая оплата для рейдов остаётся отключённой.
- В плане рейдов закреплена неизменяемая композиция интерфейса: мобильный 9:16 экран с большой центральной картой, компактным HUD без DCR, карточками локаций внутри карты и нижней шторкой выбора пэта.
- Добавлена защита рейдов при удалении и объединении аккаунтов: pending-платежи блокируют опасные операции, а рейдовый прогресс переносится или конфликтует по правилам factory.
- Добавлены уведомления рейдов: завершение рейда, ловушки, истечение ловушки, результат спасения, разблокировка слот-токена и успешная USDT-оплата теперь возвращают UI-подсказки и безопасно пишутся в уведомления кабинета.
- Подключены backend API рейдов в кабинете: появились маршруты каталога, состояния, старта рейда, сбора награды, выкупа, спасения и разблокировки слот-токена.
- Добавлен проверяемый backend flow-suite рейдов без базы данных: реальные сервисы прогоняют старт, завершение, сбор награды, подписочный режим, сброс стрика, USDT checkout, слот-токен, выкуп и спасение через in-memory окружение.
- Backend-гейт рейдов пройден для тестов без базы, линтинга, Alembic head, Alembic graph и SQL compile миграции; DB-only проверки ожидают поднятого локального PostgreSQL.
- Для рейдов добавлен frontend BFF-слой кабинета: экран сможет получать каталог, состояние, стартовать рейды, собирать награды, выкупать и спасать пэтов через same-origin API.
- Добавлен frontend-клиент рейдов: типы контракта, загрузка состояния, команды рейда, idempotency и локальные таймеры подготовлены для будущего мобильного экрана без секундных сетевых запросов.

## Internal log






<!-- Локальные технические заметки на русском. Этот раздел нельзя публиковать или отправлять в генерацию постов. -->

- Выполнен preflight status для docs/daily/2026-05-27-safiu.md: public_chars=6182, unsafe_public_markers=[]; затем запущен scripts/daily-work-publish.ps1 -Date 2026-05-27, outcome=created, page_id=153944065.
- В `diaweb` обновлены route guards для `CryptonOfferGate` и `useShopPromoOffer`: `/[lang]/factory`, вложенные factory-маршруты, `/[lang]/offers/crypton` и вложенные Crypton-маршруты исключены из показа модалок. При фактическом открытии Crypton теперь выставляется существующий sessionStorage guard магазина `diaverse:shop-promo-offer:shown-session`, чтобы `ShopPromoOfferGate` не появлялся в этой браузерной сессии. Добавлены проверки в `CryptonOfferGate.test.tsx` и `useShopPromoOffer.test.tsx`; целевые Vitest-тесты и ESLint по изменённым файлам прошли.
- В `diaverseapi` обновлены `CabCryptonService._request_offer_card`, `_window_offer_card`, catalog policy и тесты: expired request/discovered window теперь возвращают `offer_card.visible=false`, для обычных Crypton catalog офферов снят прежний малый верхний лимит количества с сохранением guard для unique-предметов, а минимальный ценовой ratio снят. В `diaweb` обновлён `CryptonOfferBuilder`: убраны clamp по `maxUnits`, автозаполнение proposed price рекомендованной ценой и UI-блокировка цены ниже минимума. Проверки: `npm test -- --run __tests__/modules/crypton/CryptonOfferBuilder.test.tsx`, `npm run lint -- modules/crypton/components/CryptonOfferBuilder.tsx modules/crypton/constants.ts modules/crypton/index.ts __tests__/modules/crypton/CryptonOfferBuilder.test.tsx`, `.venv\Scripts\python.exe -m pytest tests/test_cabinet_crypton.py -q`, `.venv\Scripts\python.exe -m ruff check app\cabinet\offers\crypton\catalog_policy.py app\cabinet\offers\crypton\service.py tests\test_cabinet_crypton.py`.
- В `diaweb` обновлены `BuildingInfoBubble`, `FactoryHotspotLayer`, `factoryScene.module.css` и `assetManifest`: из плашек карты фабрики удалён image/icon блок, сетка плашки перестроена под текст и кнопку, hotspot-координаты склада и всех цехов сдвинуты на +2% по X и Y. Проверка: `npm run lint -- modules/factory/components/BuildingInfoBubble.tsx modules/factory/components/FactoryHotspotLayer.tsx modules/factory/components/factoryScene.module.css modules/factory/assetManifest.ts` прошла без ошибок; CSS был проигнорирован ESLint по текущей конфигурации.
- В production PostgreSQL через audit-маркер `manual_admin_evo7_legendary_grant_20260528` начислены 10 разных `legendary/default` петов с `evolution=7`, уровнями 71-80, `active_mutation_rarity=legendary` и `mutation_level=1..10`; добавлены default-скины и применён множитель статов 7-й эволюции. Post-check подтвердил 10 distinct pet types, уровни `{71..80}`, мутации `{1..10}`, `all_expected=true`, audit: 10 INSERT и 10 UPDATE.
- В production PostgreSQL через audit-маркер `manual_admin_evo7_legendary_grant_20260528_tg6585430610` начислены 10 разных `legendary/default` петов пользователю `tg_user_id=6585430610`: уровни 71-80, `evolution=7`, `active_mutation_rarity=legendary`, `mutation_level=1..10`; существующий Единорог не дублировался в новой пачке, вместо него использован Бобр. Добавлены default-скины и применён множитель статов 7-й эволюции. Post-check подтвердил 10 distinct pet types, уровни `{71..80}`, мутации `{1..10}`, `all_expected=true`, audit: 10 INSERT и 10 UPDATE.

- В diaverseapi добавлены env-настройки FACTORY_DEV_FAST_TIMERS_ENABLED, FACTORY_DEV_FAST_TIMER_SECONDS, FACTORY_DEV_RESOURCE_RATE_MULTIPLIER; создан app/factory/domain/dev_tuning.py, подключено ограничение таймеров в domain/timers.py и building_service.py, множитель добычи в warehouse_service.py. Проверки: pytest app/factory/tests/test_building_service.py app/factory/tests/test_crafting_service.py app/factory/tests/test_warehouse_service.py; ruff check по изменённым backend-файлам.

- diaverseapi/dev: pushed commits 657fb8f8 and b5fb7c15. Set dev factory env variable names FACTORY_DEV_FAST_TIMERS_ENABLED, FACTORY_DEV_FAST_TIMER_SECONDS, FACTORY_DEV_RESOURCE_RATE_MULTIPLIER. Ran dev DB clamp SQL for factory timers and warehouse accrual; all counters returned 0 matching rows. Validation: targeted factory and Crypton pytest suite passed, ruff check passed, GBrain diaverseapi-code sync completed.
- Создан новый fast-plan `.ai-factory/PLAN.md` для фабрики: скрыть фрагменты и петов из browse-инвентаря при сохранении material picker, добавить строку балансов `xdv/game_dollar/brick/impulse/slot_token`, исправить `game_dollar` icon source на сущность `game_balance_usd`/`dollar.webp`, минимализировать `FactoryHotspotLayer` bubble copy и заменить inner progress bar на clockwise border progress.
- В `diaweb` обновлён `FactoryInventoryDrawer`: browse-mode фильтрует shard/user_character/character pet rows, select-mode оставляет совместимые concrete balances для `shard_id` и `user_character_id`. Добавлены проверки в `FactoryInventoryDrawer.test.tsx`; `npm test -- FactoryInventoryDrawer` прошёл.
- В `diaweb` добавлен `FactoryMapResourceStrip` в `FactoryShell` для `xdv/game_dollar/brick/impulse/slot_token`; `assetManifest` больше не мапит `factory.icon.game_dollar` на XDV fallback, добавлен локальный `game-dollar.svg`. В `diaverseapi` для virtual asset `game_dollar` указан `dollar.webp`. Проверки: `npm test -- FactoryShell`, `.venv\Scripts\python.exe -m pytest app\factory\tests\test_award_resource_support.py -q`.
- В `diaweb` упрощены `FactoryHotspotLayer` и `FactoryScene`: удалён prop `resourceLabels` из карты, active/ruins/locked bubbles теперь показывают короткий status через `formatStatus`, без production copy и build hint. Проверка: `npm test -- FactoryScene`.
- В `diaweb` обновлены `BuildingInfoBubble` и `factoryScene.module.css`: `progressPercent` теперь задаёт CSS custom property для `conic-gradient` border progress, внутренний horizontal track удалён из render. Добавлен тест running production progress в `FactoryScene.test.tsx`; `npm test -- FactoryScene` прошёл.
- Финальная проверка фабрики: `npm test -- FactoryInventoryDrawer FactoryShell FactoryScene factory-api` прошёл (4 files, 33 tests), backend `pytest app\factory\tests\test_state_service.py app\factory\tests\test_inventory_gateway.py app\factory\tests\test_award_resource_support.py -q` прошёл (16 tests), `npm run lint` завершился без ошибок с существующими warning-ами. Запущен targeted GBrain sync для `diaweb-code` и `diaverseapi-code`.
- В `diaverseapi/static` переименованы новые файлы в `slot-token.webp` и `token-details.webp`, добавлен `impulse.webp`; `resource_assets.py` теперь принудительно отдаёт эти static icons для `impulse`, `slot_token`, `token_details`. В `diaweb` WebP скопированы в `frontend/public/factory/resources` и подключены в `assetManifest.ts` как fallback. Проверки: `npm test -- FactoryShell FactoryInventoryDrawer FactoryDialogs`, `npm run lint -- modules/factory/assetManifest.ts __tests__/modules/factory/FactoryShell.test.tsx`, `pytest app\factory\tests\test_award_resource_support.py app\factory\tests\test_state_service.py -q`, `ruff check app\factory\services\resource_assets.py app\factory\tests\test_award_resource_support.py`.
- В `diaweb` карта фабрики ограничена мобильной шириной, `FactoryScene` сохраняет aspect ratio фоновой карты через CSS-переменные, а `assetManifest` обновлён под реальные ячейки level-1 map. Добавлены `displayLabels.ts` и `warehouseLive.ts`: русские названия зданий/отсеков/назначений вынесены в общий helper, live-добыча warehouse считается по `last_accrual_at`, `stop_at`, cap 10 часов и ставкам каталога. `FactoryHotspotLayer` теперь показывает накопленную добычу, движущуюся border-обводку и прямой `transfer_to_storage`/`collect_craft` action из плашки карты; `FactoryWarehouseScreen` также отображает live pending amount. Проверки: `npm test -- --run __tests__/modules/factory/FactoryScene.test.tsx __tests__/modules/factory/FactoryShell.test.tsx __tests__/modules/factory/FactoryWarehouseScreen.test.tsx __tests__/modules/factory/FactoryResourceWorkshopScreen.test.tsx __tests__/modules/factory/FactoryProductionWorkshopScreen.test.tsx __tests__/modules/factory/FactoryCompartmentScreen.test.tsx __tests__/modules/factory/FactoryDialogs.test.tsx`, `npm run lint -- modules/factory __tests__/modules/factory` (без ошибок, только существующие warnings про mock img), `npm run typecheck`.
- В `diaverseapi` выполнен `dev -> main`: локальная `main` обновлена от `origin/main`, затем влит `origin/dev` без конфликтов и запушен merge-коммит `5e29482b`. После push рабочая ветка возвращена на `dev`.
- В `diaweb` снят `max-w-[430px]` с `FactoryMapView`, canvas карты переведён в cover-режим через container units без растяжения, `BuildingInfoBubble` и `FactoryHotspotLayer` упрощены до названия/таймера/кнопки. `warehouseLive.ts` добавил whole-quantity helpers с epsilon для float-границы, а `FactoryWarehouseScreen` и карта фильтруют сбор по целым единицам. В коммит включена заменённая пользователем `frontend/public/factory/maps/level-1.webp`. Проверки: `npm test -- --run __tests__/modules/factory/FactoryScene.test.tsx __tests__/modules/factory/FactoryShell.test.tsx __tests__/modules/factory/FactoryWarehouseScreen.test.tsx` (26 passed), `npm run lint -- modules/factory/components/FactoryHotspotLayer.tsx modules/factory/components/BuildingInfoBubble.tsx modules/factory/components/FactoryScene.tsx modules/factory/components/FactoryShell.tsx modules/factory/components/FactoryWarehouseScreen.tsx modules/factory/warehouseLive.ts __tests__/modules/factory/FactoryScene.test.tsx __tests__/modules/factory/FactoryShell.test.tsx __tests__/modules/factory/FactoryWarehouseScreen.test.tsx` (0 errors, 3 existing mock-img warnings), `npm run typecheck`.

- Factory lvl1 QA grant on dev DB: resolved 11/11 requested testers, no missing users. Applied idempotent minimum balances for impulse, brick, slot_token, token_details, synthesis_core, XDV, game dollars, character shards, rare EvoGen vouchers, and rare pet availability. Verification SELECT confirmed all target users meet or exceed the QA minimums.

- Обновлён docs/tasks/raids/raids-mechanics.md: добавлен metadata-блок, структура разделов, сводные таблицы, порядок расчётов XP/ресурсов/шкатулок, секции по локациям, ловушкам, новым ресурсам и рискам. Проверки: scripts/docs-health.ps1 прошёл без ошибок по рейдовому документу; targeted GBrain sync выполнен для source diaverse-docs.
- В `diaweb` исправлен `useFactoryDialogFocusTrap`: `onClose` вынесен в ref, установка DOM listener больше не зависит от inline callback родителя, а программный focus использует `preventScroll`. Добавлен регрессионный тест в `FactoryDialogs.test.tsx` на ререндер диалога прокачки отсека без сброса фокуса наверх. Проверки: `pnpm exec vitest run __tests__/modules/factory/FactoryDialogs.test.tsx`, `pnpm exec eslint modules/factory/hooks/useFactoryDialogFocusTrap.ts __tests__/modules/factory/FactoryDialogs.test.tsx` (0 errors, существующий warning про mock img), `pnpm typecheck`.
- В `docs/tasks/raids/raids-mechanics.md` добавлен раздел `UI/UX структура мобильного интерфейса` с ASCII-схемами экранов: главная карта, раскрытая локация, шторка выбора пэта, подтверждение отправки, слот рейда, ловушка, выбор спасателя и итоги. Зафиксировано, что в первом HUD вместо DCR показываются игровые доллары. Проверки: `scripts/docs-health.ps1` прошёл без ошибок, targeted GBrain sync выполнен для `diaverse-docs` с warning только по размеру документа.
- Создан `docs/tasks/raids/raids-gameplay-guide.md`: подробный игровой сценарий рейдов, связь экранов с механиками, локации, режимы доступа, стрики, XP, ресурсы, шкатулки, ловушки, спасение, состояния пэта, UX-правила и список расхождений. В `raids-mechanics.md` добавлена ссылка на новый гид. Проверки: `scripts/docs-health.ps1` прошёл без ошибок; targeted GBrain sync выполнен для `diaverse-docs`, warning только по размеру крупных документов.
- В `docs/tasks/raids/raids-mechanics.md` и `docs/tasks/raids/raids-gameplay-guide.md` расхождения переведены в принятые решения v1: DCR не используется, игровые доллары остаются только общим HUD-балансом, слот-лимит вынесен в грейд профиля, `Спеш 2x` стал автоматической длительностью 12 часов для XDV с подпиской, стрик сбрасывается при успешном старте другой локации, скидка Оазиса считается от завершения последнего рейда, Токен слота даёт постоянный +1 XDV-слот с максимумом +5, а шкатулки ограничены roll-моделью.
- Выполнен `$aif-plan full` для рейдов: обновлены `dev` и созданы ветки `feature/raids-mechanics` в `diaweb` и `diaverseapi`; `aibot` и `club10000-bot` не затрагиваются. Создан master-plan `.ai-factory/plans/feature-raids-mechanics.md` с 27 задачами, backend/frontend матрицей, logging requirements, тестами, Alembic SQL compile gate и commit plan.
- По итогам `$aif-improve` обновлён `.ai-factory/plans/feature-raids-mechanics.md`: добавлены Task 6A для cross-domain pet guards и Task 12A для account lifecycle/pending payment safety, расширены Task 12 payment registry/finalizer, Task 17 BFF route import coverage, Task 18 shared raid clock, Task 19 fullscreen cabinet shell integration, Task 23 focus/scroll stability и verification commands.
- `$aif-implement` Task 1: в `diaverseapi` добавлены `app/raids/catalog/schema.py`, `loader.py`, `validator.py`, `data/raids_catalog.v1.yaml` и `app/raids/tests/test_catalog.py`. Каталог кодирует 3 режима, 3 локации, slot table, XDV/USDT prices, streak caps, trap/chest/crit chances, special loot gating, pet abilities, Oasis discount и token slot cap. Проверки: `.venv\Scripts\python.exe -m pytest app\raids\tests\test_catalog.py -q` -> 10 passed; `.venv\Scripts\python.exe -m ruff check app\raids\catalog app\raids\tests\test_catalog.py` -> passed.
- `$aif-implement` Task 2: в `diaverseapi` добавлены raid v1 SQLModel tables рядом с legacy `RaidVoucher`/`UserRaidVoucher`: profiles, runs, participants, traps, rescue attempts, idempotency records и payment orders. Создана миграция `raid_mechanics_v1_20260528`, подключён import моделей в `migrations/env.py`, добавлены model tests и обновлён Alembic graph sentinel/current head. Проверки: `pytest tests\test_alembic_graph.py app\raids\tests\test_models.py app\raids\tests\test_catalog.py -q` -> 28 passed; `ruff check ...` -> passed; `alembic heads` -> `raid_mechanics_v1_20260528`; `alembic upgrade runtime_settings_20260528:raid_mechanics_v1_20260528 --sql` -> generated successfully.
- `$aif-implement` Task 3: добавлены `app/raids/schemas.py`, `exceptions.py`, `infrastructure/rollout.py`, backend flag `RAIDS_WEB_ENABLED` в settings/features и disabled-response pattern для будущего API. Проверки: `ruff check app\raids\schemas.py app\raids\exceptions.py app\raids\infrastructure\rollout.py app\core\features.py app\core\settings.py` -> passed; import smoke для rollout/schema -> ok; `pytest app\raids\tests\test_catalog.py app\raids\tests\test_models.py -q` -> 14 passed.
- `$aif-implement` Task 4: добавлен `app/raids/infrastructure/repositories.py` с profile/run/participant/trap/rescue/payment helpers, `FOR UPDATE` lock methods и idempotency acquire/replay/conflict/locked flow по factory-паттерну. Добавлены hash-тесты в `app/raids/tests/test_repositories.py`. Проверки: `pytest app\raids\tests\test_repositories.py app\raids\tests\test_models.py app\raids\tests\test_catalog.py -q` -> 17 passed; `ruff check app\raids\infrastructure\repositories.py app\raids\tests\test_repositories.py app\raids\schemas.py app\raids\exceptions.py app\raids\infrastructure\rollout.py` -> passed.
- `$aif-implement` Task 5: добавлены `app/raids/domain/pricing.py`, `slots.py`, `streaks.py`, `rewards.py`, `traps.py` и `app/raids/tests/test_domain_rules.py`. Реализованы формулы цен, длительности режимов, слот-лимитов, стриков, скидки Оазиса, XP/resources/chest/special loot rolls, trap/ransom/rescue с детерминированным RNG. Проверки: `pytest app\raids\tests\test_domain_rules.py -q` -> 8 passed; `ruff check app\raids\domain app\raids\tests\test_domain_rules.py` -> passed.
- `$aif-implement` Task 6: добавлены `app/raids/infrastructure/xdv_gateway.py`, `resource_gateway.py`, `subscription_resolver.py`, `pet_gateway.py` и `app/raids/tests/test_gateways.py`. XDV переиспользует factory/BotUser adapter, ресурсы кредитуются в `UserResource`, XP начисляется выбранному `UserCharacter`, subscription mode опирается на активные subscription features, pet gateway исключает deleted/pack/rent/active raid/trap/rescue pets и предоставляет shared usage map. Проверки: `pytest app\raids\tests\test_gateways.py -q` -> 5 passed; `ruff check ...gateway...` -> passed.
- `$aif-implement` Task 6A: добавлен `CharacterRaidLockRestriction`, `assert_no_raid_pet_locks`/`raise_if_raid_pet_usage_locked` в `characters/usecases/utils.py`; guard подключён в `set_user_character_active.py`, `rent_character.py`, `merge_user_characters.py`, `packs/usecases/update_pack.py`, `packs/usecases/fix_pack.py`. Добавлен `app/raids/tests/test_pet_action_guards.py`. Проверки: `pytest app\raids\tests\test_pet_action_guards.py app\raids\tests\test_gateways.py -q` -> 7 passed; `ruff check` по изменённым guard/usecase файлам -> passed.
- `$aif-implement` Task 7: добавлен `app/raids/services/state_service.py` и `app/raids/tests/test_state_service.py`. `RaidStateService` создаёт/обновляет профиль, строит `RaidStateResponse`, показывает game dollars как HUD-only, вычисляет локации/слоты/pets, применяет passive completion и trap expiry, сбрасывает скидку Оазиса по дедлайну и выставляет `next_refresh_at`. Проверки: `pytest app\raids\tests\test_state_service.py -q` -> 1 passed; `ruff check app\raids\services\state_service.py app\raids\tests\test_state_service.py` -> passed.
- `$aif-implement` Task 8: добавлен `app/raids/services/command_service.py` и `app/raids/tests/test_command_start.py`. `RaidCommandService.start_raid` реализует idempotency acquire/replay, duplicate-pet guard, subscription guard, pet availability, XDV slot limit, XDV debit, streak update, run/participant persistence и USDT `payment_required` order. Проверки: `pytest app\raids\tests\test_command_start.py -q` -> 3 passed; `ruff check app\raids\services\command_service.py app\raids\tests\test_command_start.py` -> passed.
- `$aif-implement` Task 9: добавлен `app/raids/services/settlement_service.py` и `app/raids/tests/test_settlement_service.py`. `RaidSettlementService` переводит ready runs в completed, claim-ит participants, начисляет XP напрямую пэту и ресурсы в `UserResource`, сохраняет reward/trap roll metadata, создаёт trap после награды и не делает duplicate credits при повторном claim. Проверки: `pytest app\raids\tests\test_settlement_service.py -q` -> 2 passed; `ruff check app\raids\services\settlement_service.py app\raids\tests\test_settlement_service.py` -> passed.
- `$aif-implement` Task 10: добавлен `app/raids/services/trap_service.py` и `app/raids/tests/test_trap_service.py`. Реализованы ransom, rescue attempt, failed-rescue rescuer trap, release, expiry -> lost; v1 не hard-delete-ит `UserCharacter`, а фиксирует loss в raid tables. Проверки: `pytest app\raids\tests\test_trap_service.py -q` -> 2 passed; `ruff check app\raids\services\trap_service.py app\raids\tests\test_trap_service.py` -> passed.
- `$aif-implement` Task 11: добавлен `app/raids/services/slot_token_service.py` и `app/raids/tests/test_slot_token_service.py`. `RaidSlotTokenService` списывает `ResourceType.slot_token`, увеличивает `RaidProfile.token_slots_unlocked`, ограничивает cap +5 и возвращает `RaidCommandResponse` со state или понятными errors. Проверки: `pytest app\raids\tests\test_slot_token_service.py -q` -> 2 passed; `ruff check app\raids\services\slot_token_service.py app\raids\tests\test_slot_token_service.py` -> passed.
- `$aif-implement` Task 12: добавлены `app/raids/services/payment_service.py`, `app/raids/payment_finalizer.py`, домен `raids` в generic cabinet payments registry/types/finalizers и тесты `app/raids/tests/test_payment_service.py` + расширение `tests/test_cabinet_payment_sessions.py`. USDT start теперь создаёт `RaidPaymentOrder` + `CabinetPaymentSession`, сохраняет pending run payload/metadata/provider refs, paid finalizer idempotently переводит order в run, а guest actor для raids отключён. Проверки: `pytest app\raids\tests\test_command_start.py app\raids\tests\test_payment_service.py tests\test_cabinet_payment_sessions.py -q` -> 13 passed; `ruff check` по изменённым payment/registry/test файлам -> passed.
- В `.ai-factory/plans/feature-raids-mechanics.md` добавлен `Raid UI Composition Guardrail`: frontend Tasks 20-25 обязаны следовать `docs/tasks/raids/raids-gameplay-guide.md`, сохранять центральную карту и не показывать DCR.
- `$aif-implement` Task 12A: в `diaverseapi/app/security/usecases.py` добавлены `_count_raid_state_rows`, pending raid payment guard для delete/merge и `_merge_raid_state` с переносом `RaidProfile`, runs, participants, traps, rescue attempts, idempotency records и payment orders; добавлен `MergeRaidStateConflict`, расширен merge coverage sentinel и DB lifecycle tests. Проверки: `ruff check app/security/usecases.py app/security/exceptions.py app/raids/tests/test_account_lifecycle.py tests/test_merge_account_coverage.py tests/test_user_deletion.py` -> passed; `pytest tests/test_merge_account_coverage.py -q` -> 2 passed. DB-targeted tests `pytest app\raids\tests\test_account_lifecycle.py tests\test_user_deletion.py::test_deletion_blocks_pending_raid_payment -q` не дошли до логики из-за локального `ConnectionRefusedError` на PostgreSQL `localhost:5432`.
- `$aif-implement` Task 13: добавлен `app/raids/infrastructure/notifications.py` по factory-паттерну: stable idempotency keys, `RaidNotificationHintRead`, persistent `CabUserNotificationService` integration и non-fatal failure handling. Подключено в `RaidStateService`, `RaidSettlementService`, `RaidTrapService`, `RaidSlotTokenService`, `RaidPaymentService`. Проверки: `pytest app\raids\tests\test_notifications.py app\raids\tests\test_settlement_service.py app\raids\tests\test_trap_service.py app\raids\tests\test_slot_token_service.py app\raids\tests\test_payment_service.py -q` -> 14 passed; `ruff check` по notification/services/tests -> passed.
- `$aif-implement` Task 14: добавлены `app/raids/api.py`, `app/raids/dependencies.py`, регистрация `raids_router` в `app/routers/v1/endpoints.py` под `/v1/cabinet/raids` и `app/raids/tests/test_api.py`. API использует `NoStoreJSONRoute`, `get_current_user`, rollout disabled response и 503 для catalog load errors; command routes логируют requested/blocked. Проверки: `pytest app\raids\tests\test_api.py -q` -> 8 passed; `ruff check app\raids\api.py app\raids\dependencies.py app\routers\v1\endpoints.py app\raids\tests\test_api.py` -> passed.
- `$aif-implement` Task 15: добавлен `app/raids/tests/test_raids_flow.py` с service-level flow suite на реальных raid services и in-memory fake repository/gateways, так как локальный PostgreSQL/Docker недоступны. Покрыто: базовый XDV Wastelands start->claim, subscription 12h, streak reset при смене локации, USDT payment_required, slot token unlock, ransom и failed rescue. Проверки: `pytest app\raids\tests\test_raids_flow.py -q` -> 6 passed; `ruff check app\raids\tests\test_raids_flow.py` -> passed.
- `$aif-implement` Task 16: backend quality gate. Проверки: `pytest app\raids\tests -q --ignore=app\raids\tests\test_account_lifecycle.py` -> 64 passed; `ruff check` по raids/router/settings/security/test files -> passed; `alembic heads` -> `raid_mechanics_v1_20260528 (head)`; `alembic upgrade runtime_settings_20260528:raid_mechanics_v1_20260528 --sql` -> generated successfully; `pytest tests\test_merge_account_coverage.py tests\test_alembic_graph.py -q` -> 16 passed. DB-only tests `test_deletion_blocks_pending_raid_payment` и `test_account_lifecycle.py` не стартуют из-за `ConnectionRefusedError` на PostgreSQL `localhost:5432`.
- `$aif-implement` Task 17: в `diaweb` добавлены same-origin BFF routes `app/api/cabinet/raids/*` для catalog/state/runs/start/runs/{runId}/claim/traps/{trapId}/ransom/traps/{trapId}/rescue/slot-tokens/unlock и общий `_utils.ts` по factory proxy pattern. Маршруты форвардят cookie, accept-language, timezone, Telegram platform и request id, сохраняют no-store, не логируют payload/pet lists, а dynamic params кодируются через `encodeURIComponent`. Проверки: `npm test -- --run __tests__/app/api/cabinet/raids` -> 2 files, 7 tests; `npm run lint -- app/api/cabinet/raids __tests__/app/api/cabinet/raids` -> passed.
- `$aif-implement` Task 18: создан `diaweb/frontend/modules/raids` с `types.ts`, `api.ts`, `constants.ts`, `time.ts`, hooks `useRaidState`, `useRaidMutations`, `useRaidClock` и barrel `index.ts`. API клиент мапит camelCase input в backend snake_case (`mode_key`, `user_character_ids`, `rescuer_user_character_id`), добавляет raid idempotency keys, нормализует ошибки, ставит clock metadata на state/catalog/command result и логирует только route/status/counts без pet payloads. `useRaidState` строит query keys и refetchInterval от `next_refresh_at`, а `useRaidClock` оставляет секундные countdown/progress тики локальными. Проверки: `npm test -- --run __tests__/modules/raids` -> 2 files, 11 tests; `npm run lint -- modules/raids __tests__/modules/raids` -> passed; `npm run typecheck` -> passed.
