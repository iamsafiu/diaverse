# Daily Work - 2026-06-07 - safiu

## Public digest

- Подготовлен план реализации выбора эволюции питомца в оффере Криптона: пользователь сможет выбрать нужную эволюцию и указать желаемую цену без автоматической рекомендованной цены для питомцев.
- Улучшен план реализации выбора эволюции питомца в оффере Криптона: добавлены проверки передачи эволюции через backend, frontend, staff UI и Telegram-уведомления.
- Backend-часть оффера Криптона подготовлена к выбору эволюции питомца: заявка сохраняет выбранную эволюцию, питомец будет выдаваться с ней, а уведомления и служебные payloads получают это значение.
- Создан отдельный простой Telegram-бот-переходник для перевода пользователей старого бота в веб-версию Diaverse.
- В боте-переходнике подключены финальный текст приветствия, ссылки на web/App Store/Google Play и загруженное изображение.
- Текст приветствия бота-переходника переведен на источник `text.txt` с эмодзи и плейсхолдерами ссылок.
- Простой бот-переходник Diaverse развернут на отдельном серверном контуре и запущен.
- В боте-переходнике отключено Mini App меню; переход на сайт остается обычной ссылкой в сообщении.
- Исправлены ключевые неудобства фабрики: повторные прокачки без выхода в меню, выбор оплаты уровня фабрики, закрытие модалки на маленьком экране, кликабельные условия перехода и более точные статусы склада/раннего доступа.
- Новые рейд-иммунитеты и рейд-ваучеры подключены к staff-каталогам адвент-календаря, поддержки и магазина, чтобы сотрудники могли выбирать и выдавать их из общих каталогов.
- Все накопленные изменения workspace и дочерних репозиториев отправлены в ветку `dev`.
- Ветки `dev` для `diaweb` и `diaverseapi` влиты в production-ветки и отправлены на remote.
- В оффере Криптона добавлен выбор эволюции питомца: пользователь выбирает нужную эволюцию, рекомендованная цена для питомцев не показывается, staff видит выбор, а после оплаты питомец выдается с выбранной эволюцией.
- Исправлена ошибка сборки `diaweb` в оффере Криптона: выбор эволюции питомца теперь проходит React Compiler lint без блокирующих ошибок.

## Internal log

- Обновлен fast-план `.ai-factory/PLAN.md` для задачи Crypton pet evolution selection. Скоуп: `diaweb` для модалки и staff UI, `diaverseapi` для контракта, валидации, fulfillment и notification payloads, `diaverse-auth-bot` для отображения эволюции в Telegram caption.
- После `aif-improve` план расширен с 8 до 11 task-list задач: добавлены backend error mapping для invalid evolution, propagation в ops/payment payloads, frontend BFF forwarding/tests, явные зависимости между backend payload и auth-bot caption, а также обновлен full verification plan.
- Выполнены backend tasks 1-4 по Crypton pet evolution: добавлен `CryptonEvolutionValidationError`, schema fields `selected_evolution/selected_evolution_label/is_character_offer/evolution_options`, service helper выбора pet evolution, manual pricing mode для character offers, snapshot metadata, fulfillment `options_json.evolution`, ops/payment/telegram payload propagation. Быстрая проверка: `python -m compileall app\cabinet\offers\crypton`.
- В папке `diaverse-web-redirect-bot` добавлен standalone Python/aiogram бот: `/start` отправляет фото с caption и кнопками сайта/поддержки, Telegram menu button открывает веб-версию; добавлены `.env.example`, Dockerfile, README и базовые config-тесты.
- `diaverse-bot.jpg` перенесен в `diaverse-web-redirect-bot/assets/diaverse-bot.jpg`; `.env.example` обновлен под шаблон `{WEB_LINK}`, `{APPSTORE_LINK}`, `{GOOGLEPLAY_LINK}`, `{SUPPORT_LINK}`. Проверки: `python -m compileall app tests`, `python -m pytest tests -q`.
- `diaverse-web-redirect-bot` теперь поддерживает `START_MESSAGE_TEXT_FILE=text.txt`; Dockerfile копирует `text.txt`, тесты покрывают чтение UTF-8 шаблона с эмодзи. Проверки: `python -m compileall app tests`, `python -m pytest tests -q`.
- На сервере проверено свободное место, создана отдельная папка `/srv/diaverse-web-redirect-bot`, собран Docker-образ и запущен контейнер `diaverse-web-redirect-bot` с `restart unless-stopped`; polling стартовал успешно.
- В `diaverse-web-redirect-bot` удалено использование `MenuButtonWebApp`; при старте бот сбрасывает Telegram menu button к дефолту. Серверный контейнер пересобран и перезапущен, лог подтверждает reset menu button и запуск polling.
- В `diaweb` обновлены `FactoryUpgradeDialog`, `FactoryShell`, toast stack, production/compartment screens и i18n-словари; в `diaverseapi` обновлен `state_service.py`, чтобы `upgrade_compartment` не отдавался для max-level и inactive production part. Проверки: `npm run test -- __tests__/modules/factory`, `npm run typecheck`, `npm run lint -- ...factory touched files...`, `.venv\Scripts\python.exe -m pytest app\factory\tests -q`, `.venv\Scripts\python.exe -m ruff check app\factory\services\state_service.py app\factory\tests\test_state_service.py`.
- В `diaverseapi` добавлены `ShopSourceType.raid_immunity/raid_voucher`, shop policy для секции vouchers, Advent reward type/grant path для `raid_voucher` через `RaidVoucherGrantService`, runtime/admin reference mapping для raid voucher rewards. В `diaweb` расширены shared staff catalog labels/allowlist, support/advent/shop admin типы и picker tests. Проверки: backend `compileall` по затронутым файлам, `pytest tests\test_raid_voucher_items.py tests\test_cabinet_shop_admin.py::test_service_list_sections_uses_section_visibility_settings -q`, backend Ruff по затронутым файлам, frontend `npm run test -- --run __tests__/modules/staff-item-catalog/StaffItemCatalogPicker.test.tsx __tests__/modules/staff-advent/AdventRewardPicker.test.tsx __tests__/modules/staff-shop/ItemCatalogPicker.test.tsx`. GBrain sync отложен, потому что параллельные агенты держат незавершенные изменения в `diaverseapi` и `diaweb`.
- Выполнен полный commit/push в `dev`: root workspace, `diaverseapi`, `diaweb`, `diaverse-auth-bot`. Для root workspace и `diaverse-auth-bot` ветка `dev` была создана, так как ранее на remote ее не было; `aibot` и `club10000-bot` были clean и без новых коммитов.
- Выполнен merge `origin/dev` в production-ветки: `diaweb/master` -> `32e661e Merge dev into master`, `diaverseapi/main` -> `8a46650d Merge dev into main`; оба merge прошли без конфликтов, `git diff --check HEAD~1..HEAD` прошел в обоих репозиториях, production branches запушены.
- Выполнены frontend/auth-bot/backend verification tasks по Crypton pet evolution: в `diaweb` расширены Crypton types/API/BFF, модалка выбора оффера, staff request panel, i18n и тесты; в `diaverse-auth-bot` caption Crypton уведомления показывает `selected_evolution_label`; в `diaverseapi` добавлены targeted tests для submit validation, snapshots, payload propagation и fulfillment line options. Проверки: backend `compileall app\cabinet\offers\crypton`, `pytest tests\test_cabinet_crypton.py -q`; frontend `npm run typecheck`, `npm run test -- --run __tests__/modules/crypton/CryptonOfferBuilder.test.tsx __tests__/app/api/cabinet/offers/crypton/route.test.ts __tests__/modules/staff-shop/CryptonRequestsPanel.test.tsx`; auth-bot `python -m pytest tests/test_outbox_delivery.py -q`, `python -m compileall app tests`.
- Исправлен React Compiler lint в `diaweb/frontend/modules/crypton/components/CryptonOfferBuilder.tsx`: убрана ручная memoization массива evolution options от изменяемого shop item object, синхронный `setSelectedEvolution` вынесен из effect в обработчик выбора оффера, effect оставлен только для dev logging. Проверки: `npm run lint -- modules/crypton/components/CryptonOfferBuilder.tsx`, `npm run test -- __tests__/modules/crypton/CryptonOfferBuilder.test.tsx`, полный `npm run lint` (0 errors, существующие warnings). `diaweb/dev` запушен как `29352cd`, `diaweb/master` запушен как merge `ec97049`. GBrain search по `diaweb-code` дважды завершился timeout, исходники проверены напрямую.
