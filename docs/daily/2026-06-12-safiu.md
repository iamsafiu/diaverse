# Daily Work - 2026-06-12 - safiu

## Public digest

- Экран выбора питомцев в рейдах приближен к привычной верстке «Стаи»: карточки стали компактнее и визуально читаемее, а общая цена входа, XP-бонус и предупреждение о нехватке баланса вынесены над списком питомцев.
- Исправлены проблемные сценарии фабрики: склад больше не показывает ложную паузу до построенных цехов, автосборщик Trademaster действительно собирает ресурсы в фоне, а сообщения апгрейда стали понятнее.
- Восстановлен доступ Pro для пользователя, у которого после покупки подписка отображалась неполно в разных разделах приложения.
- Для AI Club добавлена партнерская версия страницы без прямых ссылок на оплату: вступление направляет пользователя к пригласившему партнеру.
- Тестовые аккаунты повторно подготовлены к новому ручному прогону фабрики: прогресс сброшен, ресурсы и игровые балансы пополнены.
- Дополнительный тестовый аккаунт подготовлен к повторному прохождению фабрики с чистым прогрессом.
- Для вступления в клуб без оплаты подготовлена правка: обычный пользователь теперь должен становиться активным участником сразу, с приветствием и распределением на пару; бот-аккаунты остаются исключением.
- Правка вступления в клуб доставлена в основные ветки backend и обновлена в runtime Telegram-бота.
- Начата реализация библиотеки товаров магазина: backend получил отдельный слой library SKU, где базовая цена может быть меньше 1 USDT, а витринная цена будет выводиться из библиотечной цены с защитным floor.
- Staff-интерфейс магазина начал получать typed-контракт библиотеки: появились клиентские методы для library SKU, эволюций питомцев и создания витринной позиции из библиотеки без ручной цены.
- В staff-магазине добавлен рабочий tab «Библиотека»: товар сначала заводится из общего каталога с собственной базовой ценой, а в магазин добавляется уже из библиотеки без ручного ввода цены.
- Библиотека товаров магазина закрыта focused regression-проверками: проверены цены ниже 1 USDT в библиотеке, floor витрины, эволюции питомцев, защита sync/bootstrap и staff UI flow.

## Internal log

- В `diaweb/frontend` обновлен raid pet picker: добавлен общий `RaidPetSelectionSummary`, карточки выбора переведены на Staya-style layout с ассетами из `diaverse-mobile`, из карточек убраны дубли цены входа, XP и коэффициента. Backend-изменения не потребовались: `diaverseapi/dev` уже отдает поля уровня, опыта, эволюции и мутации, а сортировка по XP уже реализована.
- Проверки: `npm run test -- __tests__/modules/raids/RaidPetDrawer.test.tsx`, `npm run typecheck`, `npm run test -- __tests__/modules/raids` в `diaweb/frontend`.
- В `diaverseapi/app/factory` добавлен фоновый warehouse autocollect при чтении state для Trademaster, active warehouse keys для cap/stop metadata и регрессии склада/state; в `diaweb/frontend/modules/factory` обновлены live warehouse статусы, подписи уровней на карте, UX ошибок/модалок апгрейда и i18n.
- Проверки: `.venv\Scripts\python.exe -m pytest app/factory/tests/test_warehouse_service.py app/factory/tests/test_state_service.py app/factory/tests/test_compartment_service.py` в `diaverseapi`; `npm test -- __tests__/modules/factory/FactoryWarehouseLive.test.ts __tests__/modules/factory/factory-api.test.ts __tests__/modules/factory/FactoryDialogs.test.tsx __tests__/modules/factory/FactoryWarehouseScreen.test.tsx __tests__/modules/factory/FactoryShell.test.tsx` и `npm run typecheck` в `diaweb/frontend`.
- Production support fix: по Telegram ID найден активный пользователь без текущего Pro entitlement; свежей успешной оплаты в cabinet/legacy payment таблицах не найдено. В одной транзакции сохранен before-снимок в `incident_backups.manual_step_pass_repairs`, создан активный `personal_fitness_pass` и `user_subscriptions` Pro на `2026-06-12`—`2026-07-12`; проверка active features вернула `pro_fitness_pass` и связанные Pro-фичи.
- Создана партнерская копия `aiclub-site/partners/index.html`: все внешние payment links удалены, верхняя и hero CTA ведут к блоку условий, нижний CTA заменен информационным блоком для обращения к пригласившему партнеру. Страница загружена в Caddy static root, добавлены точечные Caddy matchers для `/partners` -> `/partners/` и `/partners/` -> `partners/index.html`; Caddy validate/reload прошли, `https://aiclub.su/partners/` вернул 200, `/partners` вернул 301, основной `https://aiclub.su/` сохранил оплатные CTA.
- Для 12 фабричных тестовых аккаунтов выполнена повторная серверная транзакция подготовки: удалено 12 существующих `factory_profiles`, повторно начислены `impulse +120000`, `brick +100000`, `slot_token +8`, `rare_pet_fragment +5`, XDV `+250000`, game dollars `+2000`. Итоговая сверка подтвердила 12/12 аккаунтов без `factory_profiles` и достаточные минимальные остатки ресурсов/денег.
- Для дополнительного Telegram ID `8571201012` выполнен точечный factory reset: найден связанный пользователь, удален 1 `factory_profiles` с уровня 2, итоговая проверка подтвердила отсутствие фабричного профиля. Ресурсы и денежные балансы в этой операции не изменялись.
- В `diaverseapi/app/club` локально изменен no-payment Telegram join flow: human `chat_member` без оплаты активируется через обычный `activate_membership`, что запускает welcome outbox и buddy assignment; `telegram_join` в активном in-chat состоянии больше не считается review-only. В `aibot/app/clubbot/handlers.py` добавлена передача `telegram_is_bot`, чтобы backend не активировал bot-аккаунты вроде service/payment bot. Production backend не обновлялся: случайно затронутые исходники на сервере были восстановлены к прежнему состоянию без restart контейнеров, дальнейшее обновление оставлено под CI/CD. Проверки: `diaverseapi` pytest/ruff по club service, `aibot` pytest и compileall; ruff в `aibot` venv отсутствует.
- `diaverseapi` commit `706fdc5d` запушен в `dev`; тот же patch cherry-picked в `main` как `daaa08e7` и запушен. `aibot` commit `1302733` запушен в `dev`, на сервере `/srv/aibot` выполнен fast-forward pull и пересобран только `copywriting-clubbot`; контейнер вернулся в `healthy`. Повторные проверки после cherry-pick/deploy: `diaverseapi` `tests/test_club_service.py` и ruff, `aibot` `tests/test_clubbot_handlers.py`.
- По плану `Библиотека товаров магазина`, Task 1: в `diaverseapi` добавлены `CabShopLibraryItem`, nullable `CabShopItem.library_item_id`, helper `app/cabinet/shop/library_pricing.py`, Alembic revision `shop_library_20260612` с backfill из default/provider USDT-offers, metadata markers `price_source=library_backfill`, сохранением `legacy_price_xdv` и занулением `price_xdv` для library-backed rows. `migrations/env.py` импортирует основные shop-модели. Проверки: `py_compile`, `pytest tests/test_alembic_graph.py -q`, `alembic heads`, `alembic upgrade repair_raid_imm_consumption_20260612:shop_library_20260612 --sql`, `ruff check` по измененным backend-файлам.
- По Task 2 добавлен backend staff API библиотеки: schemas list/create/update/evolution generation, `CabShopLibraryAdminService`, dependency и endpoints `/v1/admin/shop/library` плюс `/v1/admin/shop/library/evolutions/generate`. Validation покрывает наличие item catalog source, character evolution range, positive USDT price и запрет удаления linked library item. При изменении library price/quantity пересчитываются linked default storefront offers через общий price policy. Проверки: `py_compile`, `ruff check` по новым API/service/schema файлам, `pytest tests/test_cabinet_shop_admin.py -q`.
- По Task 3 backend storefront flow переведен на library-backed режим без ломки legacy: `ShopAdminItemCreateRequest` поддерживает `library_item_id`, новые library-backed rows получают `price_xdv=0`, source/variant берутся из `CabShopLibraryItem`, default fulfillment для character rows получает `options_json.evolution`, default provider offer материализуется из library price и storefront quantity с floor `1 USDT`. Добавлен guard на один active listing per library item, response fields `library_item_id/price_source/floor_applied/legacy_price_xdv`, блокировка ручного изменения default price для library-backed offers и checkout guard, который refresh-ит stale default offer перед созданием order/session. Проверки: `py_compile`, `ruff check`, `pytest tests/test_cabinet_shop_admin.py tests/test_cabinet_shop_service.py -q`, `pytest tests/test_cabinet_shop_api.py -q`.
- По Task 4 защищены sync/bootstrap сценарии библиотеки магазина: stale cleanup и `rebuild_all=True` теперь пропускают admin/library-managed items/offers, `sync_preview` учитывает уже существующие library source refs и не предлагает их как missing. Проверки: `py_compile`, `ruff check app/cabinet/shop/bootstrap.py app/cabinet/shop/admin_service.py`, `pytest tests/test_cabinet_shop_bootstrap.py -q`.
- По Task 5 согласованы зависимые shop-системы с новой price policy: category deals и specials больше не создают/не принимают скидочные USDT-offers ниже `1`, общий checkout/storefront price resolver блокирует active offers ниже floor, admin manual discounts ниже floor отклоняются, promo/analytics остаются snapshot/read-only, а bundle savings сначала ищет materialized storefront price для конкретной evolution и использует старые multipliers только как fallback с warning. Проверки: `py_compile`, `ruff check`, `pytest tests/test_cabinet_shop_category_deals.py tests/test_cabinet_shop_specials.py tests/test_cabinet_shop_promo_offer.py tests/test_cabinet_shop_admin.py tests/test_cabinet_shop_service.py -q`.
- По Task 6 в `diaweb/frontend/modules/staff-shop` добавлены типы и API-клиент библиотеки (`ShopAdminLibraryItem`, create/update/disable/generate evolutions), fields `library_item_id/price_source/floor_applied/legacy_price_xdv`, cache invalidation для `["staff-shop","library"]` вместе с items/public shop caches и safe dev logging в client/UI. Проверка: `npm run typecheck`.
- По Tasks 7-9 в `diaweb/frontend/modules/staff-shop` добавлены `ShopLibraryTable` и `ShopLibraryEditor`, tab `Библиотека`, переименование workspace tab в `Магазин`, flow добавления витринной позиции через `library_item_id`, read-only derived price/floor hint, отключение inline/bulk price editing для library-backed rows и сохранение публичного shop UI без использования internal library fields. Проверки: `npm test -- __tests__/modules/staff-shop/ShopAdminPage.test.tsx __tests__/modules/staff-shop/ShopListingEditor.test.tsx __tests__/modules/staff-shop/ShopLibraryEditor.test.tsx __tests__/modules/staff-shop/shop-admin-api.test.ts`, `npm run typecheck`; raw search подтвердил отсутствие `library_item_id/price_source/floor_applied/legacy_price_xdv` в buyer-facing `frontend/modules/shop` и `frontend/app/api/cabinet/shop`.
- По Task 10 добавлены/обновлены проверки: backend `tests/test_cabinet_shop_library_pricing.py`, library API route tests, duplicate active library listing guard, bootstrap protection for library-managed rows/offers, frontend `ShopLibraryEditor` и bulk-edit library-backed regression. Итоговые проверки: backend shop suite + Alembic graph `216 passed`, Ruff `All checks passed`, `alembic heads`, `alembic upgrade repair_raid_imm_consumption_20260612:shop_library_20260612 --sql`, frontend `npm test -- __tests__/modules/staff-shop` (`57 passed`) и `npm run typecheck`.
