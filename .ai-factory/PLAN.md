# Implementation Plan: Club Member Onboarding

Branch: none
Created: 2026-06-16

## Settings
- Testing: yes
- Logging: verbose
- Docs: yes
- Mode: fast
- Referral system: no

## Roadmap Linkage
Milestone: "none"
Rationale: `.ai-factory/ROADMAP.md` is empty; this is a tactical Club onboarding delivery plan.

## Research Context
Source: user brief, `docs/tasks/club/clubDesign.md`, local GBrain `club` runbook, and verified source files.

Goal:
- Add a new authenticated cabinet section `/club` for Diaverse Club members.
- Implement only member onboarding now; future full club cabinet is out of scope.
- Preserve the approved prototype visual structure from `docs/tasks/club/diaverse-club-onboarding` as the visual truth.

Constraints:
- Do not implement referral accounting, cashback, Decardium rewards, referral backend endpoints, or referral bot flows.
- The current Telegram runtime that posts welcome/pair messages is `copywriting-clubbot` in `aibot/app/clubbot`; `diaverseapi` owns message content and club state through `club_tg_outbox`.
- `club10000-bot` remains out of scope unless payment events change; it must not own Diaverse Club onboarding state.
- The onboarding route should use a fullscreen visual mode while onboarding is active, hiding the standard cabinet chrome to avoid conflicts with the prototype dock.
- Activation links must preserve query strings through the existing Telegram login redirect.

Decisions:
- Backend source of truth is `diaverseapi/app/club`.
- Web delivery is through `diaweb` same-origin BFF routes.
- Activation link is generated by `diaverseapi`, inserted into the member-specific Telegram welcome message, and sent by `copywriting-clubbot` as a plain URL.
- Use `CABINET_PUBLIC_BASE_URL` to build `/{locale}/club?activation=<opaque_token>`.
- Use a server-owned opaque activation token tied to `ClubMembership`, not raw Telegram IDs or membership IDs in the URL.
- Store onboarding progress on `ClubMembership.metadata_json["onboarding"]` for MVP; token storage gets a dedicated table for audit and replay protection.
- Channels for MVP:
  - Chat 10000: `https://t.me/+zhqi2tg7AilmNmEy`
  - Channel 10000: `https://t.me/+ZIrcGh-O2qw5M2Iy`
  - Founder channel: `https://t.me/diaverse2030`
  - Anastasia development channel: `https://t.me/anastasiasoulence`

Open questions:
- Bonus file URLs are not supplied yet; implementation must keep bonus cards but avoid inventing file links.
- Exact production host in `CABINET_PUBLIC_BASE_URL` must be configured before live Telegram rollout.

## Workspace Mode
- Mode: multi-repo fast
- Workspace root: `C:\Users\Indigo\Desktop\diaverse`
- Knowledge: local GBrain first through `scripts\gbrain.ps1`, then raw source verification
- Note: GBrain `search` has timed out in this session, but `gbrain get club --source diaverse-docs` worked; source code remains final authority.

## Repository Matrix
| Repository | Path | Affected | Current branch | Current status | Role |
| --- | --- | --- | --- | --- | --- |
| `diaweb` | `C:\Users\Indigo\Desktop\diaverse\diaweb` | yes | `dev` | clean | cabinet route, BFF, onboarding UI, navigation, auth redirect |
| `diaverseapi` | `C:\Users\Indigo\Desktop\diaverse\diaverseapi` | yes | `dev` | clean | club cabinet API, activation token, onboarding state, Telegram welcome link |
| `aibot` | `C:\Users\Indigo\Desktop\diaverse\aibot` | no | `dev` | clean | transport already sends outbox text URLs; no code change planned |
| `club10000-bot` | `C:\Users\Indigo\Desktop\diaverse\club10000-bot` | no | `dev` | clean | payment/referral bot remains out of scope |
| `diaverse-mobile` | `C:\Users\Indigo\Desktop\diaverse\diaverse-mobile` | no | `dev` | clean | mobile app download links only |
| `diaverse-auth-bot` | `C:\Users\Indigo\Desktop\diaverse\diaverse-auth-bot` | no | `dev` | clean | existing Telegram login transport is reused |
| `diaverse` root | `C:\Users\Indigo\Desktop\diaverse` | yes | `dev` | dirty unrelated docs/tasks | owns this plan and daily/GBrain notes only |

## Commit Plan
- **Commit 1 (`diaverseapi`)** after Tasks 1-3 and backend tests: `feat: add club cabinet onboarding api`
- **Commit 2 (`diaweb`)** after Tasks 4-8 and frontend tests: `feat: add club onboarding cabinet route`
- **Commit 3 (`diaverse`)** after Task 11 if docs/daily/GBrain metadata changed: `docs: plan club onboarding rollout`

## Tasks

### Phase 1: Backend Contract And Activation

- [x] Task 1: Add server-owned Club activation token storage and helpers.

  Deliverable: `diaverseapi` can generate, hash, validate, claim, expire, and audit opaque activation tokens for club memberships.

  Expected behavior:
  - Add `ClubActivationToken` or equivalent model/table tied to `ClubMembership`.
  - Store only a token hash, never the raw token.
  - Include `membership_id`, `program_id`, `expires_at`, `claimed_at`, `claimed_by_user_id`, `created_reason`, and audit timestamps.
  - Generate URLs as `${CABINET_PUBLIC_BASE_URL}/{locale}/club?activation=<token>`.
  - Reject expired, already-claimed, unknown, and membership-mismatched tokens.
  - Claim succeeds only if the authenticated user matches the membership by `user_id` or Telegram identity; do not let a copied group link bind another account.
  - Use short explicit Alembic constraint/index names and verify PostgreSQL SQL compilation.

  Files:
  - `diaverseapi/app/club/models.py`
  - `diaverseapi/app/club/repositories.py`
  - `diaverseapi/app/club/service.py`
  - `diaverseapi/app/club/schemas.py`
  - `diaverseapi/app/core/settings.py`
  - `diaverseapi/.env.example`
  - `diaverseapi/migrations/versions/*_club_onboarding_activation.py`
  - `diaverseapi/tests/test_club_onboarding_activation.py`

  Logging requirements:
  - DEBUG for token generation inputs, hash lookup result, and claim decision branches.
  - INFO when an activation link is generated or a token is claimed.
  - WARNING for expired, reused, mismatched, or missing-token attempts.
  - Never log raw activation tokens, auth cookies, signatures, or full Telegram private identifiers.

  Dependencies: none.

- [x] Task 2: Add customer-facing club cabinet API for onboarding state.

  Deliverable: authenticated cabinet users can fetch club state, claim activation, save onboarding progress, and complete onboarding through `/v1/cabinet/club/*`.

  Expected behavior:
  - Create `app.club.cabinet_api` and register it in `app/routers/v1/endpoints.py` with prefix `cabinet/club`.
  - Add:
    - `GET /v1/cabinet/club/me`
    - `POST /v1/cabinet/club/activation/claim`
    - `PATCH /v1/cabinet/club/onboarding/progress`
    - `POST /v1/cabinet/club/onboarding/complete`
  - Use `Security(get_current_user)` with `x-platform: cabinet`.
  - Resolve active membership by `user_id`, then by Telegram identity, and call existing auto-link behavior where safe.
  - Return `access_state`, `membership`, `program`, `onboarding`, `buddy`, `channels`, `bonuses`, `app_downloads`, and `referral_enabled=false`.
  - Build buddy data from active `club_buddy_members` / `club_buddy_groups`, exposing only safe public fields: display name, avatar URL if available, username link if available, role, and pairing cycle.
  - Store progress in `ClubMembership.metadata_json["onboarding"]` with completed steps/actions and `completed_at`.
  - Channels must default to the four supplied Telegram URLs, with ability to override from program metadata.
  - Bonus cards may be returned with disabled or missing URLs until real file links are supplied.
  - Do not add referral endpoints, referral link generation, Decardium balance, cashback logic, or bot referral integration.

  Files:
  - `diaverseapi/app/club/cabinet_api.py`
  - `diaverseapi/app/club/schemas.py`
  - `diaverseapi/app/club/service.py`
  - `diaverseapi/app/club/repositories.py`
  - `diaverseapi/app/club/dependencies.py`
  - `diaverseapi/app/routers/v1/endpoints.py`
  - `diaverseapi/tests/test_club_cabinet_api.py`

  Logging requirements:
  - DEBUG for membership resolution path, safe buddy projection, and metadata progress merge.
  - INFO for activation claim success, onboarding progress update, and completion.
  - WARNING for no active access, identity conflicts, missing membership, and invalid step/action ids.
  - Logs must not include activation raw tokens, cookies, private channel invite secrets beyond configured URL ids, or raw request bodies.

  Dependencies: Task 1.

- [x] Task 3: Insert the activation URL into the Telegram group welcome flow.

  Deliverable: when `diaverseapi` queues the member welcome message, the text includes the member-specific club activation URL and `copywriting-clubbot` sends it through the existing outbox path.

  Expected behavior:
  - Extend the welcome template renderer to support `{activation_url}`.
  - Generate or reuse an unclaimed activation token before `_enqueue_group_welcome_message` renders the welcome message.
  - Update the default welcome template to mention activation in Russian.
  - Keep pair assignment message focused on buddy pairing; do not add one ambiguous shared activation URL to a pair/group notice.
  - Update staff settings schema/help text so operators know `{activation_url}` is supported.
  - Do not modify `aibot` unless plain-text URL delivery fails in verification; current `send_message` outbox path already sends text with auto-linked URLs.

  Files:
  - `diaverseapi/app/club/service.py`
  - `diaverseapi/app/club/admin_api.py`
  - `diaverseapi/app/club/schemas.py`
  - `diaverseapi/tests/test_club_service.py`
  - `diaverseapi/tests/test_club_auth_and_clubbot.py`
  - `docs/club.md` or `docs/features/club.md` only if operator docs need template notes

  Logging requirements:
  - DEBUG for welcome rendering with activation URL present/absent as a boolean only.
  - INFO when a welcome outbox command is queued with activation enabled.
  - WARNING when `CABINET_PUBLIC_BASE_URL` is missing or token generation fails.
  - Never log full activation URLs or raw tokens.

  Dependencies: Tasks 1-2.

### Phase 2: Frontend BFF, Auth, And Route Shell

- [x] Task 4: Add `diaweb` BFF routes and client types for the club cabinet API.

  Deliverable: the browser talks to `/api/cabinet/club/*`, and those routes proxy to `diaverseapi` with cabinet cookies and `x-platform: cabinet`.

  Expected behavior:
  - Add a `club` BFF utility modeled after factory/raids/shop BFF utilities.
  - Proxy:
    - `GET /api/cabinet/club/state`
    - `POST /api/cabinet/club/activation/claim`
    - `PATCH /api/cabinet/club/onboarding/progress`
    - `POST /api/cabinet/club/onboarding/complete`
  - Preserve upstream status codes and `set-cookie` behavior where relevant.
  - Add typed client helpers and response normalizers for the onboarding UI.
  - Treat `referral_enabled=false` as a first-class response state.

  Files:
  - `diaweb/frontend/app/api/cabinet/club/_utils.ts`
  - `diaweb/frontend/app/api/cabinet/club/state/route.ts`
  - `diaweb/frontend/app/api/cabinet/club/activation/claim/route.ts`
  - `diaweb/frontend/app/api/cabinet/club/onboarding/progress/route.ts`
  - `diaweb/frontend/app/api/cabinet/club/onboarding/complete/route.ts`
  - `diaweb/frontend/modules/club-onboarding/api.ts`
  - `diaweb/frontend/modules/club-onboarding/types.ts`
  - `diaweb/frontend/__tests__/app/api/cabinet/club/*.test.ts`

  Logging requirements:
  - DEBUG in non-production for upstream start/success with endpoint and method.
  - WARNING for non-2xx upstream responses.
  - ERROR for transport failures with duration and endpoint.
  - Do not log cookies, activation tokens, full activation URLs, or raw response bodies.

  Dependencies: Task 2.

- [x] Task 5: Preserve activation query strings through cabinet auth redirect and make `/club` auth-only.

  Deliverable: unauthenticated users opening `/ru/club?activation=...` are redirected to Telegram login and return to the exact activation URL after login.

  Expected behavior:
  - Add `/club` to auth-only cabinet routes.
  - Update `CabinetLayout` / `buildCabinetLoginHref` usage to include `searchParams` in redirect, not only `pathname`.
  - Keep existing guest-enabled routes unchanged.
  - Add route-motion rank for `/club` so navigation animation is deterministic.
  - Add tests for `buildCabinetLoginHref` preserving `?activation=abc`.

  Files:
  - `diaweb/frontend/modules/cabinet/routeAccess.ts`
  - `diaweb/frontend/modules/cabinet/components/CabinetLayout.tsx`
  - `diaweb/frontend/modules/cabinet/components/CabinetRouteTransition.tsx`
  - `diaweb/frontend/modules/cabinet/__tests__/routeAccess.test.ts` or existing nearest test file

  Logging requirements:
  - DEBUG for auth bootstrap route mode including `club`.
  - WARNING for protected club route redirect without user, logging only path/query presence, not token value.
  - Do not log activation token values.

  Dependencies: Task 4 can proceed in parallel, but this must be done before end-to-end activation QA.

- [x] Task 6: Add fullscreen club route mode to the cabinet shell.

  Deliverable: `/club` renders the onboarding experience without standard cabinet topbar/footer/bottom nav, while the route remains reachable from the cabinet navigation.

  Expected behavior:
  - Add a `usesClubFullscreenRoute` or equivalent flag for `/${lang}/club`.
  - Hide `CabinetTopbar`, `CabinetFooter`, and `BottomNav` on `/club` while preserving cabinet auth bootstrap and background.
  - Keep fullscreen height stable on mobile with safe-area bottom padding from the prototype, not from the cabinet bottom nav.
  - Future club hub can reuse the route; no separate marketing/landing page should be created.

  Files:
  - `diaweb/frontend/modules/cabinet/components/CabinetLayout.tsx`
  - `diaweb/frontend/modules/cabinet/components/CabinetRouteTransition.tsx` if route animation needs adjustment
  - `diaweb/frontend/__tests__/modules/cabinet/CabinetLayout.test.tsx` or nearest existing cabinet test file

  Logging requirements:
  - DEBUG in development for resolved shell mode: default, game fullscreen, or club fullscreen.
  - No sensitive payload logging.

  Dependencies: Task 5.

### Phase 3: Onboarding UI Port

- [x] Task 7: Port the approved prototype into `diaweb` as a scoped Club onboarding module.

  Deliverable: `diaweb` has a faithful React/TypeScript port of `docs/tasks/club/diaverse-club-onboarding` with copied assets and scoped styling.

  Expected behavior:
  - Copy the seven-step prototype structure, text rhythm, layout, and mobile/desktop behavior as the visual baseline.
  - Add `@phosphor-icons/react` to `diaweb/frontend/package.json` and lockfile because the prototype uses Phosphor icons.
  - Copy bitmap/SVG assets to `diaweb/frontend/public/club/onboarding/*`.
  - Scope prototype CSS under a root class to prevent global `body`, `button`, and `:root` leakage into the cabinet.
  - Preserve Exo 2 and Orbitron typography, preferably through Next font loading or a scoped import.
  - Desktop remains a centered onboarding block with the same top progress bar; no side rail and no inner scroll container.
  - QR codes for app download appear only on desktop.
  - Referral step must not create or copy a real personal referral link. Keep the visual step only as disabled/deferred if needed to preserve flow, with `referral_enabled=false` controlling behavior.
  - Initial asset optimization is not required; after parity is verified, compression to WebP/AVIF can be a follow-up.

  Files:
  - `diaweb/frontend/package.json`
  - `diaweb/frontend/package-lock.json`
  - `diaweb/frontend/modules/club-onboarding/components/ClubOnboardingClient.tsx`
  - `diaweb/frontend/modules/club-onboarding/components/ClubOnboardingFrame.tsx`
  - `diaweb/frontend/modules/club-onboarding/components/ClubOnboardingSteps.tsx`
  - `diaweb/frontend/modules/club-onboarding/components/club-onboarding.css`
  - `diaweb/frontend/modules/club-onboarding/index.ts`
  - `diaweb/frontend/public/club/onboarding/*`

  Logging requirements:
  - DEBUG in development for step changes, action acknowledgements, and disabled referral state.
  - WARNING in development for missing backend data falling back to prototype defaults.
  - Do not log activation token values, Telegram invite URLs beyond public channel constants, or auth state payloads.

  Dependencies: Tasks 4 and 6.

- [x] Task 8: Add the `/[lang]/club` page and bind UI state to backend progress.

  Deliverable: authenticated users can open `/ru/club`, claim activation, see buddy/channel/app/bonus data, progress through onboarding, and complete into a simple v0 club hub state.

  Expected behavior:
  - Create `app/[lang]/(cabinet)/club/page.tsx`.
  - On load, fetch state through the BFF.
  - If `activation` query is present, claim it once and then refetch state.
  - If no active club access exists, show a small access/support state inside the same visual shell.
  - Use backend data for buddy, app links, channels, bonus cards, and onboarding progress.
  - Track optional actions: greet, write buddy, open store, open channel, open bonus, complete.
  - Persist step/action progress with debounce or explicit step navigation calls.
  - On completion, call backend complete endpoint and render a minimal club hub state rather than a dead-end toast.
  - The v0 hub may show mission "10 000 steps", buddy summary, app links, channels, and bonuses; no leaderboard/referral system.

  Files:
  - `diaweb/frontend/app/[lang]/(cabinet)/club/page.tsx`
  - `diaweb/frontend/modules/club-onboarding/components/ClubOnboardingClient.tsx`
  - `diaweb/frontend/modules/club-onboarding/api.ts`
  - `diaweb/frontend/modules/club-onboarding/types.ts`
  - `diaweb/frontend/modules/i18n/dictionaries/ru.json`
  - `diaweb/frontend/modules/i18n/dictionaries/en.json`
  - `diaweb/frontend/__tests__/modules/club-onboarding/ClubOnboardingClient.test.tsx`

  Logging requirements:
  - DEBUG in development for state fetch, activation claim lifecycle, progress save, and completion.
  - WARNING for activation claim failures, no-access state, or backend schema mismatch.
  - Do not log activation token values, auth cookies, or full backend payloads.

  Dependencies: Tasks 2, 4, 7.

### Phase 4: Navigation And Product Integration

- [x] Task 9: Add `Клуб` to cabinet navigation and localization.

  Deliverable: `Клуб` appears among shop/factory/raids/offers/profile tabs and routes to `/club`.

  Expected behavior:
  - Add `club` navigation labels to `ru.json` and `en.json`.
  - Add `club` icon kind to topbar and bottom nav.
  - Use a production-safe icon asset derived from the onboarding paw mark or a small dedicated cabinet nav icon.
  - Include `/club` in topbar and bottom nav in the requested position among existing cabinet sections.
  - Keep staff-only nav behavior unchanged.
  - Update `Sidebar` / `MobileCabinetMenu` only if still reachable in current shell.
  - Ensure desktop/mobile nav text does not overflow when the tab count increases.

  Files:
  - `diaweb/frontend/modules/cabinet/components/CabinetTopbar.tsx`
  - `diaweb/frontend/modules/cabinet/components/BottomNav.tsx`
  - `diaweb/frontend/modules/cabinet/components/BottomNavIcon.tsx`
  - `diaweb/frontend/modules/cabinet/components/Sidebar.tsx`
  - `diaweb/frontend/modules/cabinet/components/MobileCabinetMenu.tsx`
  - `diaweb/frontend/modules/i18n/dictionaries/ru.json`
  - `diaweb/frontend/modules/i18n/dictionaries/en.json`
  - `diaweb/frontend/public/cabinet/nav-club.*`

  Logging requirements:
  - No runtime logging required beyond existing navigation pending logs.
  - If adding development diagnostics, log only route key and active state.

  Dependencies: Task 5.

### Phase 5: Verification, Docs, And Knowledge Sync

- [x] Task 10: Add focused backend regression coverage and migration checks.

  Deliverable: backend activation, state resolution, progress persistence, buddy projection, channel config, no-referral scope, and Telegram welcome link rendering are covered by tests.

  Expected behavior:
  - Test activation token generation stores only hashes.
  - Test activation claim succeeds only for matching authenticated user/Telegram identity.
  - Test copied/mismatched/expired/reused tokens are rejected.
  - Test `/v1/cabinet/club/me` returns no-access, active-member, active-buddy, and no-buddy states.
  - Test progress updates merge safely into membership metadata.
  - Test completion sets `completed_at` idempotently.
  - Test channels return the four supplied URLs.
  - Test `referral_enabled=false` and no referral link is returned.
  - Test Telegram welcome renderer supports `{activation_url}` without logging or exposing raw token in assertions.
  - Test Alembic graph and SQL DDL for the new token migration.

  Verification commands:
  ```powershell
  cd C:\Users\Indigo\Desktop\diaverse\diaverseapi
  .\.venv\Scripts\python.exe -m pytest tests/test_club_onboarding_activation.py tests/test_club_cabinet_api.py tests/test_club_service.py tests/test_club_auth_and_clubbot.py -q
  .\.venv\Scripts\python.exe -m pytest tests/test_alembic_graph.py -q
  .\.venv\Scripts\python.exe -m alembic heads
  .\.venv\Scripts\python.exe -m alembic upgrade <down_revision>:<new_revision> --sql
  .\.venv\Scripts\python.exe -m ruff check app/club tests/test_club_onboarding_activation.py tests/test_club_cabinet_api.py
  ```

  Logging requirements:
  - Tests should assert warning branches where existing log capture patterns make sense.
  - Test fixtures must not include real Telegram invite links except the supplied public/channel URLs and must not include secrets.

  Dependencies: Tasks 1-3.

- [x] Task 11: Add focused frontend tests, typecheck, and visual QA.

  Deliverable: route access, BFF proxying, onboarding UI behavior, fullscreen shell, navigation, and visual fidelity are verified.

  Expected behavior:
  - BFF route tests cover success, upstream error, and transport failure.
  - Auth redirect test proves `/ru/club?activation=abc` redirects to login with full query preserved.
  - Component tests cover activation claim once, no-access state, buddy display, channel actions, bonus disabled URLs, referral disabled state, progress saves, and completion.
  - Navigation tests cover `club` tab visibility and active state.
  - Run browser QA at 360x800, 390x844, and 900x900.
  - Compare against prototype screenshots/QA notes, checking no text overflow, no bottom dock collision, desktop QR-only behavior, and fullscreen shell without cabinet chrome.

  Verification commands:
  ```powershell
  cd C:\Users\Indigo\Desktop\diaverse\diaweb\frontend
  npm test -- __tests__/app/api/cabinet/club
  npm test -- __tests__/modules/club-onboarding
  npm test -- __tests__/modules/cabinet
  npm run typecheck
  npm run lint
  npm run build
  ```

  Browser smoke:
  ```text
  Start diaweb dev server.
  Open http://localhost:3000/ru/club?activation=test-token with mocked/backend dev state.
  Verify login redirect preserves activation query.
  Verify authenticated route claims activation once.
  Verify mobile 360x800 and 390x844 have no text or dock overlap.
  Verify desktop 900x900 shows centered onboarding block and app QR codes.
  Verify standard cabinet topbar/footer/bottom nav are hidden on /club.
  ```

  Logging requirements:
  - Frontend tests should not snapshot activation token values.
  - Development logs may include route names, step ids, and action ids only.

  Dependencies: Tasks 4-9.

- [x] Task 12: Update operator docs/daily notes and sync local knowledge after implementation.

  Deliverable: workspace docs and local GBrain reflect the new club onboarding contract.

  Expected behavior:
  - Update docs only for operator-relevant behavior: activation template variable, channels, frontend route, and no-referral MVP scope.
  - Append daily work entry in Russian with safe public digest and internal log.
  - Run targeted GBrain sync for `diaverse-docs`, `diaverse-aif`, `diaweb-code`, `diaverseapi-code`, and `aibot-code` if docs/code changed.

  Files:
  - `docs/club.md` or `docs/features/club.md` if operator docs need updating
  - `docs/daily/2026-06-16-safiu.md`
  - `.ai-factory/PLAN.md`

  Verification command:
  ```powershell
  cd C:\Users\Indigo\Desktop\diaverse
  powershell -ExecutionPolicy Bypass -File .\scripts\gbrain-sync.ps1
  ```

  Logging requirements:
  - Public digest must not include secrets, raw tokens, private infrastructure details, internal IPs, or raw stack traces.
  - Internal log may include file paths and verification commands, but no secrets.

  Dependencies: Tasks 10-11.

## Out Of Scope
- Referral system, cashback, Decardium accounting, referral link generation, and bot referral flows.
- Mobile implementation beyond existing App Store / Google Play links.
- Full club dashboard with leaderboards, payments, renewal management, or historical stats.
- Changes to `club10000-bot` payment/referral runtime.
- New Telegram bot runtime or auth flow.
- Verification of real Telegram channel subscriptions in MVP.
- Optimizing or redesigning prototype assets before visual parity.

## Implementation Notes

- Backend activation, cabinet API, welcome activation URL, and no-referral MVP scope are implemented in `diaverseapi`.
- Frontend BFF, `/[lang]/club`, fullscreen shell mode, auth redirect query preservation, scoped onboarding UI, and cabinet navigation are implemented in `diaweb`.
- Verification completed with focused backend tests, Alembic head/offline SQL checks, Ruff, focused frontend tests, typecheck, targeted ESLint, and production build.
- Full browser visual QA, daily note update, and GBrain sync were intentionally skipped per user request on 2026-06-16.

## Risks And Guards
- **Activation query loss:** current cabinet auth redirect uses `pathname`; preserve full path with query before onboarding QA.
- **Copied group link:** activation claim must match authenticated Telegram/user identity before linking or exposing membership state.
- **Global CSS leakage:** prototype CSS must be scoped under the club onboarding root.
- **Cabinet chrome collision:** `/club` must hide standard bottom nav/topbar while onboarding is active/fullscreen.
- **Referral ambiguity:** visible UI must not create a fake personal referral promise; backend returns `referral_enabled=false`.
- **Bonus URL gaps:** keep cards visible but avoid inventing file URLs.
- **Telegram template safety:** do not log raw activation URLs, and do not put a single member activation URL in pair/group notices for multiple members.
- **Asset payload weight:** copy as-is for parity, then optimize only after screenshots match.
- **GBrain staleness:** source code wins over GBrain if they disagree; run targeted sync after implementation.

## Next Step
Run `/aif-implement` from `C:\Users\Indigo\Desktop\diaverse` to execute this fast plan.
